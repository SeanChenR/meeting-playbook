## 1. i18n locale 同步 — Decision 5: i18n — Playbook → 劇本

Covers REMOVED-/ADDED-relevant strings: i18n SHALL provide Chinese "劇本" terminology for Playbook UI strings and column headers.

- [x] 1.1 寫紅燈 locale alignment 測試：擴充 `packages/web/src/locales/locales.test.ts` 斷言 14 個新 / 改 key（playbook.heading + 3 column headers + 9 menu + 2 summary chat slot）在 zh-TW 與 en 兩個 locale 同時存在。執行 `bun --filter @meeting-playbook/web test locales` 應失敗（key 尚未加入）。驗證：該測試 fail。
- [x] 1.2 [P] 在 `packages/web/src/locales/zh-TW.json` 寫入 14 個 key 的中文值（依 spec 表格：劇本 / 逐字稿 / 戰術建議 / 編輯會議 / 管理標籤 / 附件 / 相關會議 / ASR 引擎 / 錄音模式 / 重跑轉錄 / 刪除會議 / 摘要對話 / 即將推出），並把既有 `playbook.heading` 從 `"Playbook"` 改為 `"劇本"`。實作 spec ADDED requirement: i18n SHALL provide Chinese "劇本" terminology for Playbook UI strings and column headers。驗證：1.1 測試由紅轉綠。
- [x] 1.3 [P] 在 `packages/web/src/locales/en.json` 同步寫入對應英文值（Playbook / Transcript / Advisor / Edit meeting / Manage tags / Attachments / Linked meetings / ASR provider / Recording mode / Re-run transcription / Delete meeting / Summary Q&A / Coming soon）。驗證：1.1 locale alignment 測試保持綠。

## 2. 建 MeetingHeaderBar — Decision 1: MeetingHeaderBar 取代 MetadataCard

Covers ADDED: MeetingHeaderBar SHALL render core metadata as a single horizontal row. Replaces REMOVED: MetadataCard SHALL render as a two-column layout with embedded CaptureIndicator + Detail page metadata card exposes ASR provider selector, recording badge, and re-run action.

- [x] 2.1 寫紅燈測試 `packages/web/src/components/meeting-header-bar.test.tsx`：mount `<MeetingHeaderBar>` with mock meeting（scheduled status / 14:30–15:30 / 林經理 / Sean）；斷言 `data-testid="meeting-header-bar"` 存在、文字含 title、status 文字、scheduled time 文字、對方 + 我方 顯示名；斷言主要 action 群依 status 顯示 Start / Export（scheduled idle 時）或 End（in_progress）；斷言 `data-testid="meeting-metadata-card"` 不在 DOM。驗證：測試 fail（元件尚未實作）。
- [x] 2.2 實作 `packages/web/src/components/meeting-header-bar.tsx`：兩行結構（第一行 title + status + time + 對方/我方 + capture indicator + recording 指示燈；第二行 actions + ⋯ trigger slot）。props: `{ meeting, phase, bucket, onStart, onEnd, startDisabled, captureIndicator, menuTrigger, uploadSlot?, exportSlot? }`；元件本身**不**處理 menu 的開合，由 props 注入 `menuTrigger` 元素。實作 spec ADDED: MeetingHeaderBar SHALL render core metadata as a single horizontal row；同時退役 REMOVED: MetadataCard SHALL render as a two-column layout with embedded CaptureIndicator 與 Detail page metadata card exposes ASR provider selector, recording badge, and re-run action 兩條（card 結構消失、ASR/Rerun 移入 ⋯ menu、Recording badge 入 header bar）。觀察行為：依 status/bucket 顯示對應 action 按鈕；無 MetadataCard 結構（沒有 Card 外框）。驗證：2.1 測試由紅轉綠。

## 3. 建 MeetingOverflowMenu — Decision 2: MeetingOverflowMenu — 統一收 8 個次要 affordance

Covers ADDED: MeetingOverflowMenu SHALL consolidate secondary affordances behind a single ⋯ trigger. Replaces REMOVED: Meeting detail page renders an Attachments section with the dropzone component + MetadataCard header renders a related-meetings section between prev/next nav and meta.

- [x] 3.1 寫紅燈測試 `packages/web/src/components/meeting-overflow-menu.test.tsx`：mount `<MeetingOverflowMenu>` with `status="scheduled"`、`phase="idle"`、attachments=3、links=2；click trigger 後斷言 menu 開啟、9 個 items 順序為 edit/tags/attachments/linked/(separator)/asr/mode/rerun/(separator)/delete；attachments 與 linked items 含數字 badge `3`/`2`；delete 項套用 destructive 樣式。再寫一條：`phase="in_progress"` 時 menu 不含 `mode` 項。驗證：測試 fail。
- [x] 3.2 實作 `packages/web/src/components/meeting-overflow-menu.tsx`：用 shadcn DropdownMenu，依條件渲染 9 項（每項 emit `data-testid="meeting-menu-item-<key>"`），trigger 為 ⋯ icon button (`data-testid="meeting-overflow-trigger"`)。props 接 8 個 callback（onEdit / onTagsOpen / onAttachmentsOpen / onLinkedOpen / onAsrOpen / onModeOpen / onRerun / onDelete）+ counts (attachmentsCount / linksCount) + 條件旗標 (showModeItem)。實作 spec ADDED: MeetingOverflowMenu SHALL consolidate secondary affordances behind a single ⋯ trigger；同時退役 REMOVED: Meeting detail page renders an Attachments section with the dropzone component 與 MetadataCard header renders a related-meetings section between prev/next nav and meta（附件與相關會議都改成 menu 內的 dialog 觸發）。觀察行為：點任一 item 觸發對應 callback；mode item 在 `showModeItem === false` 時整項 unmount（非 disabled）。驗證：3.1 測試由紅轉綠。

## 4. Workspace 永遠三欄 — Decision 3: Workspace 永遠三欄、移除 LayoutSwitcher

Covers ADDED: Workspace SHALL always render the three columns side-by-side with equal width + Each workspace column SHALL render its own header with a localised title and column-specific tools. Replaces REMOVED: Columns mode renders three panes in a 30/40/30 grid with full-width meta + Stack mode renders the three panes vertically in P-T-A order + Workspace layout SHALL switch between three-column and stacked via animate-ui + Detail page exposes a layout switcher persisted across sessions.

- [x] 4.1 寫紅燈測試 `packages/web/src/components/workspace.test.tsx`：mount `<Workspace>`（props 只剩 `playbook / transcript / advisor`），斷言（a）三個 column wrapper `data-testid="meeting-column-playbook|transcript|advisor"` 依該順序存在；（b）每個 column 含 header 區段，分別顯示 i18n `meetings.detail.columnPlaybook|columnTranscript|columnAdvisor` 文案；（c）outer container 套 `max-w-[1380px] mx-auto`；（d）不再接受 `layout` prop（TS error 或 runtime ignore）。驗證：測試 fail。
- [x] 4.2 改寫 `packages/web/src/components/workspace.tsx`：移除 `layout` prop；永遠渲染 `grid-cols-3` (`1fr 1fr 1fr`)；每欄包一個新內部元件 `<MeetingColumn title={...} tools={...}>{children}</MeetingColumn>`，欄頭 `sticky top-0` + 標題 + 工具 slot，內容區 `flex-1 overflow-y-auto`；欄寬 `min-w-[340px] max-w-[460px]`；整體 `max-w-[1380px] mx-auto gap-4 px-6`。實作 spec ADDED: Workspace SHALL always render the three columns side-by-side with equal width 與 Each workspace column SHALL render its own header with a localised title and column-specific tools；同時退役 REMOVED: Columns mode renders three panes in a 30/40/30 grid with full-width meta（30/40/30 → 1fr 1fr 1fr）與 Workspace layout SHALL switch between three-column and stacked via animate-ui（移除 stack 模式切換）。觀察行為：永遠三欄並排、每欄獨立 scroll、欄頭固定。驗證：4.1 測試由紅轉綠。
- [x] 4.3 [P] 補對應 column tools slot：playbook 欄頭右側帶現有的 freeform/structured toggle + AI 草稿 badge（從 `playbook-pane.tsx` 內抽出來改成 prop）；transcript 欄頭右側帶 rerun overlay 狀態指示（沿用 `transcript-pane.tsx` 既有元素）；advisor 欄頭右側帶清空對話按鈕（沿用 `advisor-pane.tsx` 既有元素，若不存在則新增）。觀察行為：欄頭 sticky 時工具按鈕仍可點。驗證：手動瀏覽器確認 + 既有 pane 測試保持綠。

## 5. 三欄等高 + 獨立 scroll

Covers ADDED: The three workspace columns SHALL share an identical computed height with independent scrolling. Replaces REMOVED: Columns mode three panes share equal visible height (slice-7 round 2).

- [x] 5.1 在 `workspace.tsx` 內注入 CSS variable `--detail-column-h: calc(100vh - var(--detail-top-stack, 220px) - var(--mini-player-h, 60px))`，三個 `MeetingColumn` 用 `height: var(--detail-column-h)` 撐高。在 `detail.tsx` wrap 用 `ResizeObserver` 監測 top stack 高度（prev/next + header bar + 可選 alert + tabs）與 mini-player 高度，動態更新 CSS variable。實作 spec ADDED: The three workspace columns SHALL share an identical computed height with independent scrolling；同時退役 REMOVED: Columns mode three panes share equal visible height (slice-7 round 2)（移除「ONLY in columns mode」條件，永遠成立）。觀察行為：當 mini-player 顯示 / 隱藏或 alert 出現 / 消失時，三欄高度同步調整且 scroll 區獨立。驗證：在 `workspace.test.tsx` 加 case 模擬 ResizeObserver 通知後三欄 height 一致；手動瀏覽器確認。
- [x] 5.2 [P] 確保三欄 overflow 行為正確：mock 一條長 transcript 內容（>200 chunks）、短 playbook、短 advisor；斷言 transcript column scroll 不會推開其他兩欄、其他兩欄 scroll 條獨立。在 `detail.test.tsx` 補一條 scenario test。

## 6. detail.tsx 整合 — Decision 1+2: 換掉 MetadataCard、串 MeetingHeaderBar / Menu / SummaryView

Covers MODIFIED: Detail page wraps existing workspace in a Tabs UI with a gated Summary tab + Meeting detail page SHALL render a prev/next navigation group alongside the BackLink. Also wires up Decision 7: 不做手機端 — 1024px 以下不保證可用，本任務驗證範圍以 1440px 為基準。

- [x] 6.1 寫紅燈測試擴充 `packages/web/src/routes/meetings/detail.test.tsx`：載入 `/meetings/$id` 後斷言（a）`data-testid="meeting-metadata-card"` 不存在、`data-testid="meeting-header-bar"` 存在；（b）`data-testid="meeting-overflow-trigger"` 存在；（c）點 trigger 開 menu 後 9 個 items 全部可見；（d）點 `meeting-menu-item-delete` 開啟現有 delete confirm dialog；（e）點 `meeting-menu-item-attachments` 開啟 `data-testid="meeting-attachments-dialog"` 含 dropzone；（f）`data-testid="meeting-detail-attachments-section"` 不再存在；（g）`data-testid="meeting-detail-tags-row"` 不再存在；（h）`data-testid="meeting-edit-toggle"` 不再存在。驗證：測試 fail。
- [x] 6.2 改寫 `packages/web/src/routes/meetings/detail.tsx`：移除 MetadataCard import + render；移除獨立 tags 列 + editToggle 按鈕 + attachments section；改為渲染 `<MeetingPrevNextNav>` → `<MeetingHeaderBar menuTrigger={<MeetingOverflowMenu .../>}>` → 條件式 `<HeadphonesHint>` inline alert → tabs → tab panel；overflow menu 的 8 個 callback 在 detail.tsx 內 wire 到對應 dialog state (`editDialogOpen` / `tagsDialogOpen` / `attachmentsDialogOpen` / `linkedDialogOpen` / `asrDialogOpen` / `modeDialogOpen`)，Rerun 與 Delete 維持現有觸發路徑。實作 spec MODIFIED: Detail page wraps existing workspace in a Tabs UI with a gated Summary tab 與 Meeting detail page SHALL render a prev/next navigation group alongside the BackLink（summary tab 內換成 SummaryView 雙欄；BackLink 已收進 prev/next 列）。觀察行為：頁面結構符合 spec（無 MetadataCard / 無 inline attachments / 無獨立 tags 列）；所有原本可達的操作仍可透過 menu 達成。驗證：6.1 測試由紅轉綠 + 既有 detail.test.tsx 測試保持綠（含 i18n、prev/next nav、mini-player 等）。

## 7. 摘要 tab 雙欄 — Decision 4: MeetingDetailSummaryView 雙欄

Covers ADDED: Summary tab SHALL render a two-column layout with the markdown summary on the left and an AI-chat placeholder slot on the right.

- [x] 7.1 寫紅燈測試 `packages/web/src/components/meeting-detail-summary-view.test.tsx`：mount `<MeetingDetailSummaryView meetingId="m_1" meeting={{status:"completed", ...}} />`，斷言 `data-testid="meeting-summary-view"` 存在、grid 為兩欄等寬、左欄含 `<SummaryPane>` 內容、右欄 `data-testid="meeting-summary-chat-slot"` 含 i18n `摘要對話` + `即將推出` 文字；用 fetch mock 斷言渲染期間沒有對任何 `/chat` / `/summary-qa` 之類 endpoint 發 request。驗證：測試 fail（元件尚未實作）。
- [x] 7.2 實作 `packages/web/src/components/meeting-detail-summary-view.tsx`：CSS Grid `grid-cols-[1fr_1fr]`、`gap-4`、`max-w-[1380px] mx-auto`；左欄渲染 `<SummaryPane>` 不動；右欄渲染 placeholder section（含 header「摘要對話」+ 灰階圖示 + 「即將推出」文案，純靜態無 fetch）。實作 spec ADDED: Summary tab SHALL render a two-column layout with the markdown summary on the left and an AI-chat placeholder slot on the right。觀察行為：兩欄等寬、整塊置中、右欄為純 placeholder。驗證：7.1 測試由紅轉綠。
- [x] 7.3 [P] 在 `detail.tsx` 把 summary tab 從直接 mount `<SummaryPane>` 改成 mount `<MeetingDetailSummaryView>`。擴充 `detail.test.tsx`：completed meeting 切到 summary tab 後斷言 `meeting-summary-view` 存在且左 / 右兩欄都有內容、右欄 placeholder 可見。驗證：所有 detail.test.tsx 測試綠。

## 8. HeadphonesHint inline alert — Decision 6: HeadphonesHint 改 inline alert

Covers ADDED: HeadphonesHint SHALL render as a dismissible inline alert when dual mode is selected before start.

- [x] 8.1 寫紅燈測試擴充 `packages/web/src/components/headphones-hint.test.tsx`：mount `<HeadphonesHint visible />` 斷言 root element 套 shadcn `<Alert>` 的 class（含 `role="alert"`）而非 Card 結構；dismiss 按鈕內含 Lucide `X` icon 的 `<svg>`（從 `lucide-react` import）且 alert 文字節點不含 `×` (U+00D7) 或 `✕` (U+2715) 字元；點擊 dismiss 按鈕後元件 unmount。驗證：測試 fail（目前是 Card）。
- [x] 8.2 改寫 `packages/web/src/components/headphones-hint.tsx`：從 Card 包裝改為 `<Alert variant="default">`（用 info 樣式）+ 內含以 Lucide `X` icon 渲染的 close 按鈕（component-local state 控制 dismiss，不寫 localStorage）。在 `detail.tsx` 將 HeadphonesHint 放在 MeetingHeaderBar 與 tabs 之間（取代原本在 metadata card 上方的位置）。實作 spec ADDED: HeadphonesHint SHALL render as a dismissible inline alert when dual mode is selected before start。驗證：8.1 測試由紅轉綠 + detail.test.tsx 內既有 HeadphonesHint 條件渲染測試保持綠。

## 9. 移除舊元件 + hook — Decision 3: Workspace 永遠三欄、移除 LayoutSwitcher (剩餘清理)

Final cleanup that completes the REMOVED requirements: Detail page exposes a layout switcher persisted across sessions + Stack mode renders the three panes vertically in P-T-A order + Workspace layout SHALL switch between three-column and stacked via animate-ui + MetadataCard SHALL render as a two-column layout with embedded CaptureIndicator + Detail page metadata card exposes ASR provider selector, recording badge, and re-run action + Meeting detail page renders an Attachments section with the dropzone component + MetadataCard header renders a related-meetings section between prev/next nav and meta + Columns mode renders three panes in a 30/40/30 grid with full-width meta + Columns mode three panes share equal visible height (slice-7 round 2).

- [x] 9.1 刪除 `packages/web/src/components/metadata-card.tsx` 與 `metadata-card.test.tsx`；刪除 `packages/web/src/components/layout-switcher.tsx` 與 `layout-switcher.test.tsx`；刪除 `packages/web/src/hooks/use-detail-layout.ts` 與 `use-detail-layout.test.tsx`。全 repo grep 確認沒有其他地方 import 這三個 export，移除孤立 import。本任務完成 spec REMOVED: Detail page exposes a layout switcher persisted across sessions（LayoutSwitcher 元件刪除）+ Stack mode renders the three panes vertically in P-T-A order（stack 程式碼移除）+ Workspace layout SHALL switch between three-column and stacked via animate-ui（switching code 移除）+ MetadataCard SHALL render as a two-column layout with embedded CaptureIndicator（MetadataCard 檔案刪除）+ Detail page metadata card exposes ASR provider selector, recording badge, and re-run action（MetadataCard 內三個 slot 隨之消失）+ Meeting detail page renders an Attachments section with the dropzone component（不再有獨立 attachment section，已在 task 6.2 移除）+ MetadataCard header renders a related-meetings section between prev/next nav and meta（related-meetings 移入 ⋯ menu，已在 task 6.2 處理）+ Columns mode renders three panes in a 30/40/30 grid with full-width meta（30/40/30 → 1fr 1fr 1fr，已在 task 4.2 處理）+ Columns mode three panes share equal visible height (slice-7 round 2)（永遠成立，已在 task 5.1 處理）。觀察行為：應用啟動 + `bun --filter @meeting-playbook/web test` 全綠；TS 型別檢查（既有 pre-existing error 不算）無新增錯誤。驗證：`bun --filter @meeting-playbook/web test` 全綠、`grep -r 'MetadataCard\|LayoutSwitcher\|useDetailLayout' packages/web/src` 無 hit。

## 9.5 No-emoji rule for all UI affordances — Decision 8: 禁用 emoji，所有 UI affordance 用 Lucide icon

Covers ADDED: All UI affordances on the meeting detail page SHALL use Lucide icon components, not emoji glyphs.

- [x] 9.5.1 在 `meeting-header-bar.test.tsx` / `meeting-overflow-menu.test.tsx` / `headphones-hint.test.tsx` / `meeting-detail-summary-view.test.tsx` 各補一條 emoji-free 斷言：用 regex `/[\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}]/u` 掃 rendered DOM `textContent`，期望 match count 為 0。觀察行為：任何 emoji glyph 出現在 render 後都會被測試抓到。驗證：四個測試新 case 全綠。
- [x] 9.5.2 [P] 在 `packages/web/src/routes/meetings/detail.test.tsx` 補一條整合斷言：載入 detail page、開啟 overflow menu、開啟 attachments dialog、模擬 dual+idle 顯示 HeadphonesHint，在這四個狀態下 `document.body.textContent` 通過上述 emoji regex 掃描得到 0 match。實作 spec scenario: Rendered detail page contains no emoji code points。驗證：detail.test.tsx 該 case 綠燈。
- [x] 9.5.3 [P] 各元件實作確保 icon 來源：在新建 / 改寫的元件（`meeting-header-bar.tsx`、`meeting-overflow-menu.tsx`、`meeting-detail-summary-view.tsx`、`headphones-hint.tsx`、`workspace.tsx` MeetingColumn 工具 slot）只從 `lucide-react` import icon 元件；避免在文字節點塞 emoji glyph。實作 spec ADDED: All UI affordances on the meeting detail page SHALL use Lucide icon components, not emoji glyphs。驗證：9.5.1 + 9.5.2 全綠 + 對 4 個新元件檔執行 `grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]'` 無 hit。

## 11. Claude Design 回稿 5 細節 — Decision 9: 從 Claude Design 回稿吸收 5 個 UI 細節

Covers ADDED: Summary chat slot SHALL render three static example prompt chips beneath the placeholder + HeadphonesHint SHALL display the full localised copy explaining echo risk in dual ASR mode + Overflow menu items for ASR provider and Recording mode SHALL display the current value as a sub-hint + TranscriptPane SHALL render a flag chip and highlight row when a chunk's flag_reason field is non-empty + AdvisorPane SHALL render reply chips on AI bubbles whose suggestion_text is provided, wiring the apply chip to fill the advisor input.

- [x] 11.1 9a — Summary chat slot 示例 chips：在 `meeting-detail-summary-view.test.tsx` 加 case 斷言右欄渲染後恰有 3 個 chip 元素，文字節點分別為 `「Joyce 那項做完了嗎？」` / `「對方資安要求摘要」` / `「下次該準備什麼？」`、每個 chip `cursor: default` 且**無 onClick handler**（無 button role）。在 `meeting-detail-summary-view.tsx` 右欄渲染這 3 個 `<span>` chip 於「即將推出」placeholder 文字下方、用 `bg-(--color-surface-2) text-(--color-muted-foreground) rounded-full px-3 py-1 text-xs`、`flex gap-2 flex-wrap`。實作 spec ADDED: Summary chat slot SHALL render three static example prompt chips beneath the placeholder。觀察行為：純展示元素，不可點。驗證：測試 case 綠燈。
- [x] 11.2 [P] 9b — HeadphonesHint 完整版 copy：更新 `headphones-hint` 對應 i18n key（zh-TW: `建議戴耳機避免回音 — 雙路 ASR 模式下，喇叭外放會被麥克風重複擷取。`；en: `Recommended: wear headphones to avoid echo — in dual ASR mode, speaker output will be re-captured by the microphone.`）；改 `headphones-hint.tsx` 容器允許文字 wrap 兩行（移除 `truncate` / `whitespace-nowrap`，加 `leading-snug`）。在 `headphones-hint.test.tsx` 加 case 斷言 alert textContent 含完整 zh-TW 字串、不以 `...` 結尾。實作 spec ADDED: HeadphonesHint SHALL display the full localised copy explaining echo risk in dual ASR mode。觀察行為：1440px viewport 下完整呈現不截斷。驗證：測試 case 綠 + locale alignment 測試保持綠。
- [x] 11.3 9c — Overflow menu sub-hint：在 `meeting-overflow-menu.tsx` 為 asr / mode items 在 label 右側加一個 sub-hint span（條件式：only render 當 `asrCurrentLabel` / `modeCurrentLabel` prop 為非空字串時），樣式 `text-xs text-(--color-muted-foreground) ml-2`。元件 props 新增 `asrCurrentLabel?: string` 與 `modeCurrentLabel?: string`。在 `detail.tsx` 用既有 `asrProviders` 列表把 `meeting.asr_provider` 對應到 display name、用 i18n 把 `session.mode` 對應到 `雙路 ASR` / `單路（合流）` 後傳入。在 `meeting-overflow-menu.test.tsx` 加三條 case：(a) `asrCurrentLabel="Whisper Large v3"` 時 asr item DOM 含該字串；(b) `modeCurrentLabel="雙路 ASR"` 時 mode item DOM 含該字串；(c) 兩個 label 都 undefined 時，asr / mode item 不含任何 sub-hint span。實作 spec ADDED: Overflow menu items for ASR provider and Recording mode SHALL display the current value as a sub-hint。觀察行為：使用者開 menu 就看到當前設定值。驗證：三條 case 全綠。
- [x] 11.4 [P] 9d — Transcript flag chip 視覺殼：在 `transcript-pane.tsx` 的 chunk type 加 optional `flag_reason?: string`（純 frontend type）；渲染 chunk 時若 `flag_reason` 為非空字串則為該 row 套 `is-flag` class（CSS：4px destructive 色 left border + `bg-(--color-destructive)/4`）且在 meta line 加一個 destructive 色 chip 顯示 `flag_reason` 內容。在 `transcript-pane.test.tsx` 加兩條 case：(a) chunk 有 `flag_reason: "反對訊號"` → row 含 `is-flag` class + 含 `反對訊號` chip 元素；(b) chunks 都無 `flag_reason` → 無任何 `is-flag` class、無 destructive chip。實作 spec ADDED: TranscriptPane SHALL render a flag chip and highlight row when a chunk's flag_reason field is non-empty。觀察行為：UI 殼就緒，等後端 feature change 餵 `flag_reason` 才會自動出現。驗證：兩條 case 全綠。
- [x] 11.5 9e — Advisor reply chips：在 `advisor-pane.tsx` 的 message type 加 optional `suggestion_text?: string`（純 frontend type）+ 新 prop `onApplySuggestion: (text: string) => void`；渲染 AI bubble 時若 `suggestion_text` 為非空字串，bubble body 下方加 chip 列：「套用為回覆」（i18n key `meetings.advisor.applyReply`）click 後呼叫 `onApplySuggestion(suggestion_text)`；「換一個說法」（i18n key `meetings.advisor.rephrase`）`disabled` + `title` 為 i18n `meetings.advisor.rephraseTooltip`（zh-TW: 下版本上線 / en: Coming next release）。在 `detail.tsx` 將 `onApplySuggestion` wire 到 advisor input controller（setter 取現有 input controlled value 邏輯）。在 `advisor-pane.test.tsx` 加四條 case：(a) bubble 有 `suggestion_text` → 兩個 chip 元素存在；(b) click「套用為回覆」→ `onApplySuggestion` 被呼叫一次參數為 `suggestion_text`；(c) click「換一個說法」→ 任何 callback 都沒被呼叫；(d) bubble 無 `suggestion_text` → 無 chip 列。i18n 新增 3 個 key 在 zh-TW + en 同步補。實作 spec ADDED: AdvisorPane SHALL render reply chips on AI bubbles whose suggestion_text is provided, wiring the apply chip to fill the advisor input。觀察行為：UI 殼 + 套用 callback 就緒，「換一個說法」disabled，等後端 regenerate endpoint 另開 change 才啟用。驗證：四條 case + locale alignment 測試全綠。

## 12. 第二輪 Sean review 加 4 個 UI 項 — Decision 10: 第二輪 Sean review 加 4 個 UI 項

Covers ADDED: AsrLoadingDialog SHALL display as a non-dismissible modal while the meeting session is connecting + TranscriptPane SHALL animate new chunks sliding up from the bottom while keeping chronological order + MeetingHeaderBar SHALL render counterparty as an avatar group when the display name string contains multiple comma-separated names + Recording status indicator SHALL render four states derived from meeting bucket and recordings availability, never using grey.

- [x] 12.1 10a 紅燈測試 — 在新 `packages/web/src/components/asr-loading-dialog.test.tsx` 加四條 case：(a) `phase="connecting"` → `data-testid="asr-loading-dialog"` modal 可見、title 含 `正在啟動轉錄引擎…`、含 BarVisualizer 元素；(b) `phase="in_progress"` → modal 不在 DOM；(c) `phase="error"` → modal 不在 DOM；(d) 模擬 `Escape` keypress → modal 仍在。驗證：測試 fail（元件未實作）。
- [x] 12.2 10a 安裝套件 + 實作 dialog：執行 `bun add @elevenlabs/react`（驗證實際套件名稱，若為 `@elevenlabs/ui` 等其他名稱依實際 install），確認 BarVisualizer 元件可 import。實作 `packages/web/src/components/asr-loading-dialog.tsx`：shadcn `<Dialog>` modal、prop `phase: SessionPhase`、`open = phase === "connecting"`、`onOpenChange` 忽略 dismiss（只有 phase 變化才會關）；內含 `<BarVisualizer>`（autoplay 模式、24 bars、fill `--color-primary`、bg `--color-primary-soft`）+ i18n title (`meetings.session.asrLoading.title`) + subtitle (`meetings.session.asrLoading.subtitle`)；i18n 兩個 key 在 zh-TW + en 同步補（依 design 表格文案）。在 `detail.tsx` 加 `<AsrLoadingDialog phase={session.state.phase} />`，與 MeetingHeaderBar 平級。實作 spec ADDED: AsrLoadingDialog SHALL display as a non-dismissible modal while the meeting session is connecting。驗證：12.1 測試由紅轉綠 + locale alignment 測試保持綠。
- [x] 12.3 [P] 10b 紅燈測試 — 在 `transcript-pane.test.tsx` 加兩條 case：(a) 初始 mount with 3 個 historical chunks → 三個 chunk 元素皆在最終位置（無 transform/opacity transition initial value）；(b) 加入第 4 個 chunk 後，第 4 個的 motion.div 收到 initial `{ opacity: 0, y: 12 }` 並過渡到 final。驗證：測試 fail。
- [x] 12.4 10b 實作 transcript 動畫：在 `transcript-pane.tsx` 把 chunk render 的 outer container 改成 `<AnimatePresence initial={false}>`；每個 chunk row 改成 `motion.div`，`initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.2, ease: "easeOut" }}`；用 `useReducedMotion()` 在 reduced 環境下 override 為 `duration: 0, y: 0`。實作 spec ADDED: TranscriptPane SHALL animate new chunks sliding up from the bottom while keeping chronological order。觀察行為：新 chunk arrive 時從底部 slide-up + fade-in 200ms；既有 chunks 不重新動畫。驗證：12.3 測試由紅轉綠。
- [x] 12.5 [P] 10c 紅燈測試 — 在 `meeting-header-bar.test.tsx` 加四條 case：(a) `counterparty_display_name = "林經理"` → 單 `<Avatar>` + 文字「林經理」、無 `<AvatarGroup>`；(b) `"林經理, 王董"` → `<AvatarGroup>` 含 2 個 avatar、無 +N chip、label 含「林經理 等 2 人」；(c) `"林經理、王董, 陳副總，黃工程師、Joyce"` → 3 個 avatar + `+2` chip + label 含「林經理 等 5 人」；(d) `"A,B，C、D"` → 3 個 avatar + `+1` chip。驗證：測試 fail。
- [x] 12.6 10c 實作 avatar-group：把 animate-ui `avatar-group` 元件原始碼複製到 `packages/web/src/components/animate-ui/avatar-group.tsx`（仿既有 animate-ui icons 處理方式）+ 對應型別/exports；在 `meeting-header-bar.tsx` 用 helper `parseCounterpartyNames(s: string): string[]` 用 regex `/[,，、]\s*/` split + filter empty；當長度 1 沿用原本 `<Avatar>` + 名字；長度 >=2 渲染 `<AvatarGroup>`（前 3 個 avatar + 第 4 起變 `+N` chip、樣式 `bg-(--color-surface-2) text-(--color-muted-foreground) text-xs rounded-full size-6`）+ 名字 label 「第一個名字 等 N 人」（i18n key `meetings.detail.counterpartyGroupLabel`，zh-TW: `{name} 等 {count} 人`、en: `{name} and {others} others`）。實作 spec ADDED: MeetingHeaderBar SHALL render counterparty as an avatar group when the display name string contains multiple comma-separated names。驗證：12.5 測試由紅轉綠 + locale alignment 測試保持綠。
- [x] 12.7 [P] 10d 紅燈測試 — 在新 `packages/web/src/lib/meetings-recording-status.test.ts` 加五條 case：(a) `recordings_available=true` → `available`；(b) `bucket="needs_recording"` → `pending`；(c) `bucket="completed", recordings_available=false` → `expired`；(d) `bucket="upcoming"` → `hidden`；(e) 各 state 對應的 i18n key + 顏色 token 正確。在 `meeting-header-bar.test.tsx` 補三條 case：(a) `available` state → dot 元素 style 含 `--color-success`、含文字「錄音可用」；(b) `pending` state → dot 含 `--color-warning`、含文字「待上傳」；(c) `expired` → dot 含 `--color-accent`、含文字「錄音已過期」；(d) `hidden` → 無 recording 指示燈元素；(e) 對任何渲染出的 dot 元素 inline style 斷言不含 `--color-muted-foreground` 或 `--color-surface-3`。驗證：測試 fail。
- [x] 12.8 10d 實作 recording status helper + i18n：新建 `packages/web/src/lib/meetings-recording-status.ts` 匯出 `type RecordingStatus = 'available' | 'pending' | 'expired' | 'hidden'` + `resolveRecordingStatus(meeting): RecordingStatus`（依 spec 表格邏輯：available > pending > expired > hidden 的 priority 派生）。i18n 新增 key `meetings.detail.recordingPending`（zh-TW `待上傳` / en `Pending upload`），現有 `recordingAvailable` / `recordingExpired` 不動。改 `meeting-header-bar.tsx` 用 helper 派生 state 後切換 dot 顏色 token（success / warning / accent）+ label i18n key；`hidden` state 整個元素 unmount。實作 spec ADDED: Recording status indicator SHALL render four states derived from meeting bucket and recordings availability, never using grey。觀察行為：4 個 state 各自正確、grey token 永遠不出現。驗證：12.7 測試由紅轉綠 + locale alignment 測試保持綠。

## 13. PrevNextNav 加回會議列表 back link — Decision 11: 加回「會議列表」back link 到 PrevNextNav 列

Covers MODIFIED: Meeting detail page SHALL render a prev/next navigation group alongside the BackLink — back link reinstated as leftmost element on the same row, separated from prev/next group by a vertical divider.

- [x] 13.1 寫紅燈測試 — 在 `packages/web/src/components/meeting-prev-next-nav.test.tsx`（既有檔，沒有的話新增）加四條 case：(a) 載入 meeting `m_2`（中間 meeting）→ 同一 row 內含 back link 文字「會議列表」、含 vertical divider 元素、含 prev/next group（顯示「Q3 review」+「客戶X kickoff」）；(b) click back link → router 跳到 `/meetings`；(c) back link 的 svg 為 Lucide `ArrowLeft`，prev/next 的 svg 為 Lucide `ChevronLeft` / `ChevronRight`（用 svg path 比對）；(d) 三段元素在同一直系父元素（同一 row）且整 row 在 `MeetingHeaderBar` 之前。驗證：測試 fail。
- [x] 13.2 [P] i18n 新增 `meetings.detail.backToList`：zh-TW `會議列表` / en `All meetings`，在 `locales.test.ts` 同步把預期 keys 數量加 1。實作 spec MODIFIED: `meetings.detail.backToList` 出現在兩 locale。驗證：locale alignment 測試保持綠。
- [x] 13.3 實作 `meeting-prev-next-nav.tsx` 改造：原本只渲染 prev/next 改為 flex row 含三段 — `<Link to="/meetings">[ArrowLeft] {t("meetings.detail.backToList")}</Link>`、`<span class="w-px h-3 bg-(--color-border)" />` divider、原有 prev/next group（icons 改為 `ChevronLeft` / `ChevronRight`）。實作 spec MODIFIED: Meeting detail page SHALL render a prev/next navigation group alongside the BackLink。驗證：13.1 測試由紅轉綠。

## 14. ASR 引擎從 menu 移除 — Decision 12: ASR Provider 移到 /settings、menu 只剩 8 項（修正 propose 漏改）

Covers MODIFIED: MeetingOverflowMenu SHALL consolidate secondary affordances behind a single ⋯ trigger — ASR 引擎 item removed; item count drops from 9 to 8. ASR Provider switching SHALL only be possible via `/settings/integrations`.

- [x] 14.1 寫紅燈測試 — 修改 `meeting-overflow-menu.test.tsx`：把原本 9-item assertion 改為 8-item；斷言 menu 開啟後**不含** `data-testid="meeting-menu-item-asr"`；移除原 9c sub-hint 測試中與 asr 相關的兩條，只保留 mode 那條；新增「detail page menu 不應出現任何 ASR 切換 affordance」斷言。驗證：測試先 fail。
- [x] 14.2 [P] 實作從 `meeting-overflow-menu.tsx` 移除 asr item + 相關 props (`asrCurrentLabel`, `onAsrOpen`)；保留 `modeCurrentLabel` + `onModeOpen`。同步移除 `detail.tsx` 對 ASR popover 的 callback wiring + state。實作 spec MODIFIED: menu 9 → 8 項。驗證：14.1 測試由紅轉綠。

## 15. Playbook 欄位用 animate-ui Radix Accordion — Decision 13: 劇本欄位用 animate-ui Radix Accordion 取代手刻

Covers ADDED: Playbook column SHALL render its six fields using animate-ui Radix Accordion with height animation, replacing manual expand/collapse state.

- [x] 15.1 寫紅燈測試 — 在 `playbook-pane.test.tsx`（沒有的話新增）加三條 case：(a) 初次 mount → 6 個 accordion item（目標 / 對方輪廓 / 預期主題 / 預期反對 / 談話要點 / 紅線）；(b) click「目標」trigger → 對應 panel 出現，trigger 的 `aria-expanded="true"`；(c) `type="multiple"` 模式下，兩個 panel 可同時展開。驗證：測試 fail（目前是手刻 PlaybookField）。
- [x] 15.2 將 animate-ui Radix Accordion 元件原始碼複製到 `packages/web/src/components/animate-ui/radix-accordion.tsx`（仿既有 animate-ui copy-paste 模式）；若 `@radix-ui/react-accordion` 未安裝執行 `bun add @radix-ui/react-accordion`。驗證：import 不報錯。
- [x] 15.3 改寫 `playbook-pane.tsx`：移除手刻 `PlaybookField` 的 expand state；6 個欄位改用 `<Accordion type="multiple" defaultValue={["objective","talkingPoints"]}>` 包覆 + 6 個 `<AccordionItem>` + 每個 Item 含 `<AccordionTrigger>`（title + 內建 chevron）+ `<AccordionContent>`（欄位 markdown）。實作 spec ADDED: Playbook column SHALL render its six fields using animate-ui Radix Accordion with height animation, replacing manual expand/collapse state。驗證：15.1 測試由紅轉綠。

## 16. 上傳 / 匯出完成的 SuccessResult hazeover — Decision 14: 上傳 / 匯出完成顯示 SuccessResult overlay（hazeover）

Covers ADDED: SuccessResultOverlay SHALL render after upload audio and export bundle async operations complete, with backdrop blur (hazeover style) and auto-dismiss.

- [x] 16.1 寫紅燈測試 — 在新 `packages/web/src/components/success-result-overlay.test.tsx` 加四條 case：(a) `open={true}` + `title="上傳完成"` → overlay 含 backdrop element（`backdrop-blur-sm`）+ 含勾勾 icon SVG（Lucide `CircleCheck` 或 success-result 內建）+ 含 title 文字；(b) `open={false}` → 不在 DOM；(c) `autoDismissMs={3000}` 經過 3 秒後 onClose callback 被呼叫一次；(d) click 「完成」button → 立即 onClose。驗證：測試 fail（元件未實作）。
- [x] 16.2 [P] 實作 `packages/web/src/components/success-result-overlay.tsx`：把 DevSloka `success-result` 元件原始碼複製進來改名（純 UI，內含勾勾動畫 + title + subtitle + 可選 button）；外層包 fixed-position backdrop `bg-black/30 backdrop-blur-sm` 達到 hazeover 效果；props `{ open: boolean, title: string, subtitle?: string, autoDismissMs?: number, onClose: () => void, primaryAction?: { label: string, onClick: () => void } }`；mount 後若 `autoDismissMs > 0` 用 `setTimeout` 呼叫 `onClose`；按 ESC / 點 backdrop 也呼叫 `onClose`。實作 spec ADDED: SuccessResultOverlay SHALL render after upload audio and export bundle async operations complete, with backdrop blur and auto-dismiss。驗證：16.1 測試由紅轉綠。
- [x] 16.3 wire 到 UploadDialog（上傳音訊完成）：在 `packages/web/src/components/offline-ingest/UploadDialog.tsx` 內，當 upload + processing 流程完成（既有 `state === "done"` 之類的狀態），UploadDialog 自身關閉後在 detail page 渲染 `<SuccessResultOverlay open title="音檔處理完成" subtitle="逐字稿已自動生成" autoDismissMs={3000} primaryAction={{ label: "查看逐字稿", onClick: focusTranscriptColumn }} onClose={...} />`。State 由 detail.tsx 控管 (`uploadSuccessOpen: boolean`)；UploadDialog onSuccess callback 觸發 setUploadSuccessOpen(true)。在 `detail.test.tsx` 加 case 模擬 upload 完成 → overlay 出現。
- [x] 16.4 [P] wire 到 ExportMeetingButton（匯出 ZIP 完成）：在 `packages/web/src/components/export-meeting-button.tsx` 內，當 ZIP fetch + download 完成（既有 success state），呼叫新增的 `onExportSuccess` callback。在 detail.tsx 渲染 `<SuccessResultOverlay open title="匯出完成" subtitle="ZIP 已儲存到下載資料夾" autoDismissMs={3000} onClose={...} />`。在 `detail.test.tsx` 加 case 模擬 export 完成 → overlay 出現。
- [x] 16.5 i18n 新增 4 個 key 在 zh-TW + en 同步補：`meetings.detail.uploadSuccessTitle`（音檔處理完成 / Audio processed）/ `meetings.detail.uploadSuccessSubtitle`（逐字稿已自動生成 / Transcript generated）/ `meetings.detail.exportSuccessTitle`（匯出完成 / Export complete）/ `meetings.detail.exportSuccessSubtitle`（ZIP 已儲存到下載資料夾 / ZIP saved to Downloads）+ `meetings.detail.viewTranscript`（查看逐字稿 / View transcript）。更新 `locales.test.ts` 預期 keys 數量。驗證：locale alignment 測試保持綠。

## 10. 驗證 + 手動瀏覽器確認 — Decision 7: 不做手機端 驗證範圍

- [x] 10.1 執行 `bun --filter @meeting-playbook/web test` 全綠（含 i18n locale 對齊、所有元件 + route 測試）。驗證：summary 顯示 0 fail。
- [x] 10.2 啟動 web dev 伺服器（依 root README 的 dev 指令）以 1440px 視窗手動驗證下列 user flow（Decision 7：1024px 以下不在驗證範圍）：開一場 scheduled meeting detail 頁，確認（a）三欄並排等寬、各欄獨立 scroll、欄頭 sticky；（b）header bar 一行精簡，無 MetadataCard 痕跡，所有 affordance 都是 Lucide icon（無任何 emoji glyph）；（c）overflow menu trigger（Lucide `MoreHorizontal`）打開後 9 項可見、每項左側 icon 為對應的 Lucide 元件（Pencil / Tag / Paperclip / Link2 / Settings2 / Mic2 / RotateCw / Trash2）、asr / mode item 右側顯示當前值 sub-hint（9c）、點各項對應 dialog / popover 開啟正常；（d）切到 completed meeting 的 summary tab → 雙欄、右欄 placeholder 含 Lucide `MessageSquare` icon + 下方 3 顆灰階示例 chips（9a）；（e）dual + idle 時 HeadphonesHint inline alert 顯示完整版 copy（含「喇叭外放會被麥克風重複擷取」字句，9b）、可透過 Lucide `X` icon button 關閉；（f）逐字稿欄手動塞一條 `flag_reason: "反對訊號"` mock chunk → 該 row 套 destructive 色 highlight + chip（9d）；（g）戰術建議欄手動塞一條 `suggestion_text` mock AI bubble → 下方含「套用為回覆」（可點，按下 input 填入）+「換一個說法」（disabled + tooltip）chips（9e）；（h）按下開始錄音 → `AsrLoadingDialog` 立刻出現含 BarVisualizer + 「正在啟動轉錄引擎…」標題；Escape / 點外面不能關；模型載完自動消失（10a）；（i）開始錄音後新 chunk 從底部 slide-up 進來、舊 chunks 不重新動畫（10b）；（j）meeting create form 試輸入「林經理, 王董, 陳副總, 黃工程師, Joyce」對方欄位 → header bar 顯示 3 個 avatar + `+2` chip + 「林經理 等 5 人」label（10c）；（k）開不同 bucket 的 meeting 看 recording 指示燈：upcoming → 無、scheduled+recorded → 綠「錄音可用」、needs_recording → 橘「待上傳」、completed 無 recording → 紫 mauve「錄音已過期」、沒有任何灰色（10d）；（l）切回 workspace tab、scroll 各欄、結束會議 → mini-player 行為不變；（m）PrevNextNav row 最左側「會議列表」back link 一鍵回 `/meetings`；icon 用粗的 `ArrowLeft`、prev/next 用細的 `ChevronLeft / ChevronRight`（13）；（n）劇本欄 6 個欄位用 Radix Accordion，可多選同時展開、smooth height 動畫、aria-expanded 正確（15）；（o）overflow menu 只剩 8 項、ASR 引擎已從 menu 移除、要改 ASR 須走 `/settings/integrations`（14）；（p）需要上傳音訊的 meeting，UploadDialog 跑完 progress → 出現 hazeover backdrop + 勾勾「音檔處理完成」success overlay、3 秒自動消失（16）；（q）匯出 ZIP 下載完成 → 出現 hazeover backdrop + 勾勾「匯出完成」success overlay、3 秒自動消失（16）。觀察行為：頁面整體呼吸感比現在好、主視角佔據視覺核心、async 完成感更明確。驗證：人工確認通過、發給 Sean review。
- [x] 10.3 [P] spec sync 驗證：`spectra validate refactor-meeting-detail-three-column` 通過；archive 階段會把 delta（9 REMOVED + 3 MODIFIED + 20 ADDED）合入 live `meeting-detail-layout` spec。驗證：validate 0 error。
