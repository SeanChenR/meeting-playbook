## 1. Schema 與 config 落地

- [x] 1.1 [P] 新增 Alembic migration `packages/backend/alembic/versions/XXXX_meeting_attachment.py` 建立 `meeting_attachment` 表（columns 含 `kind` CHECK constraint、`bytes > 0` CHECK、FK `meeting_id` CASCADE、index on `(meeting_id, deleted_at)`），落實「Schema：`meeting_attachment` 獨立表 + `kind` enum」設計決策與 `meeting_attachment table stores per-meeting attachment metadata` requirement。驗證：新增 `packages/backend/tests/test_alembic_meeting_attachment.py` 跑 upgrade → downgrade → upgrade 三輪確認 schema 對稱、CHECK constraint 拒絕非法值（`uv run pytest packages/backend/tests/test_alembic_meeting_attachment.py` 全綠）。
- [x] 1.2 [P] 在 `packages/backend/meeting_playbook/config.py` 加 `attachment_dir: str = "~/MeetingPlaybook/attachments"`、於 `.env.example` 加對應 commented-out 註解區段並引用 ADR-0020 與 30 天 retention 語意，落實「Storage：本機檔案系統 `~/MeetingPlaybook/attachments/{meeting_id}/{attachment_id}.<ext>`」設計決策與 `ATTACHMENT_DIR configuration with documented default` requirement；上傳時的磁碟路徑組裝（含 `{meeting_id}/` 子資料夾 + `{attachment_id}.<canonical_extension>` 檔名）由本 task 落地的 config 提供基礎。驗證：`uv run pytest packages/backend/tests/test_config.py` 含「default 值 = `~/MeetingPlaybook/attachments`」與「env 覆寫」兩條 case；`grep ATTACHMENT_DIR .env.example` 命中。

## 2. Validation 與 kind 推斷邏輯

- [x] 2.1 [P] 在 `packages/backend/meeting_playbook/attachments/validation.py` 實作純函式 `validate_upload(content_type, filename, declared_bytes, existing_attachments) -> tuple[kind, None]` 或 raise `AttachmentValidationError(error_code)`，落實「Validation：whitelist mime + per-meeting quota（5 檔 / 30MB）」與「`kind` 推斷邏輯」設計決策，對應 `Upload validation enforces whitelist, per-meeting file count, and per-meeting byte quota` requirement。Red tests（在 `tests/attachments/test_validation.py`）：whitelist 對應表 11 條 case（含 `application/octet-stream` + `.md` 走 extension fallback、`image/bmp` reject、legacy `.doc` reject）；第 6 檔 reject `attachment.too_many`；累計超 30MiB reject `attachment.quota_exceeded`；皆為 `uv run pytest` 全綠。
- [x] 2.2 [P] 在 `packages/backend/meeting_playbook/attachments/models.py` 定義 `MeetingAttachment` SQLAlchemy model 對應 1.1 schema、並在 `packages/backend/meeting_playbook/attachments/repository.py` 實作 `list_for_meeting / create / soft_delete / get_for_download`（每個方法都驗 user 擁有該 meeting），落實 `GET list endpoint returns active attachments for the meeting owner` requirement 對應的 ownership 檢查與 `DELETE endpoint soft-deletes the row and unlinks the file` requirement 對應的軟刪行為。Red tests（`tests/attachments/test_repository.py`）：list 只回 active；非 owner 查不到（empty list / None）；soft_delete 後 list 不含該 row；皆 `uv run pytest` 全綠。

## 3. 4 個 HTTP endpoints（meeting-attachment 核心 API）

- [x] 3.1 在 `packages/backend/meeting_playbook/attachments/router.py` 實作 `GET /api/meetings/{meeting_id}/attachments` 與 `POST /api/meetings/{meeting_id}/attachments`，落實「API contract：4 個 endpoints under `/api/meetings/{id}/attachments`」設計決策與 `GET list endpoint returns active attachments for the meeting owner` + `POST upload endpoint persists file and row on success` + `Upload validation enforces whitelist, per-meeting file count, and per-meeting byte quota` requirements。POST 路徑驗證失敗時 SHALL 在回應前 unlink 已暫存檔案。Red tests 必須覆蓋設計裡的「Per-file-type 行為表（給 AttachmentProcessor TDD 用 — 本 slice 只驗 storage，本表為 S20c 預備）」全部 5 個 kind（image / pdf / docx / text / markdown）每行的 happy path 與該 kind 對應的 reject 條件；測試檔 `tests/attachments/test_endpoints.py` 覆蓋：image (PNG 200KB) happy + BMP reject、PDF 1MB happy + 改副檔名 zip 仍 accept（S20a 不 sniff magic bytes）、docx 500KB happy + 35MB reject quota_exceeded、txt 5KB happy、md 12KB happy；list owner 200 / non-owner 404；too_many 第 6 檔 422；皆 `uv run pytest` 全綠。
- [x] 3.2 在 `packages/backend/meeting_playbook/attachments/router.py` 實作 `DELETE /api/meetings/{meeting_id}/attachments/{attachment_id}` 與 `GET /api/meetings/{meeting_id}/attachments/{attachment_id}/download`，落實 `DELETE endpoint soft-deletes the row and unlinks the file` + `Download endpoint streams the file with original filename` requirements。DELETE 設 `deleted_at = now()` 並 `unlink(missing_ok=True)`；download 在磁碟檔不存在時回 HTTP 410 `attachment.expired`。Red tests（同檔加 case）：DELETE 204 → list 不含、二次 DELETE 404、download 200 + `Content-Disposition: attachment; filename="<original>"`、download 行為對應 410 expired；皆 `uv run pytest` 全綠。
- [x] 3.3 在 `packages/backend/meeting_playbook/server.py` 註冊 `attachments_router` 並接上 `X-User-Id` dependency，使該 4 endpoints 走既有 gateway 路由不繞過 auth。驗證：`uv run pytest packages/backend/tests/test_server_routes.py`（或既有 server 註冊測試）含「`GET /api/meetings/{id}/attachments` 不帶 X-User-Id 回 401」一條。

## 4. Retention 整合（recording-retention 修改）

- [x] 4.1 修改 `packages/backend/meeting_playbook/retention/job.py` 將 `cleanup` 簽名加 `attachments_dir: Path` 並擴成「先掃 recording 再掃 meeting_attachment 兩段查詢、單一 transaction commit」，落實「Retention：擴 `RecordingRetentionJob.cleanup` 多掃一張表」設計決策與 `RecordingRetentionJob.cleanup deletes WAV files older than threshold` (MODIFIED) requirement。同步擴 `runtime.py` 的 `run_forever` 傳入 `attachments_dir`，落實 `Retention job runs every 24 hours via FastAPI lifespan` (MODIFIED)。Red tests（`tests/retention/test_job.py` 擴 + 新檔）：attachment-only 場景刪超齡 + 留新；recording + attachment 混合掃；單一 row unlink 失敗其他 row 仍 commit；idempotent 第二次回 0；transcript / chat_message 不被動；皆 `uv run pytest` 全綠。

## 5. 前端 dropzone 元件 + API helper

- [x] 5.1 [P] 在 `packages/web/src/lib/attachments-api.ts` 實作 `listAttachments(meetingId)` / `uploadAttachment(meetingId, file, onProgress)` / `deleteAttachment(meetingId, attachmentId)` / `getDownloadUrl(meetingId, attachmentId)` 4 個 helper；upload 使用 `XMLHttpRequest.upload.onprogress` 回傳 0-100 給 callback，落實「Frontend：`<AttachmentDropzone>` 元件 + 上傳進度（XMLHttpRequest）」設計決策。驗證：`packages/web/src/lib/attachments-api.test.ts` 新增 case 覆蓋 happy path、422 error response 拋出 `{error_code, message}` 結構、`onProgress` callback 至少被呼叫一次；`bun --filter @meeting-playbook/web test attachments-api` 全綠。
- [x] 5.2 在 `packages/web/src/components/attachment-dropzone.tsx` 實作元件（drag-drop 區、accept whitelist、進度卡、檔案列表 row、download / delete 按鈕、錯誤訊息透過 `localizedErrorMessage`），落實 `AttachmentDropzone component renders list, dropzone, and progress` requirement。Red tests（`attachment-dropzone.test.tsx`）：drag 一個 PDF → 進度卡 0%→100% → 列表新增 row；422 `attachment.quota_exceeded` → 渲染本地化錯誤訊息且現有列表不變；delete 按鈕點擊後 row 立即消失；皆 `bun --filter @meeting-playbook/web test attachment-dropzone` 全綠。
- [x] 5.3 [P] 在 `packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json` 同步新增 `attachments.*` 命名空間（heading、hintLimit、uploadButton、emptyState、deleteConfirm 等）與 `errors.attachment.unsupportedFormat / tooMany / quotaExceeded / expired` 四條 i18n 訊息，並在 `packages/web/src/lib/i18n-errors.ts` 對應到 backend error_code；驗證 `bun --filter @meeting-playbook/web test src/locales/locales.test.ts` 之 deep-equal 仍綠。

## 6. Meeting detail 整合（meeting-detail-layout 修改）

- [x] 6.1 在 `packages/web/src/routes/meetings/detail.tsx` 新增 collapsible「附件 / Attachments」section 掛 `<AttachmentDropzone>`，落實 `Meeting detail page renders an Attachments section with the dropzone component` requirement。Section 展開狀態用 `localStorage["meeting-detail.attachments-expanded"]` 持久化；layout switch（stack ↔ columns）時保持 dropzone mounted 不重 init。Red tests（在 `detail.test.tsx` 或新檔 `detail-attachments.test.tsx`）：首次進入 section 預設展開；收合後 reload 仍收合；上傳進行中切 layout 不打斷 progress；皆 `bun --filter @meeting-playbook/web test` 全綠。

## 7. 領域語彙 / README / 收尾

- [x] 7.1 [P] 在 `CONTEXT.md` glossary 加「Meeting attachment (會議附件)」一條，描述：使用者於 meeting 上傳的圖檔 / PDF / docx / txt / md，沿用 30 天 retention 與 recording 一同清除；同步在 `README.md` 補一段「Attachment storage」介紹預設目錄與 quota（5 檔 / 30MB / meeting）。驗證：`grep -F 'Meeting attachment' CONTEXT.md` 命中、`grep -F 'ATTACHMENT_DIR' README.md` 命中。
- [x] 7.2 跑覆蓋率 `cd packages/backend && uv run pytest --cov=meeting_playbook.attachments --cov=meeting_playbook.retention --cov-report=term-missing` 確認 `attachments/*` ≥ 80%、`retention/job.py` 本 slice 修改範圍 ≥ 80%；同步跑 `bun --filter @meeting-playbook/web test attachment-dropzone --coverage` 確認元件覆蓋率 ≥ 80%；最後在 GitHub Issue #21 的 Acceptance criteria 7 條全部 check 起來，落實「最終驗證所有 AC 都完成」工作流規則。
