"""Recording-index domain (P4 IA refactor).

Read-surface for every Recording inside the active Recording window
(per `recording-retention`). Owns `GET /api/recordings` and
`POST /api/recordings/batch-download`.
"""
