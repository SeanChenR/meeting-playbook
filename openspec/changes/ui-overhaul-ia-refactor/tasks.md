## 1. Backend recording-index endpoints

- [x] 1.1 Add configuration value `RECORDING_BATCH_DOWNLOAD_MAX_BYTES` with default 2147483648 to backend settings, exposed via the existing settings module. Behavior: backend boot reads the env var, defaults to 2 GiB, and exposes the limit to the batch-download handler (per "RECORDING_BATCH_DOWNLOAD_MAX_BYTES env var configures the batch download cap"). Verify: pytest `test_recording_batch_size_cap_default` asserts default; `test_recording_batch_size_cap_override` asserts env override.
- [x] 1.2 Implement `GET /api/recordings` listing the current user's recordings within the Recording window with `since` / `until` / `search` / `page` / `page_size` query params, materializing the design choices "Decision: Recording-window membership computed at query time, not stored", "Decision: Free-text search uses ILIKE on `meeting.title`, not full-text", and "Decision: Pagination is page-based, page size 25". Behavior: returns the `{recordings,total,page,page_size}` envelope ordered by `captured_at DESC`, filtered to `deleted_at IS NULL`, retention-window-bound, `stream IN ('me','counterparty')`, and meeting ownership scoped to the session user (per "GET /api/recordings lists the current user's recordings within the Recording window" and "GET /api/recordings MUST exclude soft-deleted and retention-expired rows"). Verify: pytest covers ordering, retention-exclusion, soft-delete exclusion, foreign-user isolation, `them` filtering (the "Decision: Stream channel validation at API boundary rejects `'them'`" path), pagination metadata, ILIKE search match.
- [x] 1.3 Implement `POST /api/recordings/batch-download` with pre-flight ownership, retention, stream-channel, and size-cap validation, materializing the design choice "Decision: Batch download is a new POST endpoint, not GET, with size cap and streaming zip". Behavior: returns `StreamingResponse` of a `zipfile.ZIP_STORED` archive with `application/zip` and `Content-Disposition: attachment; filename="recordings-{YYYYMMDD-HHmmss}.zip"`; failure modes are 403 `recording.forbidden`, 410 `recording.retention_expired`, 422 `recording.invalid_stream`, 413 `recording.batch_oversize` (per "POST /api/recordings/batch-download streams a ZIP of selected recordings with a configurable size cap" and "POST /api/recordings/batch-download MUST reject retention-expired and soft-deleted rows with HTTP 410"). Verify: pytest covers each failure code, the happy-path zip integrity (entries, filenames, STORED mode), and a streaming-memory smoke check confirming the response is generator-driven (Behavior — backend acceptance criteria).
- [x] 1.4 Confirm `packages/auth` gateway already proxies `/api/recordings/**` to the backend. Behavior: requests to `/api/recordings` and `/api/recordings/batch-download` reach the FastAPI handler unchanged (per Decision "Auth gateway proxy" open question). Verify: integration test or curl walkthrough from the auth gateway origin returning 200 / expected error for an authenticated session.

## 2. Frontend /recordings page

- [x] 2.1 Add `src/lib/recordings-api.ts` with TanStack Query hooks for `GET /api/recordings` and a mutation for `POST /api/recordings/batch-download`. Behavior: hooks return typed `RecordingSummary` rows, surface `error_code` from non-2xx responses, and the batch-download mutation triggers a browser download of the streamed zip. Verify: vitest unit tests stubbing `fetch` cover happy path, 410, 413, 422, and 403 mapping to localized error keys via `localizedErrorMessage`.
- [x] 2.2 Register the `/recordings` route in `route-tree.tsx` inside `ProtectedShell` and create `src/routes/recordings/index.tsx` rendering the list page with TanStack Router URL query state for `since` / `until` / `search` / `page`, materializing the design choice "Decision: URL query state is the source of truth for filters" (per "/recordings route renders the recording index inside the Recording window" and the design's "Behavior — frontend" expectations). Behavior: navigating to `/recordings` while authenticated renders the table; URL query params bound to filter UI; back-button restores state. Verify: vitest route test asserts render, filter binding, and reload behavior.
- [x] 2.3 Implement table rows with single-row Download action wired to the existing `GET /api/meetings/:id/recordings/:rid/audio` endpoint, realizing the "Decision: Single-recording download reuses existing endpoint" path so no new flat audio route is introduced. Behavior: Download click navigates the browser to that URL; existing 410 retention-expired handling applies. Verify: vitest asserts the anchor `href` for a sample row matches the nested audio endpoint URL.
- [x] 2.4 Implement multi-select checkboxes and Batch Download action calling `POST /api/recordings/batch-download` via the mutation from task 2.1. Behavior: selecting N rows and clicking Batch Download issues the POST with `recording_ids`, saves the response as `recordings-{YYYYMMDD-HHmm}.zip`, and surfaces toasts for oversize / retention-expired failures using `errors.recording.batchOversize` and `errors.recording.retentionExpired` keys. Verify: vitest covers select-all, partial select, oversize toast, retention-expired toast, and success-flow file save.
- [x] 2.5 Implement search input (debounced 300ms) and date range pickers bound to URL state. Behavior: typing into search updates `?search=` after the debounce window and re-fires the query; date pickers update `?since=` / `?until=`. Verify: vitest asserts debounce, query refetch on filter change, and URL state propagation.
- [x] 2.6 Implement the localized empty state for zero in-window recordings using `recordings.list.empty` plus the retention hint (per "/recordings route renders the recording index inside the Recording window" empty-state scenario). Behavior: when the query returns zero rows under no filters, the page shows the empty card with the 30-day retention hint copy. Verify: vitest renders the page with an empty mock response and asserts the i18n keys are present in both locales.
- [x] 2.7 Implement the per-row "Open meeting" link navigating to `/meetings/{meeting_id}` (per "/recordings route renders the recording index inside the Recording window" link-to-source-meeting scenario). Behavior: clicking the meeting title cell pushes the meeting detail route. Verify: vitest asserts the rendered Link's `to` prop matches `/meetings/{id}` for sample rows.

## 3. Navigation + IA cleanup

- [x] 3.1 Add the Recordings nav entry to `protected-shell.tsx` between Meetings and Dashboard, label per `nav.recordings` locale key, materializing the "Decision: New nav entry placement in `protected-shell.tsx`" (per "Recordings nav entry SHALL appear in the protected shell"). Behavior: every protected page renders the nav entry in the documented order and clicking it routes to `/recordings`. Verify: vitest asserts the nav order and `to` target across both locales.
- [x] 3.2 Remove the `calendarImportRoute` registration in `route-tree.tsx` and delete `src/routes/calendar/upcoming.tsx` + `src/routes/calendar/upcoming.test.tsx`, executing the "Decision: Hard remove `/calendar/import`, no redirect" (per "/settings/integrations SHALL be the sole canonical entry point for calendar integration"). Behavior: navigating to `/calendar/import` produces the application 404 surface; `CalendarIntegrationPanel` still renders under `/settings/integrations` with no behavior change. Verify: vitest asserts no route matches `/calendar/import` or `/calendar/upcoming`; route-tree snapshot test updated; `/settings/integrations` smoke test still passes.
- [x] 3.3 Redirect the "Import from calendar" affordance on `routes/meetings/list.tsx` (and any kanban / calendar / list-view siblings) to `/settings/integrations`, satisfying the requirement that the "Import from calendar" affordance on /meetings SHALL navigate to /settings/integrations. Behavior: clicking the button on any meetings view lands the user on the settings integrations page. Verify: vitest asserts the Link `to` prop equals `/settings/integrations`; test name `meetings_list_import_calendar_button_targets_settings_integrations`.

## 4. i18n + cross-cutting

- [x] 4.1 Add all new keys under `recordings.*`, `nav.recordings`, `errors.recording.batchOversize`, and `errors.recording.retentionExpired` to BOTH `packages/web/src/locales/zh-TW.json` and `packages/web/src/locales/en.json` in the same change, populating every entry enumerated under design.md's "Interface — locale keys (added to BOTH `zh-TW.json` and `en.json`)" block (per "All user-visible recording-index strings SHALL exist in both locale files"). Behavior: every key listed in design.md Implementation Contract exists in both files with the documented values. Verify: `bun --filter @meeting-playbook/web test packages/web/src/locales/locales.test.ts` passes; manual review of zh-TW phrasing for tone.
- [x] 4.2 Update `routes-i18n.test.tsx` to drop assertions tied to the removed `/calendar/import` route and add assertions for the new `/recordings` route across both locales. Behavior: the route-locale parity test continues to gate; the deleted route no longer appears, the new route appears. Verify: `bun --filter @meeting-playbook/web test routes-i18n.test.tsx` passes.
- [x] 4.3 Cross-check the change against the design.md "Existing capabilities cross-referenced" list (recording-retention, meetings-calendar-view, settings-shell, calendar-integration) and confirm each referenced capability either receives a delta spec here or is documented as unchanged, materializing the "Decision: New capability `recording-index` rather than extending `recording-retention`" boundary. Behavior: the propose package ships exactly one new capability (`recording-index`) and three modified capabilities (`recording-retention`, `meetings-calendar-view`, `settings-shell`); `calendar-integration` stays untouched. Verify: `ls openspec/changes/ui-overhaul-ia-refactor/specs/` enumerates exactly those four directories; `spectra validate` passes.

## 5. Verification + analyzer pass

- [x] 5.1 Run `bun run lint`, `bun run format`, `bun run test`, and `cd packages/backend && uv run pytest` from repo root, hitting every item in design.md's "Acceptance criteria" block. Behavior: all CI gates pass on a clean working tree and the manual smoke (visit `/calendar/import` 404, visit `/recordings`, click "Import from calendar" on `/meetings` and land on `/settings/integrations`) succeeds. Verify: capture command output showing zero failures and a manual checklist of the three navigation assertions.
- [x] 5.2 Run `spectra validate ui-overhaul-ia-refactor` and `spectra analyze ui-overhaul-ia-refactor --json`, then audit that no work in this change has bled into the "Scope boundaries" out-of-scope list (P1 tokens, P2 primitives, P3 effect layers, P5 meeting detail, A2 dashboard, A3 settings sub-nav). Behavior: validate exits 0; analyzer reports no Critical findings and at most informational hints; the diff does not touch token files, primitive components, effect-layer components, meeting detail layout, dashboard, or settings sub-nav structure. Verify: command exit status 0, JSON output captured, and a manual diff review against the four out-of-scope areas.
