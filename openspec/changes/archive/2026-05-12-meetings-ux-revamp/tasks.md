## 1. Secondary teal token

- [x] 1.1 [Decision 1: Secondary colour as a separate token, not a primary alias] [--color-secondary SHALL be a distinct teal hue, not a surface-2 alias] 改 `packages/web/src/index.css`：在 `[data-theme="light"]` block 加 `--color-secondary: oklch(0.62 0.12 195);` + `--color-secondary-foreground: oklch(1 0 0);`；在 `[data-theme="dark"]` block 加 `--color-secondary: oklch(0.7 0.13 195);` + `--color-secondary-foreground: oklch(0.14 0.012 var(--primary-hue-dark));`。**移除**舊的 `--color-secondary: var(--color-surface-2);` binding（兩 theme 都要刪）。**驗證**：`bun run build` 成功；mount 一個 `<Button variant="secondary">` 在 happy-dom test 中，斷言 `getComputedStyle(btn).backgroundColor` 包含 `oklch(0.62 0.12 195)`（light）。

- [x] 1.2 [P] [--color-secondary SHALL be a distinct teal hue, not a surface-2 alias] [Failure modes] 巡邏 `packages/web/src` 內所有 `bg-(--color-secondary)` / `text-(--color-secondary-foreground)` / `bg-secondary` 用法，確認語意是「次要動作 / brand secondary」（保留）還是「中性表面」（改成 `bg-(--color-muted)` 或 `bg-(--color-surface-2)`）。具體檢查目標：`Button variant="secondary"` (應保留)、所有舊 grep hits。**驗證**：`grep -rn "bg-(--color-secondary)\|--color-secondary" packages/web/src` 列出每個位置 + 一句註解（保留 / 已遷移）；現有 component / route test suite 全綠。

## 2. Date-bucket helper + Kanban

- [x] 2.1 [P] [Decision 3: Date-bucket helper lives in lib, not the Kanban component] 新增 `packages/web/src/lib/meetings-bucket.ts`：export `MeetingDateBucket` type 跟 `getMeetingDateBucket(meeting, now)` pure function。實作要照 design Decision 3 三個 bucket 的精確邊界（in_progress→upcoming；scheduled in [today, today+7d)→upcoming；scheduled >=today+7d 或無日期→future；completed 或 scheduled <today→past）。`startOfToday` 用 user local TZ 算。Invalid date string fallback 到 "past" + dev-only `console.warn`。**驗證**：新增 `packages/web/src/lib/meetings-bucket.test.ts`，跑 spec scenario 「Three-bucket distribution」的 5 顆 meeting + scenario「Overdue scheduled meeting」+「Helper handles invalid scheduled_start_at safely」共 7+ assertion 全綠。

- [x] 2.2 [Decision 6: Kanban columns are equal-width with min-width caps] [/meetings SHALL render meetings as a 3-column date-bucketed Kanban] 新增 `packages/web/src/components/meetings-kanban.tsx`：接 `meetings: Meeting[]` prop，內部呼叫 `getMeetingDateBucket(m, new Date())` 分組成 3 個 array，渲染成 `grid-template-columns: repeat(3, minmax(280px, 1fr))` 的 3 欄。每欄 header = label（i18n keys `meetings.kanban.bucketUpcoming/Future/Past`） + 數字 count badge；body 是垂直 scroll 的 `<MeetingGridCard>` list（直接 reuse 既有 card component）；空 bucket 渲染中央 `<p>` 提示 (`meetings.kanban.bucketEmpty`)。column body `max-height: calc(100dvh - 280px)`。**驗證**：新增 `tests/components/meetings-kanban.test.tsx`：(a) 餵 spec scenario「Three-bucket distribution」5 顆 meeting，斷言三欄分別含 a+b、c+d、e；(b) 空 bucket 渲染提示文字；(c) `data-testid="meetings-kanban"` 根節點 `style.gridTemplateColumns` 含 `minmax(280px`。

- [x] 2.3 [/meetings SHALL render meetings as a 3-column date-bucketed Kanban] 改 `packages/web/src/routes/meetings/list.tsx`：把現有 auto-fill grid `<ul data-testid="meetings-grid">` 整段拿掉，換成 `<MeetingsKanban meetings={meetings} />`。空 list 走原本 `meetings-empty-state` Card 不變。新增 i18n keys `meetings.kanban.bucketUpcoming` / `bucketFuture` / `bucketPast` / `bucketEmpty` 到 zh-TW + en 兩 locale。**驗證**：擴充 `tests/routes/meetings/list.test.tsx` — 原 6 個 case 全綠（auto-fill grid 斷言改成檢查 `data-testid="meetings-kanban"` 存在 + 三欄存在）；`tests/locales/locales.test.ts` 全綠。

## 3. Shared MeetingsViewTabs

- [x] 3.1 [P] [Decision 5: MeetingsViewTabs uses radix Tabs in `controlled` mode] [/meetings and /meetings/calendar SHALL share a Kanban / 行事曆 tab bar] 新增 `packages/web/src/components/meetings-view-tabs.tsx`：接 `value: "kanban" | "calendar"` prop，內部 `<Tabs value={value} onValueChange={(next) => navigate({ to: next === "calendar" ? "/meetings/calendar" : "/meetings" })}>`。Tab labels 走 i18n keys `meetings.viewTabs.kanban` / `meetings.viewTabs.calendar`。新增 zh-TW: `Kanban` / `行事曆`、en: `Kanban` / `Calendar`。**驗證**：新增 `tests/components/meetings-view-tabs.test.tsx`：(a) `value="kanban"` 渲染兩個 trigger，kanban 是 active；(b) 點 calendar trigger 觸發 `useNavigate` 接 `/meetings/calendar`；(c) reverse；`locales.test.ts` 全綠。

- [x] 3.2 [/meetings and /meetings/calendar SHALL share a Kanban / 行事曆 tab bar] 把 `<MeetingsViewTabs>` mount 進 `packages/web/src/routes/meetings/list.tsx`（value="kanban"）跟 `packages/web/src/routes/meetings/calendar.tsx`（value="calendar"）兩個檔。list.tsx 內把舊的 inline `<Tabs>...卡片/日曆</Tabs>` block 整段拿掉。**驗證**：擴充 `tests/routes/meetings/list.test.tsx` + `tests/routes/meetings/calendar.test.tsx` — 各自斷言 page 含 `data-testid="meetings-view-tabs"` 且 active value 對。

## 4. ?from=calendar redirect

- [x] 4.1 [P] [Decision 4: from=calendar redirect via TanStack Router search] [NewMeeting SHALL honour ?from=calendar for redirect on submit and cancel] 改 `packages/web/src/routes/meetings/new.tsx`：用 `useSearch({ strict: false }) as { from?: string }` 抓 query；建一個 `cameFromCalendar = search.from === "calendar"` flag；submit 後 navigate 走 `cameFromCalendar ? { to: "/meetings/calendar" } : { to: "/meetings/$id", params: { id: created.id } }`；Cancel `<Link>` `to` 走 `cameFromCalendar ? "/meetings/calendar" : "/meetings"`。**驗證**：擴充 `tests/routes/meetings/new.test.tsx`：(a) 不帶 from → submit 後 router pathname 是 `/meetings/$id`（既有 case 不變）；(b) 新增 case `?from=calendar` → submit 後 router pathname 是 `/meetings/calendar`；(c) 新增 case 點 Cancel 後 pathname 是 `/meetings/calendar`；(d) `from=somewhere-else` → fallback `/meetings/$id`。

- [x] 4.2 [P] [NewMeeting SHALL honour ?from=calendar for redirect on submit and cancel] 改 `packages/web/src/routes/meetings/calendar.tsx`：把 calendar header 的「新會議」`<Link to="/meetings/new">` 改成 `<Link to="/meetings/new" search={{ from: "calendar" } as never}>`（搭 TanStack Router 的 search prop）。確保 `packages/web/src/routes/meetings/list.tsx` 的「新會議」Link 不帶 from（保留現狀）。**驗證**：擴充 `tests/routes/meetings/calendar.test.tsx`：點「新會議」trigger 後 `<a>.href` 含 `?from=calendar`。`tests/routes/meetings/list.test.tsx` 既有 case 全綠（list 的「新會議」link href 不含 query）。

## 5. Detail page nav group (BackLink + prev/next)

- [x] 5.1 [Decision 2: Prev/next nav uses cache-only lookup] [Meeting detail page SHALL render a prev/next navigation group alongside the BackLink] 新增 `packages/web/src/components/meeting-prev-next-nav.tsx`：接 `currentId: string` prop。用 `useQueryClient().getQueryData(meetingsListQueryOptions().queryKey)` 拿 cache；用 design Decision 2 的 `sortKey` function 排序；找 currentId 的 index；回傳 `[prevButton, BackLink, nextButton]` 三個 inline element。Cache miss 或 neighbour 不存在時對應方向 button `disabled`，hover 顯示 i18n tooltip `meetings.detail.prevNextDisabled`。**驗證**：新增 `tests/components/meeting-prev-next-nav.test.tsx` 涵蓋 spec 三個 scenario：(a) cache hit + middle position → 兩 button enabled + click 觸發 router navigate；(b) cache miss → 兩 button disabled；(c) edge position（first / last）→ 對應方向 disabled；新增 i18n key `meetings.detail.prevNextDisabled` 兩 locale。

- [x] 5.2 [Meeting detail page SHALL render a prev/next navigation group alongside the BackLink] 改 `packages/web/src/components/metadata-card.tsx`：把現在 `<Badge variant={...}>進行中</Badge> ... 建立時間 ...` 那行最前方插一個 `<MeetingPrevNextNav currentId={meeting.id} />` slot。改 `packages/web/src/routes/meetings/detail.tsx`：把現有的 `<BackLink to="/meetings" />` 那一行（第一個 child of main wrapper）整個刪掉 — BackLink 已經在 MetadataCard 內由 nav group 渲染。**驗證**：擴充 `tests/routes/meetings/detail.test.tsx`：(a) 既有 16 case 全綠；(b) 新增 case 斷言 detail page **沒有**獨立 `<BackLink>` 在 main wrapper 第一個 child；(c) 新增 case 斷言 MetadataCard 內含 `data-testid="meeting-prev-next-nav"`。

## 6. Acceptance

- [x] 6.1 [Acceptance criteria] 全 suite 驗證：`bun test` 347+/0 fail（從 337 加 ~10 case）、`bunx tsc --noEmit` 0 error、`bunx oxlint` 0 warning、`bun run build` 成功。**驗證**：四指令 exit code 0；`tests/locales/locales.test.ts` 全綠（zh-TW / en parity）。

- [x] 6.2 [Scope boundaries (in / out)] 確認沒越界：`git diff main..HEAD --stat -- packages/backend packages/auth packages/web/src/route-tree.tsx` 顯示 0 修改；無新增 lucide / animate-ui icon 進口（icon 政策來自 `animate-ui-icons-swap` 不變）；`packages/web/src/components/metadata-card.tsx` 右欄無新增 plan-block 元件（Q2 已 defer）。**驗證**：grep 上述條件全 pass。
