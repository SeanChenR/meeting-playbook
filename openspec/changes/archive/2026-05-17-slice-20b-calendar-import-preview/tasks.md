## 1. Backend — single Calendar event endpoint

- [x] 1.1 [P] 在 `packages/backend/meeting_playbook/calendar/schemas.py` 新增 `CalendarEventDetail` Pydantic schema（`id / title / start / end / description / attendees / organizer`），attendees 與 organizer 沿用 slice-05 既有型別；本 task 對應 spec 「GET single Calendar event returns the full detail used by the meeting preview form」shape 要求。完成標準：`uv run pytest packages/backend/tests/calendar/test_get_event_endpoint.py::test_schema_shape` red→green。
- [x] 1.2 在 `packages/backend/meeting_playbook/calendar/router.py` 加 `GET /api/calendar/events/{event_id}` endpoint：呼叫既有 `CalendarClient.get_event`、套用既有 resource-attendee filter（slice-07 落地的 `_event_from_resource`）、回 `CalendarEventDetail`；錯誤映射至 `calendar.not_connected` / `calendar.token_expired` / `calendar.event_not_found` / `calendar.network_error`。落實 spec 「GET single Calendar event returns the full detail used by the meeting preview form」與設計決策「Calendar event detail：新 `GET /api/calendar/events/{event_id}`，不重用 `/upcoming` 列表」（明確不擴 `/upcoming` 加 event_id query、不重用 batch list endpoint 做單一 detail fetch）。完成標準：新測 `packages/backend/tests/calendar/test_get_event_endpoint.py` 涵蓋 spec 的 3 個 Scenario（resource filter 去除、unknown id 回 404、未連線回 401）全綠。

## 2. Backend — POST /api/meetings 擴 calendar_event_id 與 attachments

- [x] 2.1 [P] 在 `packages/backend/meeting_playbook/meetings/schemas.py` 把 `MeetingCreate` request schema 新增 optional `calendar_event_id: str | None` 與 `attachments: list[str]`（default `[]`）；落實 spec 「Meeting carries an optional Calendar event reference」與「POST /api/meetings accepts an attachments list that associates pre-uploaded attachments with the new meeting」request body 形狀。完成標準：`uv run pytest packages/backend/tests/meetings/test_validation.py` 新增 case「body 帶 calendar_event_id + attachments 不噴 validation error」紅→綠。
- [x] 2.2 在 `packages/backend/meeting_playbook/meetings/repository.py` 把 `MeetingRepository.create` 簽名加 `calendar_event_id: str | None = None`（既有呼叫端不傳即 default null）；落實 spec 「Meeting carries an optional Calendar event reference」的「create with calendar_event_id persists the reference」Scenario。完成標準：`packages/backend/tests/meetings/test_repository.py` 新測「create with calendar_event_id」紅→綠且既有手動 create 測試保持綠。
- [x] 2.3 在 `packages/backend/meeting_playbook/meetings/router.py` 擴 `POST /api/meetings`：(a) 收 `calendar_event_id` + `attachments[]`；(b) 若 `attachments[]` 非空 → 對每個 id 驗 ownership + `meeting_id IS NULL`，任一失敗 → 422 `attachment.not_attachable` 且不建 meeting；(c) 建 meeting row（含 `calendar_event_id`）；(d) 若 attachments[] 非空 → 在同 transaction 內把 attachment row 的 `meeting_id` 寫回新 meeting id；(e) 若 `calendar_event_id` 非 null → `CalendarClient.get_event` + 呼叫既有 `PlaybookGenerator` + `PlaybookRepository.upsert_for_meeting`，generator 失敗時 meeting + attachments 已 commit、response 走既有 `playbook.generation_timeout` / `playbook.generation_failed`。落實 spec 「POST /api/meetings accepts an attachments list...」+ 「Meeting carries an optional Calendar event reference」+ 「Playbook generation is triggered by POST /api/meetings when calendar_event_id is supplied」，以及設計決策「Endpoint 拆分：保留 `POST /api/meetings` 為單一 create entry point」與「Playbook 生成觸發點下移至 `POST /api/meetings`」。完成標準：`packages/backend/tests/meetings/test_endpoints.py` 新增 6 條 case 全綠 — calendar_event_id 觸發 generator、無 calendar_event_id 不觸發 generator、attachments 寫回 meeting_id、別 user attachment 回 422、已 attach attachment 回 422、空 attachments 正常建。

## 3. Backend — 廢 POST /api/meetings/from-calendar

- [x] 3.1 在 `packages/backend/meeting_playbook/calendar/router.py` 把 `POST /api/meetings/from-calendar` handler body 替換為直接回 HTTP 410 Gone + `error_code: calendar.import_endpoint_removed` + 指向 `/meetings/new?from_calendar=<id>` 的 localizable message；落實 spec delta 內 `calendar-integration` capability 的 `POST from-calendar creates a meeting and persists a generated playbook draft` requirement REMOVAL（舊 slice-05 + slice-07 落地的 fire-and-forget 契約），以及 `meeting-management` capability 的 `POST /api/meetings/from-calendar is removed and returns HTTP 410 Gone` 新 requirement，並對應設計決策「Endpoint 拆分：保留 `POST /api/meetings` 為單一 create entry point，廢 `POST /api/meetings/from-calendar`」。完成標準：`packages/backend/tests/calendar/test_endpoints.py` 既有 `test_post_from_calendar_*` 改寫為「任何 body / 任何 auth 狀態都回 410 + correct error_code + 沒有 meeting 被建 + Playbook 生成 0 invocation」紅→綠。

## 4. Frontend — calendar API client 改寫

- [x] 4.1 [P] 在 `packages/web/src/lib/calendar-api.ts` 加 `getCalendarEvent(eventId: string)` query（tanstack-query useQuery wrapper、`GET /api/calendar/events/{id}`、回 typed `CalendarEventDetail`）；移除 `importFromCalendar` mutation 與其 export。落實 spec 「GET single Calendar event returns the full detail used by the meeting preview form」之前端對應。完成標準：`packages/web/src/lib/calendar-api.queries.test.ts` 新增 `getCalendarEvent` 測試 + 既有 mutations 測試檔內 `importFromCalendar` 對應 case 移除或改寫成 `getCalendarEvent`；`bun --filter @meeting-playbook/web test packages/web/src/lib/calendar-api` 綠。
- [x] 4.2 [P] 在 `packages/web/src/lib/meetings-api.ts` 把 `createMeeting` 的 request type 加 optional `calendar_event_id?: string` 與 `attachments?: string[]`，沿用 react-query mutation；落實 spec 「Meeting carries an optional Calendar event reference」+「POST /api/meetings accepts an attachments list...」之前端契約。完成標準：`packages/web/src/lib/meetings-api.ts` type 編譯通過；`bun --filter @meeting-playbook/web test packages/web/src/lib/meetings-api` 既有測試零 regression。

## 5. Frontend — /calendar/import import button 改 navigate

- [x] 5.1 在 `packages/web/src/routes/calendar/upcoming.tsx` 把「匯入」按鈕的 onClick 從 fire `importFromCalendar` mutation 改成 `navigate({ to: "/meetings/new", search: { from_calendar: event.id } })`（tanstack-router navigate）；移除 mutation pending / error state 對應 UI；落實 spec 「Calendar import button on /calendar/import navigates to the preview form instead of firing a create request」與設計決策「Endpoint 拆分：保留 `POST /api/meetings` 為單一 create entry point，廢 `POST /api/meetings/from-calendar`」。完成標準：`packages/web/src/routes/calendar/upcoming.test.tsx` 改寫 import click 測試為「click 後 navigate spy 被以 `/meetings/new?from_calendar=<id>` 呼叫、fetch spy 零 POST」紅→綠。

## 6. Frontend — /meetings/new 預填 + attachment 區塊

- [x] 6.1 在 `packages/web/src/routes/meetings/new.tsx` 加 `from_calendar` search param 讀取 → 觸發 `getCalendarEvent` query → 用 useEffect 把 result 預填 `title / scheduled_start_at / scheduled_end_at / me_display_name / counterparty_display_name`（pick_counterparty 邏輯走 frontend helper：單一非 viewer human → 用其 displayName、多於一個 → 留空）；落實 spec 「/meetings/new route pre-fills the form from a Calendar event when from_calendar query is present」與設計決策「前端：擴 `/meetings/new` 路由讀 `?from_calendar=<event_id>` query」。完成標準：`packages/web/src/routes/meetings/new.test.tsx` 新增 3 條 case 全綠 — single non-viewer attendee 預填 counterparty、multi attendee 留空、未帶 from_calendar query 行為不變。
- [x] 6.2 在 `packages/web/src/routes/meetings/new.tsx` 整合 S20a attachment 區塊：列出 current user `meeting_id IS NULL` 的 attachments、提供勾選 UI、submit 時把選中的 attachment id 加進 `createMeeting({ calendar_event_id, attachments })` payload；落實 spec 「POST /api/meetings accepts an attachments list...」之前端流程與設計決策「Attachment 預覽掛載：在 meeting 建立**之前** stage」。完成標準：`packages/web/src/routes/meetings/new.test.tsx` 新增 case 「勾選 2 個 attachment + submit → createMeeting 收到 `attachments: ["att_1", "att_2"]`」紅→綠。
- [x] 6.3 在 `packages/web/src/routes/meetings/new.tsx` 加 calendar fetch 失敗的 inline error banner：用 `localizedErrorMessage(error_code, t)` 解析錯誤、保留表單為空 + 可手動 submit（不傳 calendar_event_id 即走 manual create）；落實 spec 「/meetings/new route pre-fills the form from a Calendar event...」之「Calendar fetch error displays inline message and allows manual create」Scenario。完成標準：`packages/web/src/routes/meetings/new.test.tsx` 新增 case 「mock 404 calendar.event_not_found → banner 顯示對應 localized message + manual submit 仍 work」紅→綠。

## 7. i18n + ADR amendment + 收尾

- [x] 7.1 [P] 在 `packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json` 同步新增 keys：`meetings.new.preFillBanner`、`meetings.new.preFillMultiAttendeeHint`、`errors.calendar.event_not_found`、`errors.calendar.import_endpoint_removed`、`errors.attachment.not_attachable`；落實 spec 「/meetings/new route pre-fills the form...」與「POST /api/meetings/from-calendar is removed...」的 localized message 要求。完成標準：`bun --filter @meeting-playbook/web test src/locales/locales.test.ts` deep-equal 通過。
- [x] 7.2 在 `docs/adr/0027-calendar-scope-link.md` 末尾加「## Amended by slice-20b」段落，記錄 import 流程從 fire-and-forget 改成 preview-and-confirm 的拆分理由（讓使用者在 Playbook 生成前 attach 檔案 + 校 display name）、新流程 endpoint 名稱、ADR-0027 主題（OAuth scope-link via Better Auth `linkSocial`）維持不變；落實設計決策「ADR-0027 amendment（不開新 ADR）」。完成標準：`grep -F "Amended by slice-20b" docs/adr/0027-calendar-scope-link.md` 命中。
- [x] 7.3 跑全套 backend + frontend 測試與 lint：`cd packages/backend && uv run pytest`、`bun --filter @meeting-playbook/web test`、`bun run lint`；確認 `calendar` / `meetings` / `playbook_generation` 模組改動範圍覆蓋率 ≥ 80%。完成標準：所有指令 exit 0 且 coverage report 滿足門檻。
