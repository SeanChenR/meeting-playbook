## 1. Foundation — token system + theme + new primitives

- [x] 1.1 [Decision 3: Dark mode 用 `data-theme` 屬性 + ThemeProvider Context] [Design tokens SHALL be expressed as oklch-based CSS variables across two themes] 翻新 `packages/web/src/index.css`：把 `/tmp/claude_design_bundle/meeting-playbook/project/design-tokens.css` 完整 port 進來，`:root` + `[data-theme="light"]` 走紫色 (`--primary-hue: 280`)、`[data-theme="dark"]` 走暖橘 (`--primary-hue-dark: 50`)、含 `--space-N` / `--radius-N` / `--text-N` / `--color-me/them/me-soft/them-soft` / `--me-tint-alpha` / `--them-tint-alpha` 全套 token；含 density / radius / contrast tweak override blocks；含 shimmer / pulse / fade-in keyframes。**驗證**：產出的 `index.css` 內無任何 raw hex 字串於非 token 區段；`bun run build` 通過；手動 `documentElement.setAttribute("data-theme", "dark")` 後 `--color-primary` resolve 成 oklch hue 接近 50。
- [x] 1.2 [P] [Decision 3: Dark mode 用 `data-theme` 屬性 + ThemeProvider Context] [ThemeProvider SHALL persist user choice and expose a typed Context] [Data shapes (TypeScript)] 新增 `packages/web/src/lib/theme-provider.tsx` export `ThemeProvider` + `useTheme` hook，支援 `theme: "light" | "dark" | "system"` 三態、resolve "system" → 看 `prefers-color-scheme`、writes/reads `localStorage.mp-theme`、`matchMedia` listener 響應 OS 偏好變化、corrupted localStorage 回 fallback `system` 不 throw。**驗證**：新增 `tests/lib/theme-provider.test.tsx` 涵蓋四 scenario：(a) no localStorage + system light → resolved=light + data-theme=light、(b) setTheme("dark") → localStorage="dark" + data-theme="dark"、(c) `matchMedia` 改變觸發 resolved 重算、(d) localStorage 寫 garbage → fallback system 不 throw。
- [x] 1.3 [P] [Decision 1: shadcn/radix headless 重寫（不 port inline-style）] [Five shadcn primitives SHALL be added with consistent file layout] 用 `bunx shadcn@latest add select dropdown-menu tooltip skeleton sonner` 拉進 5 個 primitives 到 `packages/web/src/components/ui/`，每個都產出對應檔案（select.tsx / dropdown-menu.tsx / tooltip.tsx / skeleton.tsx / sonner.tsx）並都消費 token CSS variable（無 hex）。**驗證**：5 個檔案存在；新增 `tests/components/ui/primitives-smoke.test.tsx`，每個 primitive 至少一個 mount + 可見性 smoke test；`bunx tsc --noEmit` 0 error；`bunx oxlint` 0 error。
- [x] 1.4 [Decision 3: Dark mode 用 `data-theme` 屬性 + ThemeProvider Context] 新增 `packages/web/src/components/theme-toggle.tsx`：使用 shadcn `DropdownMenu` + lucide `Sun` / `Moon` / `Monitor` icons，點擊展開 3 選項 (light / dark / system) 並呼叫 `useTheme().setTheme(...)`；現在選的選項顯示 `Check` icon。新增 i18n keys `theme.toggle.light` / `theme.toggle.dark` / `theme.toggle.system` 至 zh-TW + en。**驗證**：新增 `tests/components/theme-toggle.test.tsx` 涵蓋三 case：渲染、切 dark 觸發 setTheme、切 system 觸發 setTheme；`locales.test.ts` 通過。
- [x] 1.5 [P] [Decision 4: 動畫套件分工] `bun add framer-motion sonner` 加入 deps 並用 `bunx shadcn@latest` 沒帶到的 radix peer deps 補齊；新增 `packages/web/src/lib/motion-presets.ts` export `paneEnter` / `paneStagger` / `tabContent` / `modalScale` / `cardHover` typed `Variants` + `Transition` 物件。**驗證**：`package.json` 含 `framer-motion`、`sonner`、`@radix-ui/react-{select,dropdown-menu,tooltip}`；`lib/motion-presets.ts` 各 export 跑 TypeScript inference 拿到正確 `Variants` / `Transition` 型別；新增 `tests/lib/motion-presets.test.ts` 確認每個 preset shape 含 `initial` + `animate`（exit 可選）。
- [x] 1.6 [P] [Decision 4: 動畫套件分工] [Animation library usage SHALL follow the framer-motion / magicui / animate-ui split] 安裝 magicui + animate-ui：`bunx magicui-cli@latest add number-ticker shimmer marquee` 拉 magicui shadcn-style 元件、`bun add @animate-ui/react` 拉 animate-ui，新增 `packages/web/src/components/magicui/` 跟 `packages/web/src/components/animate-ui/` 目錄存放這兩家的元件。**驗證**：兩個目錄存在 + 至少各有 1 個元件（譬如 `number-ticker.tsx` 跟 `route-transition.tsx`）；`bunx tsc --noEmit` 0 error；smoke test `tests/components/magicui/number-ticker.test.tsx` 確認從 0 ticker 到 5 可被觀察。

## 2. Shells — ProtectedShell + AuthShell + NavBar/BackLink 一致性

- [x] 2.1 [Behavior contract] [Decision 1: shadcn/radix headless 重寫（不 port inline-style）] 重寫 `packages/web/src/components/protected-shell.tsx`：套上 `ThemeProvider` wrap、NavBar 為 56px sticky top（左 logo + 右 locale dropdown / theme-toggle / Avatar / Logout button）、main 區走 max-width 1200 `mx-auto` 並支援 `fullBleed` prop 不限寬、bottom mount `<Toaster />` (sonner)。**驗證**：擴充 `tests/components/protected-shell.test.tsx` 涵蓋：(a) NavBar height 56、(b) theme-toggle / locale-toggle / avatar / logout 都在右側 cluster、(c) fullBleed=true 時 main 沒 max-width 限制、(d) Toaster 永遠 mount。
- [x] 2.2 [P] [Behavior contract] 重寫 `packages/web/src/components/auth-shell.tsx`：中央 card layout + 右上角浮 theme-toggle（不顯示完整 NavBar）；無 max-width 限制。**驗證**：新增 `tests/components/auth-shell.test.tsx`：(a) theme-toggle 在右上 absolute 位置渲染、(b) `children` 在 viewport 中央。
- [x] 2.3 [Behavior contract] 改 `packages/web/src/components/locale-toggle.tsx` 從單按鈕 → shadcn `DropdownMenu`：trigger 顯示當前語言碼（`zh-TW` / `EN`），展開 2 個選項；選擇後 invoke `i18n.changeLanguage(...)`。**驗證**：擴充 `tests/components/locale-toggle.test.tsx`：(a) trigger 渲染當前語言、(b) 點開出現 zh-TW + EN 兩選項、(c) 選 EN 觸發 changeLanguage。
- [x] 2.4 [P] [Behavior contract] 新增 `packages/web/src/components/back-link.tsx` 抽出共用 BackLink primitive：ghost button + ArrowLeft icon + 預設文字 `返回列表`，接 `to` prop 直接走 TanStack Router `<Link>`。新增 i18n key `nav.back` / `nav.backToList`。**驗證**：新增 `tests/components/back-link.test.tsx`：(a) 渲染 ArrowLeft icon + 預設 fallback 文字、(b) 點擊觸發 router navigate；`locales.test.ts` 通過。

## 3. Auth routes — login / signup / totp / home

- [x] 3.1 [Decision 7: Demo 資料只當設計參考、production 走空 state / API] [Scope boundaries (in / out)] 重寫 `packages/web/src/routes/login.tsx` 對齊 design bundle `LoginScreen`：360px width card、Logo block、Email + Password fields、`Button variant="primary" full`、`Or` separator、`使用 Google 登入` secondary button、底部 `沒帳號？立即註冊` 連結；保留所有現有 i18n key 跟 auth hook 行為。**驗證**：擴充既有 `routes/login.test.tsx` + `tests/routes/login-i18n.test.tsx`：原有 3 個 case 全綠（包含被列為 baseline failure 的 `renders link to signup page`）。
- [x] 3.2 [P] 重寫 `packages/web/src/routes/signup.tsx` 對齊 design bundle `SignupScreen`：360px card、Display name + Email + Password fields with hint、`建立帳號` primary button、`已有帳號？登入` 連結。**驗證**：擴充既有 `routes/signup.test.tsx`：3 個 case 全綠（包含 baseline failure `renders link back to login`）。
- [x] 3.3 [P] [Decision 5: 既有 8 個 baseline test failures 在 phase 6 一起修] 重寫 `packages/web/src/routes/totp/enroll.tsx` 對齊 design bundle `TotpEnrollScreen`：420px card、Shield icon hero、QR code 168×168 placeholder grid、secret code mono row + copy button、6-slot TotpInput、Separator、Backup codes 2-col grid + download button、完成設定 primary button。**驗證**：既有 `tests/routes/totp/enroll.test.tsx` 4 個 case 全綠（含原 baseline 一個 `invalid 'data'` 失敗，因 mocked api shape 被新元件矯正）。
- [x] 3.4 [P] 重寫 `packages/web/src/routes/totp/verify.tsx` 對齊 design bundle `TotpVerifyScreen`：380px card、Lock icon hero、6-slot TotpInput with 30s refresh hint、`驗證` primary button、`使用備用碼登入` 連結。**驗證**：既有 `tests/routes/totp/verify.test.tsx` 2 個 case 全綠（含 baseline `Tuple type '[]' of length '0'` 改寫後消失）。
- [x] 3.5 [Decision 5: 既有 8 個 baseline test failures 在 phase 6 一起修] 重寫 `packages/web/src/routes/home.tsx` 對齊 design bundle `HomeScreen`：頂部 greeting + 下個會議時間、Sparkles CTA card（新會議 / 從 Calendar 匯入）、條件渲染 `Alert tone="warning"` 提示啟用 TOTP（when 2FA off）、3-col Stat cards (本月會議 / 平均時長 / 進行中)、最近會議列表 card。**驗證**：既有 `tests/routes/home.test.tsx` 全部 case 全綠（含 baseline failure `renders Enable two-factor link when twoFactorEnabled is false`）；用空 API 結果驅動 empty state（per Decision 7）。

## 4. Meeting routes (non-detail)

- [x] 4.1 [Behavior contract] 重寫 `packages/web/src/routes/meetings/list.tsx` 對齊 design bundle `MeetingsList`：page header（標題 + meta `N 場 · 本週 M 場待進行`）+ 右側 controls (Tabs 卡片/日曆 + 從 Calendar 匯入 secondary button + 新會議 primary button)；3-col responsive grid (`repeat(auto-fill, minmax(320px, 1fr))`) 渲染 meeting cards；卡片 hover shadow + border 升級、左側 3px 色條（依 status 或 hash 選色）、status badge dot + size sm、底部 calendar icon + duration + 可選 `playbook` badge；無 emoji；空狀態走設計裡的 emptyHint copy 但去 emoji。**驗證**：擴充 `tests/routes/meetings/list.test.tsx`：(a) cards 3-col grid 渲染、(b) hover 樣式套用、(c) empty list 渲染中性 copy（無 emoji）、(d) 點卡片觸發 navigate。
- [x] 4.2 [P] [Decision 5: 既有 8 個 baseline test failures] 重寫 `packages/web/src/routes/meetings/new.tsx` 對齊 design bundle `NewMeetingScreen`：520px 中央 card、`BackLink` 在 main 第一行、會議標題 + 對方/我方 grid + 預定開始/結束 grid + 會議目標 textarea (hint：選填，AI 會用來生成 playbook)；底部 `取消` ghost + 右側 `儲存草稿` secondary + `建立並生成 playbook` primary（Sparkles icon）。**驗證**：既有 `tests/routes/meetings/new.test.tsx` + `routes-i18n.test.tsx` 全綠（含 baseline failure `submitting valid fields posts and navigates`）。
- [x] 4.3 [P] 重寫 `packages/web/src/routes/meetings/calendar.tsx` 對齊 design bundle `CalendarScreen`：page header（標題 + 年月+週次 meta）+ 右側 controls（Tabs 月/週 + 上一頁/今天/下一頁 + 新會議 primary）；主區 grid 1fr+280px：左是 WeekGrid（50px 時段欄 + 7 day columns，event 著色用 `color-mix(in oklch, ...)` 配 3px 左 border）、右 sidebar Card「未排程」內含 3 個提案 cards 各帶 排定 / 隱藏 buttons。**驗證**：既有 `tests/routes/meetings/calendar.test.*` 全綠（或從零建一個 smoke test 涵蓋：渲染 + event 顯示 + 切月/週 toggle）。
- [x] 4.4 [P] [Decision 5: 既有 8 個 baseline test failures] 重寫 `packages/web/src/routes/calendar/upcoming.tsx` 對齊 design bundle `CalendarImportScreen`：BackLink、page header（標題 + 來源說明 + 重新整理 button）、`Card padded={false}` 容器內列每場 event row：左 42×42 月日方塊、中間標題 + Suggested badge + 描述 row（人 / 公司 / 時間 / 參與者數）、右 `匯入 + 生成` primary button (Sparkles icon) 或 `已匯入` success badge。**驗證**：既有 `tests/routes/calendar/upcoming.test.tsx` 全綠（含 baseline failure `clicking import dispatches POST and on success navigates`）。

## 5. Detail centerpiece — MetadataCard + Tabs + Workspace + Panes shell

- [x] 5.1 [Behavior contract] [MetadataCard SHALL render as a two-column layout with embedded CaptureIndicator] 重寫 `packages/web/src/routes/meetings/detail.tsx` 對齊 design bundle `MeetingDetailScreen`：在 `ProtectedShell fullBleed` 內部 padding wrapper（max-width 1440 置中）→ BackLink → MetadataCard → Tabs 列 + LayoutSwitcher（在 tabs 右側、只在 workspace tab 顯示）→ Workspace / Summary tab content 區塊。**驗證**：擴充 `tests/routes/meetings/detail.test.tsx` 既有 case 全綠（含 baseline failure `delete confirmation flow`）；新加 case：BackLink 渲染、LayoutSwitcher 只在 workspace tab 出現。
- [x] 5.2 [P] [MetadataCard SHALL render as a two-column layout with embedded CaptureIndicator] 重寫 metadata card 部分（detail.tsx 內部 / 視需要抽出 `metadata-card.tsx`）：左欄含 status badge + title（`--text-2xl` font-weight 700） + grid 2-col 對方/我方/錄音 with `Avatar` size 18；右欄 0-0-280px：`Label` + shadcn `Select`（hint）+ embedded `CaptureIndicator`。**驗證**：擴充 detail.test.tsx 加 metadata-card 結構斷言：(a) data-testid="meeting-metadata-card" 存在、(b) 左欄含 `meeting-title` + 3 個 metadata rows、(c) 右欄含 `asr-provider-selector` + `capture-indicator`。
- [x] 5.3 [MetadataCard SHALL render as a two-column layout with embedded CaptureIndicator] 重寫 `packages/web/src/components/capture-indicator.tsx`：兩條 CaptureRow（麥克風 / 我方 + 系統音訊 / 對方），每條 `Dot tone="rec" pulse` + label 100px min-width + 10-bar sparkline（bar 寬 2px、高度依 audio frame amplitude 動態，bar 顏色 `--color-recording` 帶 opacity）；recording=false 時 dot 變灰 + 所有 bar 高度回 1px。**驗證**：擴充既有 `tests/components/capture-indicator.test.tsx`：(a) recording=true 渲染 2 row、(b) 每 row 含 10 bar、(c) recording=false dot 為 muted。
- [x] 5.4 [P] [Workspace layout SHALL switch between three-column and stacked via animate-ui transition] 重寫 `packages/web/src/components/layout-switcher.tsx` 改成 design bundle 樣式（icon-only segmented：Cols / Rows icon 各一個小 button、3px padded outer + 26px height inner button、active state 用 surface-2 + foreground、inactive 用 muted-foreground）；persist 到 `localStorage.mp-detail-layout`。**驗證**：擴充既有 `tests/components/layout-switcher.test.tsx`：(a) 渲染 2 icon button、(b) 點 rows 寫 localStorage + 觸發 onChange、(c) reload 後 prop 預設值來自 localStorage。
- [x] 5.5 [Workspace layout SHALL switch between three-column and stacked via animate-ui transition] 新增 `packages/web/src/components/workspace.tsx`（或在 detail.tsx 內部）包 PlaybookPane / TranscriptPane / AdvisorPane 三 panes，`cols` mode = grid `minmax(260px, 0.8fr) minmax(360px, 1.2fr) minmax(280px, 0.9fr)` 12px gap、`rows` mode = grid rows `minmax(180px, 0.9fr) minmax(220px, 1.2fr) minmax(180px, 0.9fr)`；layout 切換用 `animate-ui` layout-transition wrapper （reorder + size morph 250ms）；尊重 `prefers-reduced-motion`。**驗證**：新增 `tests/components/workspace.test.tsx`：(a) cols mode grid template 為 3 cols、(b) rows mode grid template 為 3 rows、(c) prefers-reduced-motion=reduce 時無 transition class、(d) 3 panes 都 mount。
- [x] 5.6 [Tab content SHALL cross-fade via framer-motion] [Decision 4: 動畫套件分工] 在 detail.tsx tab content 切換加上 framer-motion `AnimatePresence` + `tabContent` preset（exit 100ms、enter 150ms cross-fade）；summary tab 在 status !== "completed" 時 disabled（radix tabs 原生支援 `disabled` prop）。**驗證**：擴充 detail.test.tsx：(a) 切 tabs 觸發 AnimatePresence motion class、(b) status='scheduled' 時 summary tab 有 `data-disabled` attribute、(c) `prefers-reduced-motion=reduce` 時動畫即刻完成。

## 6. Pane polish + animations + baseline test fixes

- [x] 6.1 [TranscriptPane SHALL render speaker contrast via all four visual cues simultaneously] [TranscriptChunk speaker contrast SHALL exceed the slice-7 baseline] 重寫 `packages/web/src/components/transcript-pane.tsx` 對齊 design bundle `TranscriptPane` + `TranscriptChunk`：每 chunk 一個 wrapper div 含 `data-speaker` attribute + `border-left: 3px solid var(--color-me|them)` + background `color-mix(in oklch, var(--color-me-soft|them-soft) calc(var(--*-tint-alpha)*1000%), transparent)` + 內含 `data-testid="speaker-dot"` (Dot tone=me/them) + 名字 semibold + 時間 mono font + 主文 1.6 line-height；可接 `contrastLevel?: "subtle"|"strong"` prop 但預設 "strong"；live placeholder chunk 用 design bundle 的 `LiveChunkPlaceholder` 含 Skeleton bars + pulse dot。**驗證**：重寫 + 擴充 `tests/components/transcript-pane.test.tsx`：(a) counterparty chunk 渲染 4 cue（border-left-width: 3px、background non-transparent、speaker-dot 存在、name font-weight=600）、(b) me chunk 用 `--color-me` 系列、(c) `data-speaker` attribute 保留、(d) contrastLevel="strong" 時 `--*-tint-alpha` 套到 `0.14/0.20`。
- [x] 6.2 [P] [TranscriptPane re-run overlay SHALL animate in with framer-motion] [Decision 4: 動畫套件分工] 整合既有 slice-11 `transcript-rerun-overlay` 用 framer-motion `AnimatePresence` + `paneEnter` preset 包；overlay 內 `chunks_processed` 數字用 magicui `NumberTicker` 動態 tick；`chunks_total` 純文字；`data-testid="transcript-rerun-overlay"` 保留。**驗證**：擴充既有 `tests/components/transcript-pane-rerun.test.tsx`：(a) overlay 出現時帶 motion class（AnimatePresence wrap）、(b) `chunks_processed` element 使用 NumberTicker（via dom 查 numeric content 變化 transition）、(c) overlay hide 時 exit 動畫 trigger。
- [x] 6.3 [P] [ChatBubble component SHALL replace flat advisor message list] [Behavior contract] 重寫 `packages/web/src/components/advisor-pane.tsx` 對齊 design bundle `AdvisorPane`：訊息列表渲染為 `ChatBubble` (新元件 `packages/web/src/components/chat-bubble.tsx`)：role=user 右對齊 primary 背景 4-corner asymm radius、role=assistant 左對齊 surface-2 1px border 4-corner asymm radius；max-width 85%；底部 sticky input row（單行 input + Send icon primary button）；訊息列表 idle 時尾巴顯示 3 個 suggestion `Chip` button（i18n keys `meetings.advisor.suggestions.{nextStep,realConcern,closing}`）；點 chip prefill input + focus；保留 slice-9 chat persistence behaviour。**驗證**：擴充既有 `tests/components/advisor-pane.test.tsx`：(a) ChatBubble user 右對齊 primary 背景、(b) ChatBubble assistant 左對齊 surface-2、(c) idle 時 3 suggestion chips 渲染、(d) 點 chip 觸發 input value + focus；`locales.test.ts` 通過。
- [x] 6.4 [P] [Behavior contract] 重寫 `packages/web/src/components/playbook-pane.tsx` 對齊 design bundle `PlaybookPane`：用新 `Pane` shell（accent 3px 色條 + header title + AI 草稿 badge + actions tabs 結構化/Markdown）；結構化視圖渲染 Section header（uppercase muted text）+ section body（lists / strong / em 樣式）；保留所有現有 slice-4 props 與 save 行為；Markdown tab 仍為純 textarea（markdown 渲染留 deferred change）。**驗證**：擴充既有 `tests/components/playbook-pane.test.tsx`：(a) Pane shell 渲染、(b) AI 草稿 badge 顯示、(c) Section headers 都是 uppercase letter-spacing 0.8、(d) save / dirty / clean states 保持原 e2e。
- [x] 6.5 [Behavior contract] 重寫 `packages/web/src/components/summary-pane.tsx` 對齊 design bundle `SummaryPane`：主區 + 280px sidebar grid；主區內含 markdown 內容 + 標頭含 Sparkles icon + AI 摘要 badge (vN · time) + 重新生成 ghost / 複製 secondary / 匯出 .md primary 三按鈕；side card 含 KV 元資料 (時長/逐字稿段數/ASR/信心度) + 關鍵字 Badge primary 列表 + Sparkles Alert info；保留 slice-10 markdown 渲染 + export 行為。**驗證**：擴充既有 `tests/components/summary-pane.test.tsx`：(a) 主區 + sidebar 2-col grid、(b) 3 個 action button、(c) sidebar KV rows、(d) keyword Badge 渲染。
- [x] 6.6 [P] [Decision 2: 1 change 6 phase，spec deltas 統一 archive] [Decision 6: 既有 i18n keys 保留 + locale parity] [Failure modes] i18n parity 補齊：將 phase 1-6 內新增的所有 keys (theme.toggle.*, nav.back, meetings.advisor.suggestions.*, etc.) 同步加進 `packages/web/src/locales/zh-TW.json` + `en.json`。**驗證**：`bun test src/locales/locales.test.ts` 6 case 全綠（deep-equal guard 過）。
- [x] 6.7 [Decision 5: 既有 8 個 baseline test failures 在 phase 6 一起修] [Acceptance criteria] 修剩餘 baseline failure：(a) `tests/lib/auth-client.test.ts` 兩個 `exposes signIn.social` / `exposes twoFactor.verifyTotp via plugin` — audit auth-client.ts 是否 export 對；(b) `tests/test/fixtures/router.tsx` TypeScript `trailingSlash` 型別 — 改 `trailingSlash: "never" as const` 強制 narrow；(c) phase 3-5 改 DOM 後若仍有 link / form test 失敗，調整 selector 對齊新 DOM。**驗證**：`bun test` 完整跑：所有 baseline 8 條原 fail 變綠；總 passed 從 250 提升到 ≥290；新元件 tests 全綠；`bunx tsc --noEmit` 0 error；`bunx oxlint` 0 error。

## 7. Acceptance verification

- [x] 7.1 [Acceptance criteria] [Scope boundaries (in / out)] 跑完整 frontend test + lint + build + type-check：`bun test` ≥290 passed + 0 fail、`bunx tsc --noEmit` 0 error、`bunx oxlint` 0 error、`bun run build` 成功。**驗證**：四指令 exit code 0；backend test 不動 (`packages/backend` 維持 357 passed)；無 i18n key drift（locales.test.ts 過）。
- [x] 7.2 [Decision 8: Mobile / 響應式 design 暫時不處理] [Acceptance criteria] Sean 手動 walkthrough 11 個 route：home / login / signup / totp/enroll / totp/verify / meetings / meetings/new / meetings/calendar / meetings/$id / calendar/import；每 route 在 light + dark 各看一次。**驗證**：Sean 在 GitHub PR 描述或 commit message 內列出 11 route × 2 theme 的勾選清單，每項目 OK；行動裝置 (375px viewport) 至少不崩 (no horizontal scroll / 元素疊到看不到)。
- [x] 7.3 [Acceptance criteria] 確認本 change 沒越界 backend / migration / router-tree 等 out-of-scope 區域。**驗證**：`git diff main..HEAD --stat -- packages/backend packages/auth packages/web/src/route-tree.tsx openspec/specs` 顯示 backend / auth / route-tree 零修改；spec changes 限於本 change 的 deltas 經 archive 流程自動 sync。
