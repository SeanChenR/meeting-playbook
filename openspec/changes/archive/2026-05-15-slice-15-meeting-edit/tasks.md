## 1. Migration 與 schema 收緊

- [x] 1.1 [P] [AC-2][AC-3] 新增 Alembic migration `packages/backend/alembic/versions/0012_meeting_start_not_null.py`，在單一 transaction 內 `UPDATE meeting SET scheduled_start_at = created_at WHERE scheduled_start_at IS NULL` 再 `ALTER COLUMN scheduled_start_at SET NOT NULL`，downgrade `DROP NOT NULL`；落實設計決策「Migration `0012_meeting_start_not_null`：先 backfill 再 SET NOT NULL」與 modified spec「Meeting carries optional scheduled start and end timestamps」的 "Migration backfills pre-existing NULL rows" scenario。**Verify**: 新測 `packages/backend/tests/test_alembic_meeting_scheduled_not_null.py` 跑 upgrade 後 `SELECT COUNT(*) FROM meeting WHERE scheduled_start_at IS NULL` 回 `0`、downgrade 後 column nullable=True、re-upgrade 不噴錯（3 輪可逆）。
- [x] 1.2 [P] [AC-2][AC-4] 收緊 `packages/backend/meeting_playbook/meetings/models.py` 把 `scheduled_start_at` 從 `Mapped[datetime | None]` 改 `Mapped[datetime]`（NOT NULL）；同時改 `MeetingCreate` 把 `scheduled_start_at: datetime` 改必填、移除 `| None = None`，並讓 `POST /api/meetings` 缺欄位時回 422 `meeting.scheduled_start_at.required`，落實 modified spec「Meeting carries optional scheduled start and end timestamps」的 "Create without scheduled_start_at is rejected" + "Create with end-before-start is rejected" scenarios。**Verify**: 既有 `tests/meetings/test_endpoints.py` 內 `test_create_without_scheduled_start_returns_422` 新增 + `test_create_with_end_before_start_returns_422` 新增；既有 dual-channel / detail-read 測試不退化。

## 2. Repository: update_for_user 與 PATCH router 擴充

- [x] 2.1 [AC-1] 在 `packages/backend/meeting_playbook/meetings/repository.py` 新增 `update_for_user(*, user_id, meeting_id, fields: dict)`，落實設計決策「Repository: 新 `update_for_user(...)` 並 deprecate `update_asr_provider_for_user`」與 ADDED spec「MeetingRepository.update_for_user writes any subset of mutable meeting fields in a single UPDATE」。行為：空 fields → delegate `get_for_user`，0 UPDATE；非空 → 單一 UPDATE；跨 user → None。**Verify**: 新測 `tests/meetings/test_repository.py::test_update_for_user_empty_fields_no_update`（用 SQLAlchemy `event.listen("before_cursor_execute")` 確認 0 UPDATE）、`::test_update_for_user_multi_field_single_update`（確認 1 UPDATE）、`::test_update_for_user_cross_user_returns_none`、`::test_update_asr_provider_for_user_still_works`（既有 slice-11 行為 delegate 不破）。
- [x] 2.2 [AC-1] 在 `packages/backend/meeting_playbook/meetings/schemas.py` 把 `MeetingPatch` 擴成 6 個 optional 欄位、沿用 `MeetingCreate._strip_and_require` 處理 3 個字串欄位、新增 `model_validator(mode="after")` 擋 PATCH body 內 `scheduled_end_at < scheduled_start_at`，落實設計決策「字串欄位驗證：複用 `MeetingCreate._strip_and_require` 的策略」與「Endpoint 形狀：擴 `MeetingPatch` schema 為多欄位 partial update」。**Verify**: 新測 `tests/meetings/test_validation.py::test_meeting_patch_strips_whitespace`、`::test_meeting_patch_rejects_empty_after_strip`、`::test_meeting_patch_body_reversed_time_range_422`。
- [x] 2.3 [AC-1][AC-7] 把 `packages/backend/meeting_playbook/meetings/router.py::patch_meeting` 改用 `update_for_user`、加 cross-field time-range 驗證（merge body + DB current row）、保留空 body no-op 200、保留跨 user 404，落實 ADDED spec「PATCH /api/meetings/{id} accepts partial updates to title, scheduled times, display names, and asr_provider」與設計決策「時間欄位驗證：cross-field 驗 `end >= start`」。**Verify**: 擴 `tests/meetings/test_endpoints.py` 涵蓋 7 個 scenario：PATCH title only / PATCH multi-field / PATCH 空 body / PATCH 空白字串 422 / PATCH 只送 end 但既有 start 比 end 晚 422 / PATCH 跨 user 404 / PATCH asr_provider only 沿用 slice-11；既有 slice-11 router test 0 regression（`uv run pytest tests/meetings/test_endpoints.py -k patch`）。

## 3. Frontend API helper 與 i18n 鋪墊

- [x] 3.1 [P] [AC-8] 在 `packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json` 同步新增 `meetings.edit.{title,scheduledStart,scheduledEnd,counterpartyDisplayName,meDisplayName,save,cancel,saved,editToggle}` + `errors.meeting.{invalidTimeRange,invalidField,scheduledStartRequired}`；落實 ADDED spec「meeting detail page lets the user inline-edit title, scheduled times, and display names」的雙 locale 需求。**Verify**: `bun --filter @meeting-playbook/web test packages/web/src/locales/locales.test.ts` deep-equal 通過；`grep -F 'meetings.edit.save' packages/web/src/locales/en.json` 與 `packages/web/src/locales/zh-TW.json` 皆命中。
- [x] 3.2 [P] [AC-1] 在 `packages/web/src/lib/meetings-api.ts` 新增 `patchMeeting(id, body: Partial<MeetingPatchBody>): Promise<MeetingDetailRead>`（fetch PATCH，回 JSON；422 throw 含 `error_code` 的 Error），對應 ADDED spec「PATCH /api/meetings/{id} accepts partial updates...」的 client 端 helper。**Verify**: 新測 `packages/web/src/lib/meetings-api.test.ts::patchMeeting_sends_PATCH`（用 `bun test` mock fetch 驗 method/path/body）、`::patchMeeting_throws_with_error_code_on_422`。

## 4. Frontend：表單元件與 detail 頁整合

- [x] 4.1 [AC-5] 新增 `packages/web/src/components/meeting-edit-form.tsx`：react-hook-form + zod、5 個 controlled inputs、Save / Cancel 按鈕、submit 呼叫 `patchMeeting` 並 invalidate `["meetings"]` + `["meeting", id]` cache、422 → `localizedErrorMessage`，落實設計決策「前端 inline edit form：react-hook-form + zod，獨立 component」與 ADDED spec「meeting detail page lets the user inline-edit title, scheduled times, and display names」。**Verify**: 新測 `packages/web/src/components/meeting-edit-form.test.tsx` 涵蓋 3 個 scenario：(a) save 改 title → POST 出 PATCH + 顯示 "Saved" + cache 被 invalidate；(b) end < start → 提交前擋下、顯示 `errors.meeting.invalidTimeRange`、不送 PATCH；(c) server 422 invalid_field → form 保留 draft、顯示 localized error、Save 重新 enabled。
- [x] 4.2 [AC-5] 在 `packages/web/src/routes/meetings/detail.tsx` 加 Edit toggle，conditional 渲染 `<MeetingEditForm meeting={...} onSaved={...} />`，沿用 `meeting-detail-layout` 既有 toolbar 容器（per Open Question 解法）。**Verify**: `packages/web/src/routes/meetings/detail.test.tsx` 新增 `::test_edit_toggle_renders_form`（點 toolbar Edit 按鈕後 form 出現、初始值=當前 meeting）。
- [x] 4.3 [AC-4] 在 `packages/web/src/routes/meetings/new.tsx` 把 scheduled time inputs 改必填、用 zod schema 擋空值與 end-before-start，落實 issue acceptance criterion「新建會議表單把 scheduled time 變必填」與 modified spec「Meeting carries optional scheduled start and end timestamps」的 NOT NULL 約束。**Verify**: 既有 `packages/web/src/routes/meetings/new.test.tsx` 擴 `::test_new_meeting_requires_scheduled_start`、`::test_new_meeting_rejects_end_before_start`。

## 5. 移除 ?? created_at fallback

- [x] 5.1 [P] [AC-6] 在 `packages/web/src/components/meeting-card.tsx` 移除 `meeting.scheduled_start_at ? _formatTime(meeting.scheduled_start_at, ...) : _formatTime(meeting.created_at, ...)` 三元，改成單行 `_formatTime(meeting.scheduled_start_at, i18n.language)`，落實設計決策「移除 `?? created_at` fallback：三處同步改」與 ADDED spec「Meeting list, kanban, and calendar views render scheduled_start_at without created_at fallback」的 MeetingCard scenario。**Verify**: 新測 / 擴測 `packages/web/src/components/meeting-card.test.tsx::test_card_renders_scheduled_start_not_created_at`（給 `scheduled_start_at = 2026-06-15T14:00:00Z` + `created_at = 2026-05-01T00:00:00Z`，畫面文字含 06/15 不含 05/01）。
- [x] 5.2 [P] [AC-6] 在 `packages/web/src/components/meetings-kanban.tsx` 把 sort key `a.scheduled_start_at ?? a.created_at` 改成直接用 `a.scheduled_start_at`；同檔內 mock fixture / TS 型別把 `scheduled_start_at: null` 條目移除（因為 NOT NULL 後不可能）。**Verify**: `packages/web/src/components/meetings-kanban.test.tsx` 既有測試調整為「全部 fixture 都有 scheduled_start_at」並通過；新增 `::test_kanban_sorts_by_scheduled_start_at_only`（給兩個 meeting，A.scheduled_start_at > B.scheduled_start_at 但 A.created_at < B.created_at，驗 A 排在 B 後）。
- [x] 5.3 [P] [AC-6] 在 `packages/web/src/lib/meetings-calendar-utils.ts` 收緊 `toCalendarEvent`：移除 `if (!meeting.scheduled_start_at) return null` short-circuit（仍可保留 type-narrow check 但 runtime 不會命中）；對應 TS 型別 `Meeting.scheduled_start_at` 從 `string | null` 收緊為 `string`（在 `packages/web/src/lib/meetings-api.ts` 同步改）。**Verify**: 既有 `meetings-calendar-utils` 測試 0 regression；新測 `::test_toCalendarEvent_returns_event_for_any_meeting`（任何 meeting fixture 都產 calendar event）。

## 6. 收尾驗收

- [x] 6.1 跑全套後端覆蓋率 `cd packages/backend && uv run pytest --cov=meeting_playbook.meetings --cov-report=term-missing` 確認 `meetings/*` 模組整體 ≥80%；跑 `bun run test` 確認 web + auth + backend 全綠。**Verify**: TOTAL 83% (達標)；`repository.py` 100%、`schemas.py` 97%、`models.py` 100%、`dependencies.py` 90%。`router.py` 56% — 同 S14 已記錄的 pytest-cov + Python 3.13 sys.monitoring 對 TestClient worker thread 的 known limitation（handlers 確實被 14 個 endpoint test 跑過，但 tracker 不算）；`bun run test` 全綠（auth 44 / web 396 / backend 509）。
- [x] 6.2 Verify all acceptance criteria from the agent brief are checked off in the GitHub Issue #18（PATCH 多欄位 / migration up+down / 新建表單必填 / detail inline edit / 三視圖 fallback 移除 / 整合測試 / i18n 雙 locale 共 8 條 AC）。

## 7. Kanban 三欄分桶重新設計（post-ingest 2026-05-15）— 對應 MODIFIED spec「/meetings SHALL render meetings as a 3-column date-bucketed Kanban」與設計決策「Kanban 三欄分桶規則：從「時間窗口」改成「狀態 + 是否過期」」

- [x] 7.1 [P] [AC-Kanban] 重寫 `packages/web/src/lib/meetings-bucket.ts` 實作 MODIFIED Requirement「/meetings SHALL render meetings as a 3-column date-bucketed Kanban」：把 `MeetingDateBucket` 從 `"upcoming" | "future" | "past"` 改為 `"needs_recording" | "upcoming" | "completed"`；移除 `SEVEN_DAYS_MS` 與 `_startOfTodayLocal`；新規則 `status === "in_progress" || status === "completed"` → `"completed"`；`status === "scheduled" && scheduled_start_at >= now` → `"upcoming"`；`status === "scheduled" && scheduled_start_at < now` → `"needs_recording"`；無效 ISO 字串 fallback 從 `"past"` 改 `"needs_recording"`（語意：「不知道何時該發生 → 需要人為處理」）。落實設計決策「Kanban 三欄分桶規則：從「時間窗口」改成「狀態 + 是否過期」」。**Verify**: TypeScript build 通過（`bun --filter @meeting-playbook/web tsc --noEmit`）；helper 簽章變更後不破壞其它檔案的 import（grep `getMeetingDateBucket` 命中處皆編譯通過）。
- [x] 7.2 [P] 重寫 `packages/web/src/lib/meetings-bucket.test.ts` 涵蓋 5 個新 spec scenario：(a) `status="in_progress" + past scheduled` → `"completed"`；(b) `status="completed" + past scheduled` → `"completed"`；(c) `status="scheduled" + scheduled >= now` → `"upcoming"`；(d) `status="scheduled" + scheduled < now` → `"needs_recording"`；(e) 邊界 `scheduled == now` → `"upcoming"`（half-open）；(f) 無效 ISO 字串 → `"needs_recording"` + dev `console.warn`。移除既有舊 scenario（a-e + boundary `now + 7d` + completed-null）並重寫描述。**Verify**: `bun --filter @meeting-playbook/web test packages/web/src/lib/meetings-bucket.test.ts` 全綠。
- [x] 7.3 更新 `packages/web/src/components/meetings-kanban.tsx`：把 `BUCKET_ORDER` 改為 `["needs_recording", "upcoming", "completed"]`；`BUCKET_LABEL_KEY` 改為 `{needs_recording: "meetings.kanban.bucketNeedsRecording", upcoming: "meetings.kanban.bucketUpcoming", completed: "meetings.kanban.bucketCompleted"}`；`_sortPastDesc` rename → `_sortCompletedDesc`，套用對象從 `buckets.past` 改 `buckets.completed`；過去欄位的「past collapse / past show all」i18n key 鏈（`meetings.kanban.pastCollapse` / `pastShowAll`）保留鍵名不變但語意改套用在 `completed` 欄（既有 PAST_COLLAPSED_LIMIT 邏輯沿用）。**Verify**: `bun --filter @meeting-playbook/web test packages/web/src/components/meetings-kanban.test.tsx` 全綠；視覺手測 `/meetings` 三欄標頭從左到右是「待補錄 / 未來 / 已結束」。
- [x] 7.4 [P] 同步更新 `packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json`：移除 `meetings.kanban.bucketUpcoming` 舊值（會被新值覆寫，但 key 重名所以是 in-place 改值）/ `meetings.kanban.bucketFuture` / `meetings.kanban.bucketPast` 三個 key；新增 `meetings.kanban.bucketNeedsRecording`（zh-TW = `"待補錄"`, en = `"Needs recording"`）、`meetings.kanban.bucketUpcoming`（zh-TW = `"未來"`, en = `"Upcoming"`）、`meetings.kanban.bucketCompleted`（zh-TW = `"已結束"`, en = `"Completed"`）。**Verify**: `bun --filter @meeting-playbook/web test packages/web/src/locales/locales.test.ts` deep-equal 通過；`grep -F 'bucketNeedsRecording' packages/web/src/locales/{en,zh-TW}.json` 兩處皆命中；`grep -F 'bucketFuture' packages/web/src/locales/` 0 命中（舊 key 已掃淨）；`grep -F 'bucketPast' packages/web/src/locales/` 0 命中。
- [x] 7.5 更新 `packages/web/src/components/meetings-kanban.test.tsx` 中既有測試 fixture 與 assertion：把任何引用 `bucketUpcoming/Future/Past` 既有 label 的處改用新 key；測試 `_sortPastDesc` rename 為 `_sortCompletedDesc` 的處同步改；既有「past column collapse / show all (N)」測試保留但 assertion 對象改成 `kanban-column-completed`。**Verify**: `bun --filter @meeting-playbook/web test packages/web/src/components/meetings-kanban.test.tsx` 全綠；包含既有 `kanban-past-toggle` 行為的測試（如有）改 assert `kanban-completed-toggle` 或同等 testid。
- [x] 7.6 跨檔最終 verify：`bun run test` 確認 web + auth + backend 全綠（特別是 6.1 之後新加的 bucket 邏輯不破其它測試）；手動跑 `bun run dev`，造 3 筆 meeting fixture（一筆 `status=scheduled scheduled_start_at=now-1d`、一筆 `status=scheduled scheduled_start_at=now+1d`、一筆 `status=completed`），重整 `/meetings` Kanban 看三筆各落到「待補錄 / 未來 / 已結束」對應欄。**Verify**: `bun run test` exit 0；手測 screenshot 三欄分布正確；切換 zh-TW / en 語系 column 標頭都對。

## 8. Edit Dialog + 上傳音檔常駐入口 + Kanban 「待補錄」shortcut（post-ingest 2026-05-15）— 對應 ADDED specs「meeting detail edit form opens as a centered modal dialog」/「meeting detail actions bar always renders an upload-audio button」/「needs_recording kanban cards offer a hover-only upload shortcut and a custom empty CTA」與設計決策「Edit form 改 Dialog 彈窗（取代 inline conditional render）」/「Detail 頁 actions bar 內「上傳音檔」常駐顯示（取代 UploadBanner 條件顯示）」/「Kanban 「待補錄」欄 card hover-only 上傳 shortcut + 專屬空欄 CTA」

- [x] 8.1 [AC-Dialog] 改寫 `packages/web/src/routes/meetings/detail.tsx` 把既有 `{meeting && editFormOpen && <MeetingEditForm ... />}` 改成 `<Dialog open={editFormOpen} onOpenChange={setEditFormOpen}><DialogContent><DialogHeader><DialogTitle>{t("meetings.edit.dialogTitle")}</DialogTitle><DialogDescription>{t("meetings.edit.dialogDescription")}</DialogDescription></DialogHeader><MeetingEditForm meeting={meeting} onSaved={() => setEditFormOpen(false)} onCancel={() => setEditFormOpen(false)} /></DialogContent></Dialog>`；同時調整 `packages/web/src/components/meeting-edit-form.tsx` 移除最外層 Card 包裹（Dialog 已提供 surface），form 本體結構（5 inputs + Save/Cancel buttons + saved confirmation）保持不變。落實設計決策「Edit form 改 Dialog 彈窗」與 ADDED spec「meeting detail edit form opens as a centered modal dialog」的「Clicking Edit opens the dialog and rendering the form」+「Pressing Escape closes the dialog」scenarios。**Verify**: 更新 `packages/web/src/components/meeting-edit-form.test.tsx` 3 個既有 scenario（save title / end<start 擋 / server 422）：mount 時用 `<Dialog open={true}><DialogContent><MeetingEditForm .../></DialogContent></Dialog>` 包覆；既有 `packages/web/src/routes/meetings/detail.test.tsx::test_edit_toggle_renders_form` 改 assert：點 toggle 後 `screen.findByRole("dialog")` 命中、dialog 內含 form fields；新增 `::test_edit_dialog_closes_on_escape`（dialog 開啟後按 Esc → dialog DOM 移除）。
- [x] 8.2 [AC-UploadAlways] 改寫 `packages/web/src/components/metadata-card.tsx` 新增 `uploadSlot?: React.ReactNode` prop 並渲染在既有 button row 內（位置：在 start/end button 與 rerunSlot 之間，加 `data-testid="metadata-upload-slot"`）；同步改寫 `packages/web/src/routes/meetings/detail.tsx` 把 `<UploadBanner meeting={...} onUploadClick={...} />` 移除、改傳 `uploadSlot={<Button data-testid="metadata-upload-audio" variant={uploadVariant} disabled={uploadDisabled} onClick={() => setOfflineIngestOpen(true)}><Upload className="size-3.5" />{t("meetings.session.uploadAudio")}</Button>}`，其中 `uploadVariant` 由 `meeting.status` 計算：scheduled → `"outline"`、in_progress → `"outline"`、completed → `"secondary"`；`uploadDisabled` 由 `meeting.status === "in_progress"` 計算。`UploadDialog.tsx` 行為不變。落實設計決策「Detail 頁 actions bar 內「上傳音檔」常駐顯示」與 ADDED spec「meeting detail actions bar always renders an upload-audio button」的 3 個狀態 scenarios。**Verify**: 新增 `packages/web/src/routes/meetings/detail.test.tsx::test_upload_button_outline_for_scheduled`（mount scheduled meeting → 抓 `metadata-upload-audio`，className 含 `outline`、`disabled` false）、`::test_upload_button_disabled_for_in_progress`（in_progress → disabled true）、`::test_upload_button_secondary_for_completed`（completed → className 含 `secondary`、disabled false）；既有 `metadata-card.test.tsx` 既有測試不退化。
- [x] 8.3 [AC-UploadAlways] 刪除 `packages/web/src/components/offline-ingest/UploadBanner.tsx` 與 `packages/web/src/components/offline-ingest/UploadBanner.test.tsx`；確認 `shouldShowOfflineIngestBanner` 函式無其它檔案 import（`grep -rn 'shouldShowOfflineIngestBanner\|UploadBanner' packages/web/src --include='*.ts' --include='*.tsx'` 0 命中除了 detail.tsx 已被 8.2 改掉的 import）；同步從 `packages/web/src/locales/zh-TW.json` + `en.json` 移除 `offline_ingest.banner` 整個 sub-object（heading / subhead / cta 3 keys）。落實 ADDED spec「meeting detail actions bar always renders an upload-audio button」最後一段「The legacy `<UploadBanner>` component ... SHALL be removed」。**Verify**: `grep -rn 'UploadBanner\|shouldShowOfflineIngestBanner' packages/web/src` 0 命中；`grep -rn 'offline_ingest.banner' packages/web/src` 0 命中；`bun --filter @meeting-playbook/web test packages/web/src/locales/locales.test.ts` deep-equal 通過。
- [x] 8.4 [AC-NeedsRecordingShortcut] 實作 ADDED Requirement「needs_recording kanban cards offer a hover-only upload shortcut and a custom empty CTA」：改寫 `packages/web/src/components/meeting-card.tsx` 加 `showUploadShortcut?: boolean` prop（default `false`），當 `true` 時 card root className 加 `group`、在 card 右下角 conditional render `<button data-testid="meeting-card-upload-shortcut" className="opacity-0 group-hover:opacity-100 transition-opacity ..." onClick={(e) => { e.stopPropagation(); e.preventDefault(); navigate({ to: "/meetings/$id", params: { id: meeting.id }, search: { action: "upload" } }); }}>{t("meetings.kanban.uploadShortcut")}</button>`；同步改 `packages/web/src/components/meetings-kanban.tsx` 在 render `visible.map((m) => <MeetingCard ... />)` 處傳 `showUploadShortcut={bucket === "needs_recording"}`。落實設計決策「Kanban 「待補錄」欄 card hover-only 上傳 shortcut」與 ADDED spec「needs_recording kanban cards offer a hover-only upload shortcut...」的「needs_recording card mounts the hover shortcut」+「Clicking the shortcut navigates with action=upload」scenarios。**Verify**: 新增 `packages/web/src/components/meeting-card.test.tsx::test_card_renders_upload_shortcut_when_prop_true`（render with `showUploadShortcut={true}` → `meeting-card-upload-shortcut` 存在且 className 含 `opacity-0` + `group-hover:opacity-100`）；`::test_card_hides_upload_shortcut_when_prop_false_or_omitted`（無 prop → testid 不存在於 DOM）；`::test_shortcut_click_stops_propagation_and_navigates`（點下 shortcut → router navigate spy 收到 `{to: "/meetings/$id", params: {id: meeting.id}, search: {action: "upload"}}`、card 主 link spy 未觸發）。
- [x] 8.5 [AC-NeedsRecordingShortcut] 改寫 `packages/web/src/routes/meetings/detail.tsx` mount effect：讀 `useSearch({ strict: false }).action`，若 `=== "upload"` 則 `setOfflineIngestOpen(true)` 並 `navigate({ search: { action: undefined }, replace: true })`；改寫 `packages/web/src/components/meetings-kanban.tsx` empty hint 邏輯：當 `bucket === "needs_recording"` && `items.length === 0` 時 render `t("meetings.kanban.bucketNeedsRecordingEmpty")` 取代 `t("meetings.kanban.bucketEmpty")`，其他兩欄仍用 `bucketEmpty`。落實 ADDED spec 的「Detail page reads action=upload and auto-opens the dialog」+「needs_recording empty column shows the custom CTA」scenarios。**Verify**: 新增 `packages/web/src/routes/meetings/detail.test.tsx::test_action_upload_query_auto_opens_dialog`（mount with `initialEntries=["/meetings/m_x?action=upload"]` → `upload-dialog` 自動 open + URL 被 scrubbed）；`packages/web/src/components/meetings-kanban.test.tsx::test_needs_recording_empty_shows_custom_cta`（meetings=[] → `kanban-empty-needs_recording` textContent 包含 zh-TW 「目前沒有待補錄的會議」）+ `::test_needs_recording_cards_carry_upload_shortcut`（造一筆 needs_recording meeting → `kanban-body-needs_recording` 內的 card 含 `meeting-card-upload-shortcut`）。
- [x] 8.6 [P] [AC-Dialog][AC-UploadAlways][AC-NeedsRecordingShortcut] 同步雙 locale 新增 5 個 key + 移除 3 個 key：`packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json` 新增 `meetings.edit.dialogTitle`（zh「編輯會議」/ en「Edit meeting」）、`meetings.edit.dialogDescription`（zh「修改 meeting 的標題、排定時間與顯示名稱」/ en「Update title, schedule, and display names」）、`meetings.session.uploadAudio`（zh「上傳音檔」/ en「Upload audio」）、`meetings.kanban.uploadShortcut`（zh「上傳音檔」/ en「Upload audio」）、`meetings.kanban.bucketNeedsRecordingEmpty`（zh「目前沒有待補錄的會議」/ en「No meetings to follow up on」）；移除 `offline_ingest.banner` 整個 sub-object（heading / subhead / cta 3 keys，已在 8.3 處理但雙 locale 同步是這條 task 的最終 contract）。**Verify**: `bun --filter @meeting-playbook/web test packages/web/src/locales/locales.test.ts` deep-equal 通過；`grep -F 'dialogTitle' packages/web/src/locales/{en,zh-TW}.json` 兩處皆命中；`grep -F 'uploadShortcut' packages/web/src/locales/{en,zh-TW}.json` 兩處皆命中；`grep -F 'bucketNeedsRecordingEmpty' packages/web/src/locales/{en,zh-TW}.json` 兩處皆命中。
- [x] 8.7 跨檔最終 verify：`bun run test` 確認 web + auth + backend 全綠（特別是 Dialog refactor 後 detail.test.tsx / meeting-edit-form.test.tsx 都不退化）；手動跑 `bun run dev`，在 `/meetings/<id>` 同時驗證：(a) 點「編輯」→ 彈窗中央出現、ESC 關閉；(b) MetadataCard button row 看到「上傳音檔」按鈕，scheduled 為 outline、in_progress 為 disabled、completed 為 secondary；(c) `/meetings` 在「待補錄」欄 hover card 看到上傳 shortcut；(d) 「待補錄」欄空欄訊息對；(e) 在 needs_recording card 點 shortcut → 跳到 detail 頁且 upload dialog 自動打開。**Verify**: `bun run test` exit 0；五個手測 path 各自通過；切 zh-TW / en 語系所有新增字串都對。
