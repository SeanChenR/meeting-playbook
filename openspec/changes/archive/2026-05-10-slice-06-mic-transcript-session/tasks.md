## 1. Backend — Alembic migration + status transition

- [x] 1.1 [AC: Requirement "Meeting status transitions are atomic and only allowed in the forward direction"] Action for "Status transitions on `meeting`" (RED): add `packages/backend/tests/meetings/test_transition_status.py` with four cases — (a) scheduled → in_progress succeeds and updates `updated_at`, (b) in_progress → completed succeeds, (c) completed → completed raises `MeetingStatusConflict`, (d) scheduled → completed (skipping) raises `MeetingStatusConflict`.
- [x] 1.2 Implement `MeetingRepository.transition_status(meeting_id, expected_from, target)` in `packages/backend/meeting_playbook/meetings/repository.py`. Use atomic `UPDATE ... WHERE status = :expected_from RETURNING id`; raise `MeetingStatusConflict` (new exception in the same module) when the RETURNING row is empty. Allowed transitions enumerated as a constant. 1.1 turns green.
- [x] 1.3 [AC: Requirement "Audio capture writes one WAV file per session under RECORDINGS_DIR" + "Transcript chunks are persisted in chronological order"] Action for the new schema (RED): add `packages/backend/tests/test_alembic_session_tables.py` asserting that after `alembic upgrade head` the tables `transcript_chunk` and `recording` exist with the columns from the design — `transcript_chunk(id, meeting_id, speaker, text, started_at, ended_at, asr_provider_used, confidence)` and `recording(id, meeting_id, stream, file_path, bytes, created_at)` — both with FK to `meeting.id` ON DELETE CASCADE.
- [x] 1.4 Add migration `packages/backend/alembic/versions/0003_create_session_tables.py` that creates both tables with the schema above. `transcript_chunk.speaker` is `text NOT NULL` with a `CHECK (speaker IN ('me', 'counterparty', 'system'))` constraint; `recording.stream` is `text NOT NULL` with a `CHECK (stream IN ('me', 'counterparty'))` constraint. Both `started_at` / `ended_at` are `timestamp with time zone`. 1.3 turns green.
- [x] 1.5 Update `packages/backend/tests/conftest.py` to TRUNCATE the two new tables before each test (insert order: transcript_chunk → recording → playbook → meeting → user, all CASCADE).

## 2. Backend — ASRProvider interface + WhisperProvider

- [x] 2.1 [AC: Requirement "ASRProvider interface enables multiple ASR engines (per ADR-0005)"] Action for "ASRProvider interface (per ADR-0005, formalized here)" (RED): add `packages/backend/tests/asr/test_base.py` asserting (a) the `TranscriptChunk` dataclass exists with frozen=True and the fields `text, started_at, ended_at, asr_provider_used, confidence`, (b) the `ASRProvider` Protocol exists and declares `name` (str property) plus `async transcribe_chunk(audio_bytes, sample_rate_hz, language_hint=None)`.
- [x] 2.2 Add `packages/backend/meeting_playbook/asr/{__init__,base}.py` with the `TranscriptChunk` dataclass (frozen) and the `ASRProvider` Protocol per the design contract. 2.1 turns green.
- [x] 2.3 [AC: Requirement "ASRProvider interface enables multiple ASR engines (per ADR-0005)"] Action for "WhisperProvider lazy model load" (RED, contract test): add `packages/backend/tests/asr/test_whisper_provider.py` with a single integration case that calls `WhisperProvider().transcribe_chunk(audio_bytes, sample_rate_hz=16000, language_hint="zh")` against `tests/asr/fixtures/short_speech_zh.wav` (a ~3-second Chinese utterance Sean records and commits) and asserts: `result.text` is non-empty after strip, `result.asr_provider_used == "whisper"`, `result.ended_at > result.started_at`, `0 <= result.confidence <= 1` when not None. Test uses module-scope fixture so the model loads once per test session.
- [x] 2.4 Add `packages/backend/meeting_playbook/asr/whisper_provider.py` implementing `ASRProvider` with `faster_whisper.WhisperModel`. Constructor reads model size / device / compute type from Settings (lazy: model NOT loaded in `__init__`). First `transcribe_chunk` call instantiates `WhisperModel` and stores it on the instance; subsequent calls reuse. 2.3 turns green. Add a unit test verifying that two consecutive calls reuse the same `WhisperModel` instance (via `weakref.ref` or `id()`).

## 3. Backend — AudioCaptureService

- [x] 3.1 [AC: Requirement "Audio capture writes one WAV file per session under RECORDINGS_DIR" + Requirement "Silence in the audio stream is reported within 30 seconds"] Action for "AudioCaptureService shape" (RED, capture protocol): add `packages/backend/tests/audio/test_capture_protocol.py` covering the contract WITHOUT touching real audio — pass a stub `_input_stream_factory` that yields pre-recorded numpy arrays; assert (a) `AudioCaptureService` async-context-manager opens and closes cleanly, (b) `events()` async iterator yields `AudioChunk` objects every `chunk_seconds` of accumulated audio, (c) sustained silence (RMS below threshold) for `silence_warning_after` seconds yields exactly one `SilenceWarning`, (d) on exit a WAV file at `{recordings_dir}/{meeting_id}/me.wav` exists and contains the entire captured stream.
- [x] 3.2 Add `packages/backend/meeting_playbook/audio/{__init__,capture}.py` implementing `AudioCaptureService` per design — async context manager, `events()` async iterator over `AudioChunk | SilenceWarning`, sliding-window RMS for silence detection, atomic WAV write on exit (`.tmp` then rename). The `_input_stream_factory` parameter defaults to a real `sounddevice.RawInputStream` in production but tests override it. 3.1 turns green.
- [x] 3.3 [AC: Requirement "Audio capture writes one WAV file per session under RECORDINGS_DIR"] Action for "AudioCaptureService shape" (RED, real-device integration): add `packages/backend/tests/audio/test_capture_integration.py` skipif `sounddevice.query_devices(kind='input')` raises or returns nothing — opens `AudioCaptureService` against the real default input device for 2 seconds, asserts at least one `AudioChunk` is yielded and the WAV file is created on disk. Test is `@pytest.mark.audio_device` and skipped on CI.

## 4. Backend — Sessions repository + service

- [x] 4.1 [AC: Requirement "Transcript chunks are persisted in chronological order" + "Audio capture writes one WAV file per session"] Action for "Sessions repository" (RED): add `packages/backend/tests/sessions/test_repository.py` covering `SessionRepository.insert_chunk(meeting_id, speaker, text, started_at, ended_at, asr_provider_used, confidence)` and `SessionRepository.insert_recording(meeting_id, stream, file_path, bytes_size)`. Assertions: insert returns the persisted ORM object with id; multiple chunks for the same meeting query back in `started_at` ascending order; CHECK constraint rejects `speaker = "stranger"`.
- [x] 4.2 Add `packages/backend/meeting_playbook/sessions/{__init__,models,repository}.py` with the SQLAlchemy models and the `SessionRepository` class. 4.1 turns green.
- [x] 4.3 [AC: Requirement "Transcript chunks are persisted in chronological order" + Requirement "Silence in the audio stream is reported within 30 seconds"] Action for the session orchestrator (RED): add `packages/backend/tests/sessions/test_service.py` exercising the orchestration loop with mock ASRProvider (returns deterministic `TranscriptChunk` for any input bytes) + mock AudioCaptureService (yields a scripted sequence of `AudioChunk` and `SilenceWarning`). Assertions: for each AudioChunk → ASR called once → SessionRepository.insert_chunk called once → outbound WS frame sent in that order; SilenceWarning forwarded as one outbound frame; multiple consecutive SilenceWarnings within a window are deduplicated to one outbound frame; if SessionRepository.insert_chunk raises, the service emits `error` frame with code `session.persist_failed` and exits.
- [x] 4.4 Add `packages/backend/meeting_playbook/sessions/service.py` — pure orchestration, takes injected `ASRProvider`, `AudioCaptureService`, `SessionRepository`, and an outbound `send(frame)` callable. Implements the loop described in the design's "WebSocket endpoint design" steps 6–10. 4.3 turns green.

## 5. Backend — WebSocket router + Pydantic message contract

- [x] 5.1 [AC: Requirement "WebSocket message contract for the meeting session"] Action for "WS message contract — Pydantic models" (RED): add `packages/backend/tests/sessions/test_messages.py` parametrized across the spec's "minimum required keys per server-side message type" example matrix — for each row, build the Pydantic model and assert (a) `model_dump_json()` round-trips back through the discriminated union into the same model, (b) extra unknown keys are rejected (strict mode), (c) the `type` discriminator field matches the row.
- [x] 5.2 Add `packages/backend/meeting_playbook/sessions/messages.py` with Pydantic v2 discriminated-union models for client→server and server→client message types per the spec contract. Use `Field(discriminator="type")` for the union. 5.1 turns green.
- [x] 5.3 [AC: Requirement "WebSocket session endpoint at /api/meetings/{id}/session is gated by meeting ownership" + Requirement "WebSocket message contract for the meeting session"] Action for "WebSocket endpoint design" (RED): add `packages/backend/tests/sessions/test_router.py` using FastAPI's `TestClient.websocket_connect`. Cases: (a) authenticated owner connects, sends `start_meeting` with matching id → receives `meeting_started`; sends `end_meeting` → receives `meeting_ended`; (b) non-owner gets HTTP 404 + `meeting.not_found` BEFORE upgrade; (c) unauthenticated request gets HTTP 401 + `auth.gateway_bypass`; (d) start_meeting with mismatched id → `error` frame with `session.bad_start` then close; (e) start when meeting status is already `completed` → `error` frame with `session.bad_status`; (f) full sequence with mock ASR + mock capture: start → meeting_started → 2× transcript_chunk → end_meeting → meeting_ended; meeting status is `completed` after.
- [x] 5.4 Add `packages/backend/meeting_playbook/sessions/router.py` with the `GET /api/meetings/{id}/session` WebSocket endpoint. Mount in `packages/backend/meeting_playbook/server.py`. The router uses dependency injection for ASRProvider, AudioCaptureService factory, SessionRepository, MeetingRepository — tests override these via FastAPI dependency overrides. 5.3 turns green.

## 6. Backend — Configuration

- [x] 6.1 Extend `packages/backend/meeting_playbook/config.py` Settings with: `recordings_dir: str = "~/MeetingPlaybook/recordings"`, `whisper_model_size: str = "large-v3-turbo"`, `whisper_device: str = "auto"`, `whisper_compute_type: str = "auto"`. Add a unit test in `packages/backend/tests/test_config.py` (or extend existing) asserting these defaults are present and overridable via env vars.

## 7. Auth gateway — WebSocket upgrade contract

- [x] 7.1 [AC: Requirement "Gateway authenticates and proxies WebSocket upgrades with identity headers preserved"] Action for "Bun gateway WebSocket upgrade" (RED): add `packages/auth/src/__tests__/gateway-websocket.test.ts` with three cases — (a) authenticated WebSocket upgrade to `/api/meetings/m_abc/session`: forwarded request carries `X-User-Id`, `X-User-Name`, `X-User-Email` (URL-encoded); (b) unauthenticated upgrade returns HTTP 401 + `auth.gateway_bypass` and upstream is NOT contacted; (c) public path `/api/health` upgrade attempt is rejected (treated as no-upgrade pass-through OR explicit 400; assert one of those, document choice).
- [x] 7.2 Update `packages/auth/src/server.ts` if needed so the WebSocket-upgrade branch sets the same three identity headers as the regular branch. The `isWebSocketUpgrade` helper already exists from slice-01; this task formalizes that headers are set on upgrade requests just like non-upgrade. 7.1 turns green. If existing implementation already satisfies the spec, the impl change is "no-op"; mark the task done after verifying.

## 8. Frontend — WebSocket client + reducer hook

- [x] 8.1 [AC: Requirement "useMeetingSession hook owns the WebSocket lifecycle and reducer state"] Action for "WebSocket client wrapper" (RED): add `packages/web/src/lib/session-ws.test.ts` using a custom MockWebSocket class. Cases: (a) `openSessionSocket(meetingId)` returns a thin wrapper exposing `send`, `close`, and an `onMessage` setter; (b) inbound JSON parsed and dispatched to onMessage with typed payload; (c) malformed JSON triggers `onError` callback; (d) calling `send(message)` JSON-stringifies and writes to the underlying socket.
- [x] 8.2 Add `packages/web/src/lib/session-ws.ts` implementing the wrapper. 8.1 turns green.
- [x] 8.3 [AC: Requirement "useMeetingSession hook owns the WebSocket lifecycle and reducer state"] Action for "React `useMeetingSession` hook" (RED, state transitions): add `packages/web/src/hooks/use-meeting-session.test.tsx` using `renderHook` + MockWebSocket. Cases: (a) initial state is `{phase: "idle"}`; (b) `start()` opens WS and state becomes `{phase: "connecting"}`; (c) inbound `meeting_started` → `{phase: "in_progress", chunks: [], silenceSince: null}`; (d) inbound `transcript_chunk` appends to chunks (chronological); (e) inbound `silence_warning` sets `silenceSince`; (f) inbound `meeting_ended` → `{phase: "ended", chunks: [...]}`; (g) inbound `error` → `{phase: "error", errorCode, chunks: [...]}` AND wrapper's close() called; (h) unexpected close during `in_progress` triggers ONE retry attempt with 1s backoff (use fake timers).
- [x] 8.4 Add `packages/web/src/hooks/use-meeting-session.ts` implementing the reducer + ref-held WebSocket. 8.3 turns green.

## 9. Frontend — TranscriptPane + capture indicator + detail page wiring

- [x] 9.1 [AC: Requirement "TranscriptPane renders chunks with the meeting's me display name"] Action for "React `TranscriptPane` + capture indicator" (RED): add `packages/web/src/components/transcript-pane.test.tsx`. Cases: (a) empty chunks renders a placeholder element (per spec scenario); (b) three chunks render in order, each visible with `meDisplayName` as speaker label; (c) a chunk with `speaker = "counterparty"` renders with a different visual style (CSS class assertion is sufficient; slice-7 will exercise the real path).
- [x] 9.2 Add `packages/web/src/components/transcript-pane.tsx`. NO emojis (per UI feedback memory). 9.1 turns green.
- [x] 9.3 [P] Add `packages/web/src/components/capture-indicator.tsx` + test `capture-indicator.test.tsx`. Three states: `idle` (no indicator), `active` (pulsing dot + "擷取中" label), `silence_warning` (warning style + "未偵測到聲音" label). NO emojis.
- [x] 9.4 [AC: Requirement "useMeetingSession hook owns the WebSocket lifecycle and reducer state" + Requirement "TranscriptPane renders chunks with the meeting's me display name"] Action for "Detail page wiring" (RED): extend `packages/web/src/routes/meetings/detail.test.tsx` with three cases — (a) "Start Meeting" button click opens WS and shows the capture indicator (use the MockWebSocket strategy from session-ws); (b) when transcript_chunk messages arrive the TranscriptPane renders them; (c) "End Meeting" button click sends end_meeting and closes the WS, capture indicator returns to idle.
- [x] 9.5 Edit `packages/web/src/routes/meetings/detail.tsx` to: (i) read meeting from existing query, (ii) call `useMeetingSession(meetingId)`, (iii) add Start / End buttons disabled by phase, (iv) render `<CaptureIndicator>` based on phase + silenceSince, (v) render `<TranscriptPane chunks={state.chunks} meDisplayName={meeting.me_display_name}>` below the playbook pane. 9.4 turns green.

## 10. i18n + docs + deps

- [x] 10.1 [P] Add the `meeting.session.*` and `errors.session.*` namespaces to BOTH `packages/web/src/locales/zh-TW.json` and `packages/web/src/locales/en.json`. Required keys: `meeting.session.start` (Start Meeting / 開始會議), `meeting.session.end`, `meeting.session.capturing`, `meeting.session.silenceWarning`, `meeting.session.transcriptEmpty`, `errors.session.no_audio_device`, `errors.session.bad_start`, `errors.session.bad_status`, `errors.session.persist_failed`, `errors.session.unknown_message`. Update `packages/web/src/locales/locales.test.ts` with explicit assertions.
- [x] 10.2 [P] Add `docs/agents/sessions.md` describing: WS contract (with the example matrix), ASRProvider invariant ("session router MUST NOT import any concrete provider"), AudioCaptureService context-manager lifecycle, RECORDINGS_DIR + WAV file path convention, status transition rules (only `MeetingRepository.transition_status` may write the column).
- [x] 10.3 [P] Add `docs/agents/asr.md` describing the ASRProvider Protocol, the lazy-load pattern, the `language_hint` convention, and the "drop-in second provider" test (grep for non-Whisper imports in router source).
- [x] 10.4 Add a section to `docs/agents/calendar.md` (or a new `docs/agents/audio.md`) — pick one — describing the macOS microphone-permission UX: first run will prompt; if denied, the error surfaces as `session.no_audio_device` with a localized retry instruction in the UI.
- [x] 10.5 Update `pyproject.toml` to add `faster-whisper>=1.0`, `sounddevice>=0.4`, `numpy>=1.26`. Run `uv lock` and commit `uv.lock`.
- [x] 10.6 Update `.env.example` with `RECORDINGS_DIR`, `WHISPER_MODEL_SIZE`, `WHISPER_DEVICE`, `WHISPER_COMPUTE_TYPE`. Document the Whisper model first-download size (~1.5GB) in a comment.
- [x] 10.7 Verify all acceptance criteria from the agent brief are checked off in GitHub Issue #8.

## 11. Test strategy roll-up

- [x] 11.1 Action for "Test strategy (TDD vertical slice)" — run the full test sweep proving slice-6 satisfies the design's TDD discipline: `cd packages/backend && uv run pytest` (existing 137 + new alembic + asr base + asr whisper + audio capture protocol + audio capture integration (skipped on CI) + sessions repository + sessions service + sessions messages + sessions router + transition_status + config), `bun --filter @meeting-playbook/web test` (existing 127 + new session-ws + use-meeting-session + transcript-pane + capture-indicator + detail.test additions), `bun --filter @meeting-playbook/auth test` (existing 41 + new gateway-websocket). On each suite run, capture the number of new tests added in the commit message.
