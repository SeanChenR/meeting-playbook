## 1. Schema 與 config 落地

- [x] 1.1 [P] 新增兩個 Alembic migration 並寫 round-trip 測（注意：S14 已在 `recording` 表加過 nullable `started_at` column，本 slice 任務改為 backfill + SET NOT NULL；migration 編號從原 proposal 的 0009/0010 改為 **0014/0015**（S17 worktree 已預留 0013））：`packages/backend/alembic/versions/0014_recording_started_at.py`（up 兩步：`UPDATE recording SET started_at = created_at WHERE started_at IS NULL` backfill → `ALTER COLUMN started_at SET NOT NULL`；down 是 `ALTER COLUMN started_at DROP NOT NULL`；revision id `0014_recording_started_at` ≤ 32 chars）與 `packages/backend/alembic/versions/0015_chunk_text_edited_at.py`（up 加 `text_edited_at TIMESTAMPTZ NULL`；down 是 drop column；revision id `0015_chunk_text_edited_at`）。落實設計決策「Migration：`recording.started_at TIMESTAMPTZ NOT NULL` + backfill 為 `created_at`」與 `transcript_chunk gains text_edited_at column` requirement。**驗證**：新測 `packages/backend/tests/test_alembic_recording_started_at.py` 用 4 個 fixture row 跑 upgrade → 驗 `started_at == created_at` → downgrade → upgrade 三輪驗可逆；新測 `packages/backend/tests/test_alembic_transcript_chunk_text_edited_at.py` 跑 round-trip 並驗新插入 chunk 的 `text_edited_at IS NULL`。對應 spec scenarios「Migration backfills existing recording rows」、「Migration down step drops the column」、「Newly inserted ASR chunks have text_edited_at NULL」。
- [x] 1.2 [P] 在 `packages/backend/meeting_playbook/config.py` 加 `audio_range_max_bytes: int = 2_097_152` 欄位，並在 `.env.example` 加 `AUDIO_RANGE_MAX_BYTES` 註解；落實設計決策「env var：`AUDIO_RANGE_MAX_BYTES` default 2 MiB」與 `AUDIO_RANGE_MAX_BYTES configuration with 2 MiB default` requirement。**驗證**：新測 `packages/backend/tests/test_config.py::test_audio_range_max_bytes_*` 兩條 — 預設 `2_097_152`、env override 為 `1048576` 後生效，對應 spec 兩個 scenario。

## 2. WAV header 解析（純函式 TDD）

- [x] 2.1 在 `packages/backend/meeting_playbook/audio_playback/wav_header.py` 實作 `parse_wav_header(header_bytes: bytes) -> WavHeaderInfo` 純函式，落實設計決策「`AudioRangeServer` 為 deep module + 純函式 WAV header 解析」第 1 層與 `parse_wav_header reads the WAV header and surfaces format metadata` requirement。**行為**：scan RIFF chunks 找 `fmt ` / `data`、回 `(data_offset, sample_rate, channels, bits_per_sample, data_bytes)`、非 16kHz mono 16-bit PCM raise `UnsupportedWavFormat`、容忍 LIST 等中繼 chunk。**Red tests 先寫**（`packages/backend/tests/audio_playback/test_wav_header.py`）：(a) 標準 44-byte header → `data_offset = 44`；(b) header 含 36-byte LIST chunk → `data_offset > 44`；(c) 48kHz fixture → raise `UnsupportedWavFormat` 訊息含 `sample_rate`；(d) stereo fixture → raise `UnsupportedWavFormat` 訊息含 `channels`，對應 spec 四個 scenario。
- [x] 2.2 在同檔 `wav_header.py` 實作 `compute_byte_range(*, header, start_second, end_second) -> tuple[int, int]`，落實設計決策「`AudioRangeServer` 為 deep module + 純函式 WAV header 解析」內的 byte math 與 `compute_byte_range converts a second-aligned interval into a block-aligned byte range` requirement。**行為**：用 `bytes_per_second = sample_rate * channels * (bits_per_sample // 8)` 算 byte offset、block-align 至 `block_align = 2`、clamp 至 `[data_offset, data_offset + data_bytes - 1]`、`end < start` 或 interval 超出 data 範圍 raise `InvalidRange`。**Red tests 先寫**：(a) 10-second slice 從 5s 到 15s 對 40-second WAV 回 `(160_044, 480_043)`；(b) `end_second < start_second` raise；(c) interval 超出 audio data raise；(d) example table 中三個 boundary case 全部通過，對應 spec 三個 scenario + 一個 example block。

## 3. HTTP Range 解析 + AudioRangeServer integration

- [x] 3.1 在 `packages/backend/meeting_playbook/audio_playback/range_server.py` 一次實作兩支：純函式 `parse_range_header(value: str, total_length: int) -> tuple[int, int]`（支援 `bytes=A-B` / `bytes=A-` / `bytes=-N` 三型；clamp END 至 `total_length - 1`；其他 syntax / `START > END` / `START >= total_length` raise `MalformedRange`）與 deep module `AudioRangeServer.serve(*, file_path, recording_started_at, chunk_start, chunk_end, range_header, max_bytes) -> RangeResponse`（開檔讀 header bytes → `parse_wav_header` → 算 chunk_start / chunk_end 相對 `recording_started_at` 的 seconds → `compute_byte_range` → 套 `parse_range_header` → cap byte count at `max_bytes` → 回 `(status_code, headers, body iterator)`）。落實設計決策「`AudioRangeServer` 為 deep module + 純函式 WAV header 解析」第 2 層、`parse_range_header decodes an HTTP Range header into byte offsets` requirement、以及 router spec 的 byte-accurate scenarios。**Red tests 先寫**（`packages/backend/tests/audio_playback/test_range_server.py`）：parser 四條 — (a) `bytes=100-200` → `(100, 200)`；(b) `bytes=500-` 對 total 1000 → `(500, 999)`；(c) `bytes=-200` 對 total 1000 → `(800, 999)`；(d) `items=0-10` raise `MalformedRange`；serve 三條 — (e) 含 Range header 對 10-second slice 回 status 206 + body bytes 對應檔案 offset 160044-480043 inclusive；(f) 無 Range header 回 status 200 + full body；(g) max_bytes cap 啟用時 body length ≤ max_bytes，對應 spec 四 + 三 scenarios。

## 4. GET /audio endpoint（含 retention gate）+ sessions started_at

- [x] 4.1 在 `packages/backend/meeting_playbook/audio_playback/router.py` 實作 `GET /api/meetings/{meeting_id}/recordings/{recording_id}/audio` 並掛載至 `packages/backend/meeting_playbook/server.py`；同時更新 `packages/backend/meeting_playbook/sessions/router.py` 的 finalize 區段，把 audio capture 紀錄的 first-sample wall-clock 傳入 `recording_repo.create(...)` 的 `started_at` 欄位（落實 `Audio capture writes one WAV file per session under RECORDINGS_DIR` requirement 在 capture 紀錄第一個 sample wall-clock 並寫入 `recording.started_at` 的副作用）。落實設計決策「`GET /api/meetings/{id}/recordings/{recording_id}/audio` 支援 `Range`，無 `Range` 回 200 全檔」、`GET /api/meetings/{id}/recordings/{recording_id}/audio serves Range-aware audio streaming` requirement，以及 `recording-retention` 的 `GET /api/meetings/{id}/recordings/{recording_id}/audio returns 410 Gone when the recording is retention-expired` requirement。**Endpoint 行為**：load recording → 驗 meeting_id 與 X-User-Id 對應 owner → 若 `deleted_at IS NOT NULL` 回 410 `audio_playback.expired` 且不串 byte；否則呼叫 `AudioRangeServer.serve`。失敗模式：404 `recording_not_found` / 403 `forbidden` / 410 `expired` / 416 `range_not_satisfiable` / 422 `malformed_range`。**Red tests 先寫**（`packages/backend/tests/audio_playback/test_router.py`）：七條 — (a) 含 Range 對 40s 檔回 206 + Content-Range / Content-Length 對應；(b) expired recording + Range → 410 `audio_playback.expired` 且 body 不含 WAV bytes；(c) 無 Range → 200 + 全檔；(d) Range 超出 max_bytes → 206 但 Content-Length capped at `2_097_152`；(e) recording 不屬於 path meeting → 404；(f) 非 owner 走 X-User-Id → 403；(g) Range 超出檔案 → 416。同時擴 `packages/backend/tests/sessions/test_router.py` 既有 finalize fixture 驗 finalize 後兩個 stream 的 `started_at` 與該 stream `first_sample_ts` 一致；既有 dual-channel router test 0 regression。對應 `audio-playback` spec 七 scenarios + `recording-retention` 兩 scenarios + `meeting-session` 兩 scenarios。

## 5. PATCH /transcript_chunks endpoint

- [x] 5.1 在 `packages/backend/meeting_playbook/transcript_edit/router.py` 實作 `PATCH /api/meetings/{meeting_id}/transcript_chunks/{chunk_id}`、並掛 server.py 路由，落實設計決策「`PATCH /api/meetings/{id}/transcript_chunks/{chunk_id}` 只允許 `text`」、`PATCH /api/meetings/{id}/transcript_chunks/{chunk_id} updates only the text field` requirement 與 `transcript_chunk gains text_edited_at column tracking user edits` 中「Successful PATCH stamps text_edited_at」scenario。**行為**：body 只接 `text`（`1 <= len <= 10000`），任何其他欄位（speaker / 時間戳 / asr_provider_used / confidence）出現 → 422 `transcript_edit.immutable_field` 並列出被拒欄位名；驗 owner、驗 chunk.meeting_id 對 path；成功時 `text_edited_at = now()`、回 200 + `{id, text, text_edited_at}`。**Red tests 先寫**（`packages/backend/tests/transcript_edit/test_router.py`）：六條 — (a) valid text → 200 + DB row `text_edited_at` 非 NULL；(b) body 帶 `speaker` → 422 `immutable_field` 列 `speaker`；(c) 空 text → 422 `invalid_text`；(d) 10001 字元 → 422 `invalid_text`；(e) 非 owner → 403 `forbidden`；(f) chunk 屬於別的 meeting → 404 `chunk_not_found`，對應 spec 六個 scenario。

## 6. Cluster color customisation（pure 模組 → hook → popover → 整合）

- [x] 6.1 [P] 落實設計決策「Cluster color customisation：預定 5 scheme + per-cluster override + `localStorage` 持久化」：新增 `packages/web/src/lib/transcript-color-schemes.ts`，暴露 `TRANSCRIPT_COLOR_SCHEME_IDS = ["default", "vivid", "pastel", "high-contrast", "grayscale"] as const` 與 `TRANSCRIPT_COLOR_SCHEMES` map（`default.hues = [300, 150, 30, 240, 90, 0]` 維持與既有 `transcript-pane._CLUSTER_HUES` 完全一致；`vivid` / `pastel` / `high-contrast` 各 6-entry hue array + `accentL/accentC/bgL/bgC` 數字；`grayscale` 提供 `lightness` 6-entry ladder）、純函式 `_resolveClusterColor(speaker, pref) -> { background, accent }`（依設計 design.md 的精確 precedence：override > scheme palette > `me`/`counterparty` semantic > unknown muted > fallback；`me` / `counterparty` 一律忽略 pref）、純函式 `parseTranscriptColorPref(raw: string | null) -> TranscriptColorPref`（fail-soft 回 `{ scheme: "default", overrides: {} }`）。落實 `TranscriptColorScheme palettes and per-cluster overrides drive cluster colour rendering` requirement。**Red tests 先寫**（`packages/web/src/lib/transcript-color-schemes.test.ts`）：八條 — (a) `default` scheme 對 cluster 1 / 2 回 hue 300 / 150 accent；(b) override 蓋過 scheme（`pref.overrides[3] = "oklch(0.65 0.20 200)"`，`vivid` 下仍回該 override）；(c) `grayscale` cluster 2 回 `oklch(<L> 0 0)` 零 chroma；(d) `me` + `overrides[1]` 仍回 `var(--color-me)`；(e) `counterparty` + grayscale scheme 仍回 `var(--color-them)`；(f) `speaker_cluster_unknown` 回 muted 色且不讀 overrides；(g) `parseTranscriptColorPref("{not-valid-json")` 回 default；(h) `parseTranscriptColorPref('{"scheme":"weird","overrides":[]}')` 回 default。對應 spec 五個 scenario。
- [x] 6.2 新增 `packages/web/src/hooks/use-transcript-color-pref.ts`：用 `useSyncExternalStore` 把 `localStorage["meeting-playbook:transcript-color-pref"]` 對映成 React state；暴露 `{ pref, setScheme(id), setOverride(clusterN, color), resetOverride(clusterN) }`；setter 寫 `JSON.stringify` 回 storage 並 dispatch 自訂 event 觸發同 tab re-render；listen `window` 的 `storage` event 同步跨 tab；初始化用 `parseTranscriptColorPref`。落實 `TranscriptColorScheme palettes ...` requirement 中關於 localStorage 持久化與 fail-soft 載入的部分。**Red tests 先寫**（`packages/web/src/hooks/use-transcript-color-pref.test.tsx`）：五條 — (a) 空 localStorage 初始化回 default；(b) `setScheme("vivid")` 後 localStorage 內容更新且 `pref.scheme === "vivid"`；(c) `setOverride(2, "#ff0000")` 後 `pref.overrides[2] === "#ff0000"`；(d) `resetOverride(2)` 後 `overrides` 不再含 2；(e) 模擬另一 tab 的 `storage` event → hook 內 state 更新。
- [x] 6.3 新增 `packages/web/src/components/speaker-color-popover.tsx` + 同檔級 long-press helper：用 shadcn `<Popover>`；trigger 由 caller 控制（`open` / `onOpenChange` props + 預設 anchor 為 speaker name button）；content 含 5 個 scheme radio chip（i18n `transcript.color.scheme.<id>`）、目前 scheme 對該 cluster N 的 6 swatch、`<Input type="color">` 自訂、Reset 鍵；對 `me` / `counterparty` / `speaker_cluster_unknown` 三 speaker 由 caller 條件 short-circuit 不掛 listener（component 本身 prop `clusterN: number` 為必填，從 type system 上不接受非 cluster speaker）。同步在 `transcript-pane.tsx` 把 speaker name 改成 `<button>` 並掛 `onContextMenu` preventDefault + `useLongPress` 偵測（≥ 500ms touchstart、touchmove > 8px 取消），對 `me` / `counterparty` / unknown 完全不掛 handler。`_chunkBackground` / `_speakerAccent` 改 call `_resolveClusterColor(chunk.speaker, pref)` 並刪除原 `_CLUSTER_HUES` 常數（移到 scheme module）。落實 `SpeakerColorPopover provides inline scheme and override controls` requirement 與設計 design.md「Cluster color customisation」popover UX 段落。**Red tests 先寫**（`packages/web/src/components/speaker-color-popover.test.tsx`）：五條 — (a) `clusterN = 2` 開啟後渲染 5 個 scheme radio + 6 個 swatch + custom color input + Reset 鍵；(b) 點某 swatch 後 `localStorage` payload `overrides["2"]` 更新為該 swatch 顏色字串；(c) 切 scheme 後 swatch grid 重算（用新 scheme 的 hue 算）；(d) Reset 鍵移除 `overrides[2]` 但保留 `overrides[1]`；(e) 切到 `grayscale` scheme 後 swatch grid 顯示 6 個 achromatic 灰階色（無 hue 環）。同時擴 `packages/web/src/components/transcript-pane.test.tsx`（若存在；否則新增）：(f) speaker = `me` 行右鍵 → 不開 popover 且原生 contextmenu 未被 preventDefault；(g) speaker = `speaker_cluster_1` 行右鍵 → popover 開且 anchor 在該 speaker name；(h) override 套用後 chunk 列背景重新渲染對應顏色。對應 spec 四個 scenario。

## 7. 前端：TranscriptChunkRow + mini-player

- [x] 7.1 在 `packages/web/src/components/transcript-chunk-row.tsx` 升級組件：hover 顯示 ▶ 與 ✎ 兩 icon button、✎ 進 inline edit mode（autosize textarea + Save / Cancel）、Save 呼叫 `PATCH` 並依回應結束 / 保留 edit mode；▶ 若該 chunk 對應 recording.deleted_at != null → disabled + Tooltip 顯示 i18n key `meeting.detail.audioPlayer.chunkExpired`。落實 `TranscriptChunkRow exposes inline edit mode for the chunk text` 與 `TranscriptChunkRow disables the play button when the recording is retention-expired` requirements，以及設計決策「Retention-aware UI：`deleted_at IS NOT NULL` → ▶ disabled + tooltip」。同步在 `packages/web/src/lib/transcript-edit-api.ts` 包裝 PATCH helper，錯誤經 `localizedErrorMessage` 對應 i18n。**Red tests 先寫**（`packages/web/src/components/transcript-chunk-row.test.tsx`）：六條 — (a) Cancel 不發 PATCH 並還原原文；(b) Save 成功更新顯示文字並退出 edit mode；(c) Save 收 422 `invalid_text` → 留 edit mode + 顯示本地化錯誤訊息 + Save button 可再按；(d) expired recording → ▶ disabled + zh-TW tooltip `"錄音已過 30 天保留期"`；(e) 非 expired → ▶ enabled，點擊觸發 mini-player；(f) 在 dual-channel context，`speaker = "counterparty"` 的 chunk 點 ▶ 後 mini-player active chunk 對應 counterparty recording。
- [x] 7.2 在 `packages/web/src/hooks/use-mini-player.ts` 實作 `useChunkAudioSource(chunk, recordings) -> Recording` 與 zustand store `useMiniPlayerStore`（state: `current_chunk_id / playback_rate / is_playing / chunks_sorted`；rate 寫 `localStorage.miniPlayerRate`），並在 `packages/web/src/components/meeting-audio-mini-player.tsx` 實作 sticky bottom 組件：play / pause / prev / next / speed dropdown（六段 `[0.5, 0.75, 1.0, 1.25, 1.5, 2.0]`），單一 `<audio>` element 透過 src 切換 chunk slice URL，速度變化只改 `playbackRate` 不重 fetch；rate 自 localStorage 載入 / 寫入。落實設計決策「Speaker-aware playback：dual 用 chunk.speaker 選 stream WAV、single 用唯一 recording」、「`<MeetingAudioMiniPlayer>` 黏在 detail 頁面底部，狀態存 zustand store」與 `useChunkAudioSource maps a transcript chunk to the correct recording` / `MeetingAudioMiniPlayer is a sticky bottom control bar with speed and chunk navigation` requirements。**Red tests 先寫**：`packages/web/src/hooks/use-mini-player.test.tsx` 四條 — (a) single-channel：任何 speaker 都回唯一 recording；(b) dual `speaker = "me"` → 回 `r_m`；(c) dual `speaker = "counterparty"` → 回 `r_c`；(d) dual + cluster label 防禦性 → 回 `r_m`；`packages/web/src/components/meeting-audio-mini-player.test.tsx` 五條 — (e) row ▶ click 後 `<audio>` src 切到 `/api/meetings/{id}/recordings/{recId}/audio?start=...&end=...`；(f) 速度切 `1.5x` → `playbackRate === 1.5` 且 src 未變；(g) Next 按鈕切下一段；(h) 第一段時 Prev disabled、最後一段時 Next disabled；(i) localStorage 持久化 — 重新 mount 後 default rate 為 `0.75`，對應 spec 八 scenarios。

## 8. Meeting detail 頁面整合 + i18n + docs

- [x] 8.1 在 `packages/web/src/routes/meetings/detail.tsx` 加入 `<MeetingAudioMiniPlayer>` 為 page-level sticky bottom bar（同時存在於 columns mode 與 stack mode、跨 Tab 切換不消失），並在 layout 預留 mini-player 高度避免遮蔽底部內容；落實 `Meeting detail page renders MeetingAudioMiniPlayer pinned to the bottom` requirement 與 `Retention expiration surfaces in the frontend via disabled play affordances`（前端錯誤 toast 部分）。**Red tests 先寫**（`packages/web/src/routes/meetings/detail.test.tsx` 新增 case）：四條 — (a) columns mode 渲染後 mini-player 在 DOM 內最後一個 child、三欄寬度 30/40/30 不變；(b) stack mode 渲染後 mini-player 在 P-T-A 之後；(c) Workspace Tab 切到 Summary Tab 後 mini-player 仍在 DOM；(d) idle 狀態下 Play / Prev / Next disabled 且 Speed dropdown enabled，對應 spec 四個 scenario。同檔再加一條：模擬 ▶ 收到後端 410 → 顯示 toast `"錄音已過 30 天保留期，無法播放"` 且 chunk row ▶ 變 disabled，對應 `recording-retention` spec scenario「Backend 410 response during a race causes the row to become disabled」。
- [x] 8.2 [P] 一次同步 i18n 與專案文件：在 `packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json` 同步新增 keys `transcript.chunk.edit.{startEdit,save,cancel,placeholder}`、`transcript.chunk.edit.errors.{invalidText,immutableField}`、`meeting.detail.audioPlayer.{play,pause,prev,next,speed,chunkExpired,playbackError}`、`transcript.color.scheme.{default,vivid,pastel,highContrast,grayscale}`、`transcript.color.{swatch,resetToScheme,customColor,popoverTitle}`；同時在 `CONTEXT.md` glossary 新增三條：「Mini-player」（meeting detail 頁面底部 sticky audio player）、「Transcript chunk edit」（chunk text 後編輯；speaker / 時間戳 immutable）、「Transcript color scheme」（5 hard-coded scheme + per-cluster override；`me` / `counterparty` 不可自訂）。**驗證**：`bun --filter @meeting-playbook/web test src/locales/locales.test.ts` deep-equal 比對通過；`grep -F 'Mini-player' CONTEXT.md` / `grep -F 'Transcript chunk edit' CONTEXT.md` / `grep -F 'Transcript color scheme' CONTEXT.md` 三條都命中；`transcript-chunk-row.test.tsx` / `meeting-audio-mini-player.test.tsx` / `speaker-color-popover.test.tsx` 在 zh-TW 與 en 雙 locale 渲染下都拿到正確字串。

## 9. End-to-end + coverage

- [x] 9.1 新增 `packages/backend/tests/integration/test_audio_playback_e2e.py`：模擬 dual-channel session finalize → 寫入兩個 recording row 含 `started_at` → 對 `me.wav` 發 Range request 取 10-second slice 驗 byte 對應；接著手動 `UPDATE recording SET deleted_at = now()` → 同一 endpoint 回 410；同檔再跑 single-channel 路徑（一個 recording row）驗 任何 chunk speaker 都映射到該 recording。對應 `audio-playback` + `recording-retention` 跨 capability 的端到端契約。**驗證**：`uv run pytest --cov=meeting_playbook.audio_playback --cov=meeting_playbook.transcript_edit --cov-report=term-missing` 同時跑出 `audio_playback/*` 與 `transcript_edit/*` 各自 80%+ coverage。

## 10. Apply 中後 UX 重設計（post-ingest 2026-05-15）— 對應 MODIFIED specs「MeetingAudioMiniPlayer ... whole-recording playback」/「TranscriptChunkRow exposes inline edit mode」/「SpeakerColorPopover provides inline scheme and override controls」與 ADDED specs「ChunkActionMenu surfaces per-chunk actions」/「Cluster speaker label override is persisted in localStorage per meeting」/「TranscriptColorPref stores cluster overrides as hue numbers」

- [x] 10.1 [AC-Hue] 實作 ADDED Requirement「TranscriptColorPref stores cluster overrides as hue numbers, not full color strings」並落實設計決策「cluster override 改存 hue 數字（不再存 full color string）」：改寫 `packages/web/src/lib/transcript-color-schemes.ts` 把 `TranscriptColorPref.overrides` 從 `Record<number, string>` 改成 `Record<number, number>`（hue 0–360）；`_resolveClusterColor` 處理 override 時用 hue 算 `accent = oklch(scheme.accentL scheme.accentC hue)` + `background = color-mix(in oklch, oklch(scheme.bgL scheme.bgC hue) calc(var(--them-tint-alpha) * 1000%), transparent)`（跟無 override 相同邏輯但 hue 來自 user）；grayscale scheme 下 overrides 一律忽略（resolver 仍走 lightness ladder）；`parseTranscriptColorPref` 驗 override value 必須是 `Number.isFinite && >= 0 && < 360`，否則 fail-soft drop 該 entry。**Verify**: 更新 `packages/web/src/lib/transcript-color-schemes.test.ts` 既有 (b) override scenario 改用 hue 數字（e.g. `pref.overrides[3] = 200` → accent 含 `200`）；新增 scenario：legacy string override `'oklch(0.55 0.18 150)'` 被 parser 丟掉；grayscale 下任何 override 不生效。對應 ADDED spec「TranscriptColorPref stores cluster overrides as hue numbers」兩個 scenario。
- [x] 10.2 改寫 `packages/web/src/components/speaker-color-popover.tsx`：移除 `onContextMenu` / long-press / `useEffect` 內的 click-outside listener 倚賴 anchor 的 contextmenu trigger 邏輯；改為 caller 完全 prop-driven（`open` / `onOpenChange` / `anchorRef`）。Swatch click 改寫 hue 數字（從 `scheme.hues[idx]` 直接取，非 grayscale）；Custom color input 移除；grayscale scheme 下隱藏 swatch grid 改顯示 hint「Grayscale 不支援 per-cluster 覆寫」。對應 MODIFIED spec「SpeakerColorPopover provides inline scheme and override controls」。**Verify**: 更新 `packages/web/src/components/speaker-color-popover.test.tsx`：(a) swatch click 後 `localStorage` payload `overrides["2"]` 是 number 而非 string；(b) grayscale scheme 下 swatch grid 不出現；(c) 不再驗 `onContextMenu` 行為（caller 控制）。
- [x] 10.3 [AC-Rename] 實作 ADDED Requirement「Cluster speaker label override is persisted in localStorage per meeting」前半並落實設計決策「cluster speaker rename（per-meeting localstorage override）」：新增 `packages/web/src/hooks/use-cluster-speaker-labels.ts`：`useSyncExternalStore` 包 `localStorage["meeting-playbook:speaker-labels"]`，schema `Record<meetingId, Record<clusterN, label>>`；暴露 `{ labels, setLabel(clusterN, label), resetLabel(clusterN) }`；invalid label（empty trim、>50 chars、非 string）fail-soft 丟掉；跨 tab `storage` event 同步。對應 ADDED spec「Cluster speaker label override is persisted in localStorage per meeting」前半。**Verify**: 新增 `packages/web/src/hooks/use-cluster-speaker-labels.test.tsx`：四條 — (a) 空 localStorage → `labels` empty object；(b) `setLabel(2, "Alice")` → localStorage 更新 + hook re-render；(c) `setLabel(2, "")` / `setLabel(2, "x".repeat(51))` → 不寫；(d) 模擬另一 tab `storage` event → hook state 更新。
- [x] 10.4 [AC-Menu] 實作 ADDED Requirement「ChunkActionMenu surfaces per-chunk actions in a single dropdown」與「ChunkActionMenu disables the play action when the recording is retention-expired」並落實設計決策「chunk action menu 取代右鍵 popover + hover ▶/✎」：新增 `packages/web/src/components/chunk-action-menu.tsx` + 同檔測試。Component 用 hand-rolled popover（同 ui/dialog 風格）；props: `chunk`, `meetingId`, `recording` (resolved by `pickMeetingRecording`), `open`, `onOpenChange`, `onPlayRequested`, `onEditTextRequested`, `onEditColorRequested`, `onRenameRequested`。Render 順序：Play this chunk → Edit text → (cluster only) Edit color → (cluster only) Rename speaker。`me` / `counterparty` / `speaker_cluster_unknown` 不渲染後兩個 items。Retention-expired（recording.deleted_at != null）時 Play item disabled + aria-label `meeting.detail.audioPlayer.chunkExpired`。對應 ADDED spec「ChunkActionMenu surfaces per-chunk actions」三個 scenario。**Verify**: 新增 `packages/web/src/components/chunk-action-menu.test.tsx`：三條 — (a) cluster speaker → 4 items；(b) `me` speaker → 2 items, Edit color / Rename speaker DOM 不存在；(c) expired recording → Play disabled。
- [x] 10.5 改寫 `packages/web/src/components/transcript-chunk-row.tsx`：移除 hover-revealed ▶ / ✎ 兩個 button；改成固定 ✎ icon 在 row 末尾（永遠可見），click 開 `<ChunkActionMenu>`；保留既有 inline edit textarea mode；新增 inline rename mode（input 取代 speaker name 顯示，autofocus + select-all、Enter 提交 / Esc 取消）；接受新 prop `clusterN?: number` 與 `speakerLabel?: string`（由 transcript-pane 傳入）。對應 MODIFIED spec「TranscriptChunkRow exposes inline edit mode for the chunk text」與 ADDED spec「Cluster speaker label override ...」後半的 rename UI。**Verify**: 更新 `packages/web/src/components/transcript-chunk-row.test.tsx`：(a) ✎ icon 永遠可見（無 `opacity-0`）；(b) click ✎ → `<chunk-action-menu>` 出現；(c) Cancel inline edit 不發 PATCH；(d) Save inline edit 成功；(e) cluster speaker 點 Rename → 顯示 rename input → Enter 提交呼叫 `setLabel(clusterN, value)`；(f) Esc 取消 rename 不寫 localStorage。
- [x] 10.6 [AC-WholeRecording] 實作 MODIFIED Requirement「MeetingAudioMiniPlayer is a sticky bottom control bar with whole-recording playback」並落實設計決策「mini-player 改成整段 recording 播放（不再 chunk-by-chunk 切 src）」store 端：改寫 `packages/web/src/hooks/use-mini-player.ts`：把 `useChunkAudioSource` rename → `pickMeetingRecording(recordings) -> Recording`（dual 永遠回 `me` stream；single 唯一一個）；`miniPlayerStore` 加 method `seekToChunk(chunkId)`（內部算 `audio.currentTime` 但實際 seek 由 component 透過 ref 執行；store 只記 `seek_target_seconds`）；保留 `playback_rate`、`is_playing`、`chunks_sorted`、`recordings`、`meeting_id` 狀態；移除 `current_chunk_id` 概念（mini-player 改成整段播放，"current chunk" 變成「audio.currentTime 落在哪個 chunk's [started_at, ended_at] 區間」由 selector 計算）。對應 MODIFIED spec「MeetingAudioMiniPlayer ... whole-recording playback」上半。**Verify**: 更新 `packages/web/src/hooks/use-mini-player.test.tsx`：(a) `pickMeetingRecording([me, cp])` 回 me；(b) `pickMeetingRecording([only])` 回 only；(c) `seekToChunk("c2")` 後 `state.seek_target_seconds` 等於 `chunk_c2.started_at - recording.started_at` 的秒數；(d) `setRate(1.5)` 仍持久化到 localStorage。
- [x] 10.7 改寫 `packages/web/src/components/meeting-audio-mini-player.tsx`：單一 `<audio src={pickMeetingRecording(recordings).id}>` mount 後不換 src；新增 seek bar (`<input type="range" min={0} max={audio.duration} value={audio.currentTime}>`)、current time / duration 文字；Prev / Next 改 seek 到上/下一個 chunk 的 `started_at` 對應秒數（非切 src）；訂閱 `seek_target_seconds` 變動觸發 `audioRef.current.currentTime = target`。對應 MODIFIED spec「MeetingAudioMiniPlayer ... whole-recording playback」下半。**Verify**: 更新 `packages/web/src/components/meeting-audio-mini-player.test.tsx`：(a) mount 後 `<audio src>` 等於 `/api/.../recordings/<me-stream-rid>/audio`；(b) `seekToChunk("c2")` 後 `audio.src` 不變、`audio.currentTime` 更新；(c) seek bar 拖拉 → `audio.currentTime` 更新；(d) Prev / Next 變成 seek（不切 src）；(e) idle 狀態（無 recording）所有 transport disabled。
- [x] 10.8 改寫 `packages/web/src/components/transcript-pane.tsx`：把 chunk 渲染區從既有 inline `<li>` 結構改用 `<TranscriptChunkRow>`；傳入 `meetingId`、`chunkId`、`text`、`speaker`、`clusterN`（解析 `/^speaker_cluster_(\d+)$/`）、`speakerLabel`（從 `useClusterSpeakerLabels(meetingId).labels[clusterN]` 或 fallback `t("meetings.speaker.cluster", { n: clusterN })`）、`recording`（從 `pickMeetingRecording(recordings)`）、`isExpired`（`recording.deleted_at != null`）等；移除既有 `onContextMenu` 開 popover 邏輯；color resolver 仍照舊用 `_resolveClusterColor(chunk.speaker, pref)`。**Verify**: 既有 `transcript-pane.test.tsx`（若存在）0 regression；視覺手測：speaker `cluster_1` chunk hover ✎ 出現、click ✎ 開 menu、選 Rename → inline input 出現。
- [x] 10.9 [P] 同步雙 locale 新增 keys：`transcript.chunk.actions.{play,editText,editColor,renameSpeaker}`（zh：「播放此段 / 編輯文字 / 編輯顏色 / 重新命名講者」, en：「Play this chunk / Edit text / Edit color / Rename speaker」）；`transcript.chunk.rename.{save,cancel,placeholder,invalidLength}`（zh：「儲存 / 取消 / 輸入講者名稱（1–50 字） / 名稱必須是 1–50 字元」, en：「Save / Cancel / Enter speaker name (1–50 chars) / Name must be 1–50 characters」）；`meeting.detail.audioPlayer.{seekBar,currentTime,duration}`（zh：「進度 / 目前 / 總長」, en：「Progress / Current / Duration」）。`packages/web/src/locales/{zh-TW,en}.json` 同步寫入。**Verify**: `bun --filter @meeting-playbook/web test src/locales/locales.test.ts` deep-equal 通過；新 component 測試 i18n key lookup 不缺鍵。
- [x] 10.10 跨檔最終 verify — 對應設計文件「implementation contract (post-ingest)」全部 acceptance 項：：`bun run test` 全綠（web 預期 +15~25 個新測試、backend 不動）。手動跑 `bun run dev`，在一個有 recordings 的 meeting 驗證五個 path：(a) 底部 mini-player 載入後 `<audio src>` 是整段 recording、seek bar 可拉、速度持久化；(b) chunk 末尾 ✎ icon 永遠可見、click 開 menu；(c) cluster speaker chunk menu 含 4 items、me/counterparty chunk menu 含 2 items；(d) Edit color 後 chunk 背景與 speaker name 對比清晰可讀（不再同色）；(e) Rename speaker 後該 cluster 所有 chunks 都顯示新名稱、重整頁面後仍保留。**Verify**: 五個手測 path 各自通過；`bun run test` exit 0。
