## 0. Coverage map (analyzer aid — links task groups to spec requirements + design decisions)

| Task group | meeting-summary spec requirement(s)                        | meeting-session spec requirement(s)            | meeting-detail-layout spec requirement(s)     | design.md decision(s) |
| ---------- | ---------------------------------------------------------- | ---------------------------------------------- | --------------------------------------------- | --------------------- |
| 1          | summary table + SummaryRepository                          | —                                              | —                                             | Decision 1, 8, 9 |
| 2          | MeetingSummarizer Protocol + VertexProSummarizer + prompts | —                                              | —                                             | Decision 5 |
| 3          | runtime.spawn_summary_task + in-flight registry            | —                                              | —                                             | Decision 3 |
| 4          | POST + GET endpoints (busy / pending / not_found)          | —                                              | —                                             | Decision 4 |
| 5          | —                                                          | finalize fire-and-forget spawn                 | —                                             | Decision 2 |
| 6          | —                                                          | —                                              | Tabs UI + SummaryPane + export                | Decision 6, 7 |
| 7          | —                                                          | —                                              | (i18n + agent doc + verification)             | — |

## 1. summary table + SummaryRepository

- [x] 1.1 [P] Write `packages/backend/tests/test_alembic_summary.py::test_upgrade_creates_summary_table_with_unique_meeting_id` — RED test running the new Alembic migration on the test DB asserting the `summary` table exists with columns (`id`, `meeting_id`, `markdown`, `generated_at`), the FK constraint to `meeting.id` with `ON DELETE CASCADE`, the `UNIQUE` constraint on `meeting_id`, and that all four columns are NOT NULL. Mirror the structure of `test_alembic_chat_message.py` (slice 9 precedent).
- [x] 1.2 Add `packages/backend/alembic/versions/0006_create_summary.py` with `revision="0006_create_summary"` and `down_revision="0005_create_chat_message"`. The `upgrade()` function SHALL create the `summary` table per Decision 1's exact schema (4 columns, FK CASCADE to meeting, UNIQUE on meeting_id, default `now()` on generated_at). The `downgrade()` function SHALL drop the table. Drive 1.1 GREEN.
- [x] 1.3 [P] Add `packages/backend/meeting_playbook/summarization/__init__.py` (empty package marker).
- [x] 1.4 [P] Add `packages/backend/meeting_playbook/summarization/models.py` exporting `Summary` SQLAlchemy ORM mirroring the migration; columns `id` (String PK), `meeting_id` (String, FK to meeting.id ON DELETE CASCADE, unique=True), `markdown` (Text), `generated_at` (TIMESTAMP timezone=True). Use the same Mapped/mapped_column pattern as `chat/models.py`.
- [x] 1.5 [P] Write `packages/backend/tests/summarization/__init__.py` (empty) AND `packages/backend/tests/summarization/test_repository.py::test_upsert_creates_then_replaces_row_atomically` — RED test seeding a meeting then calling `repo.upsert(meeting_id, markdown="v1")`, asserting one row exists with content "v1"; calling `repo.upsert(meeting_id, markdown="v2")`, asserting still one row with content "v2" and `generated_at` strictly greater than the first.
- [x] 1.6 [P] Write `packages/backend/tests/summarization/test_repository.py::test_get_for_meeting_returns_none_when_absent` — RED test with a meeting that has no summary row; assert `repo.get_for_meeting(meeting_id) is None`.
- [x] 1.7 [P] Write `packages/backend/tests/summarization/test_repository.py::test_cascade_delete_on_meeting_removal` — RED test inserting meeting + summary row; deleting the meeting row; asserting summary row is gone.
- [x] 1.8 [P] Write `packages/backend/tests/summarization/test_repository.py::test_get_with_stale_flag_true_when_transcript_chunk_newer_than_generated_at` — RED test seeding meeting + summary row at T0 + transcript_chunk at T0+10s; assert `repo.get_with_stale_flag(meeting_id).is_stale is True`.
- [x] 1.9 [P] Write `packages/backend/tests/summarization/test_repository.py::test_get_with_stale_flag_true_when_playbook_updated_after_summary` — RED test seeding meeting + summary at T0, then upserting playbook at T0+5s; assert `is_stale is True`.
- [x] 1.10 [P] Write `packages/backend/tests/summarization/test_repository.py::test_get_with_stale_flag_true_when_chat_message_newer_than_generated_at` — RED test seeding meeting + summary at T0 + chat_message pair via slice-9 ChatMessageRepository at T0+5s; assert `is_stale is True`.
- [x] 1.11 [P] Write `packages/backend/tests/summarization/test_repository.py::test_get_with_stale_flag_false_when_nothing_changed_after_generation` — RED test seeding meeting + summary at T_now (no transcript / playbook / chat newer); assert `is_stale is False`.
- [x] 1.12 Add `packages/backend/meeting_playbook/summarization/repository.py` exporting `SummaryRepository(session)` with three methods: `get_for_meeting(meeting_id) -> Summary | None` (SELECT one or none), `get_with_stale_flag(meeting_id) -> SummaryWithStale | None` (single SQL using PostgreSQL `MAX()` subqueries on transcript_chunk / playbook / chat_message — see Decision 9 for the SQL — wrapped in a `SummaryWithStale` dataclass with fields `id, meeting_id, markdown, generated_at, is_stale`), and `upsert(meeting_id, markdown) -> Summary` (uses `pg_insert(...).on_conflict_do_update(index_elements=["meeting_id"], set_={"markdown": ..., "generated_at": now()}).returning(Summary)`, then `await session.commit()`). The repository SHALL NOT expose any delete method. Drive 1.5-1.11 GREEN.

## 2. MeetingSummarizer Protocol + VertexProSummarizer + prompts

- [x] 2.1 [P] Add `packages/backend/meeting_playbook/summarization/base.py` exporting `MeetingSummarizer` Protocol (single async method `summarize(meeting_id: str) -> str`), `Locale = Literal["zh-TW", "en"]`, and `class SummaryFormatError(Exception)`. The Protocol module imports nothing from `google.genai` so test mocks don't need GCP libs.
- [x] 2.2 [P] Add `packages/backend/meeting_playbook/config.py` field `vertex_pro_model_id: str = "gemini-2.5-pro"` mirroring the existing `vertex_flash_model_id` field. Update the inline comment block to mention this is for `MeetingSummarizer` (post-meeting summary, slice 10) AND must be a Vertex AI publisher model id (NOT a Gemini API name).
- [x] 2.3 [P] Update `.env.example` — add commented `# VERTEX_PRO_MODEL_ID=gemini-2.5-pro` next to the existing `VERTEX_FLASH_MODEL_ID` line, with a note explaining default + override pattern.
- [x] 2.4 [P] Write `packages/backend/tests/summarization/test_prompts.py::test_system_instruction_zh_tw_lists_4_headings_verbatim` — RED test asserting `build_system_instruction("zh-TW")` returns a string containing the 4 literal substrings `## 重點討論`, `## 決議`, `## Action items`, `## 待解決問題` in that document order, AND containing a phrase forbidding the model from changing heading text or order.
- [x] 2.5 [P] Write `packages/backend/tests/summarization/test_prompts.py::test_system_instruction_en_lists_4_headings_verbatim` — RED test asserting `build_system_instruction("en")` returns a string containing the 4 literal substrings `## Key discussion points`, `## Decisions`, `## Action items`, `## Open questions` in that document order.
- [x] 2.6 [P] Write `packages/backend/tests/summarization/test_prompts.py::test_user_message_renders_4_section_headings_in_order` — RED test feeding a meeting + 1 playbook field + 3 transcript chunks + 1 chat exchange (use the same SimpleNamespace duck-typing pattern as `tests/advisor/test_prompts.py`); assert `build_user_message(...)` returns a string containing `## 會議基本資料`, `## Playbook`, `## 整場 Transcript`, `## In-meeting Advisor 對話` in that document order.
- [x] 2.7 [P] Write `packages/backend/tests/summarization/test_prompts.py::test_user_message_empty_chat_history_renders_locale_placeholder` — RED test calling `build_user_message(..., chat_history=[], locale="zh-TW")`; assert the `## In-meeting Advisor 對話` section's body equals the literal `(無)`.
- [x] 2.8 [P] Write `packages/backend/tests/summarization/test_prompts.py::test_user_message_empty_playbook_renders_locale_placeholder` — RED test calling `build_user_message(..., playbook=<all-empty>, locale="zh-TW")`; assert the `## Playbook` section's body equals the literal `(尚未填寫)`.
- [x] 2.9 Add `packages/backend/meeting_playbook/summarization/prompts.py` exporting `LOCALE_INSTRUCTION: dict[Locale, str]`, `build_system_instruction(locale)`, `build_user_message(meeting, playbook, recent_chunks, chat_history, locale)`, and the four required-heading constants `_REQUIRED_HEADINGS: dict[Locale, tuple[str, str, str, str]]`. System instruction body enumerates the 4 headings in order, the action item line format `- [{owner_or_TBD}] {action}`, the empty-section behavior `(無)` / `(none)`, and the forbidden behaviors per Decision 5. User message renders the 4 sections in order with the slice-9 dialogue formatting reused for transcript and chat history. Drive 2.4-2.8 GREEN.
- [x] 2.10 [P] Write `packages/backend/tests/summarization/test_vertex_summarizer.py::test_summarize_returns_markdown_when_4_headings_present_in_order` — RED test using a mocked SDK client (mirror the `_build_mock_client` pattern from `tests/advisor/test_vertex_advisor.py`) returning markdown `"## 重點討論\n- a\n## 決議\n(無)\n## Action items\n- [TBD] x\n## 待解決問題\n(無)"`; assert `summarizer.summarize("m_x")` returns that string verbatim.
- [x] 2.11 [P] Write `packages/backend/tests/summarization/test_vertex_summarizer.py::test_summarize_raises_summary_format_error_when_heading_missing` — RED test with mocked client returning markdown that omits `## 決議`; assert `summarizer.summarize("m_x")` raises `SummaryFormatError`.
- [x] 2.12 [P] Write `packages/backend/tests/summarization/test_vertex_summarizer.py::test_summarize_raises_summary_format_error_when_headings_out_of_order` — RED test with mocked client returning markdown where `## Action items` appears before `## 決議`; assert `summarizer.summarize` raises `SummaryFormatError`.
- [x] 2.13 [P] Write `packages/backend/tests/summarization/test_vertex_summarizer.py::test_summarize_raises_timeout_after_90s` — RED test with mocked client whose generation sleeps 100 seconds; assert `summarizer.summarize` raises `asyncio.TimeoutError` between 90.0s and 92.0s after invocation.
- [x] 2.14 Add `packages/backend/meeting_playbook/summarization/vertex_summarizer.py` exporting `VertexProSummarizer(client_factory: Callable[[], Any], model_id: str, session_factory: async_sessionmaker[AsyncSession])`. The implementation SHALL fetch context inside `async with session_factory() as ...` (transcript via SessionRepository.list_chunks_for_meeting, playbook via PlaybookRepository.get_or_create_for_meeting, chat history via ChatMessageRepository.list_for_meeting; also fetch `Meeting` row for title + display names), close the session, then run the LLM call inside `asyncio.timeout(90.0)`. Use `client.aio.models.generate_content(...)` (non-streaming — we want the full markdown to validate before yielding). After the LLM response, call `_validate_4_headings(markdown, locale)` which raises `SummaryFormatError` on mismatch (case- and trim-insensitive substring match for the 4 headings, in document order). Locale is hardcoded to `"zh-TW"` for now (slice 10 ships single-locale; later slice can plumb from frontend). Drive 2.10-2.13 GREEN.
- [x] 2.15 [P] Add `packages/backend/meeting_playbook/summarization/dependencies.py` exporting `get_meeting_summarizer_dependency() -> MeetingSummarizer` using `functools.lru_cache` to memoise a single `VertexProSummarizer` per process. Build pattern mirrors `advisor/dependencies.py` slice-9 design: client factory reads from `Settings.vertex_ai_project` + `Settings.vertex_ai_location` (NOT os.environ), and session_factory is the application-wide `async_sessionmaker` from `meetings/dependencies.py`.
- [x] 2.16 [P] Write `packages/backend/tests/summarization/test_dependencies.py::test_dependency_returns_singleton` — RED test calling `get_meeting_summarizer_dependency()` twice; assert `result1 is result2`. Use `monkeypatch` to set `VERTEX_AI_PROJECT` env var so the dependency builder doesn't try real GCP auth.

## 3. runtime.spawn_summary_task + in-flight registry

- [x] 3.1 [P] Write `packages/backend/tests/summarization/test_runtime.py::test_spawn_returns_true_first_time_then_false_while_pending` — RED test (use `runtime.spawn_summary_task` with a mock summarizer that sleeps then returns valid markdown). First call returns `True` AND `is_pending(meeting_id) == True`; second call (before first completes) returns `False`; await first; assert `is_pending == False`.
- [x] 3.2 [P] Write `packages/backend/tests/summarization/test_runtime.py::test_successful_run_upserts_then_pops_registry` — RED test with mock summarizer returning valid markdown; spawn task; await its completion via `await asyncio.gather(*runtime._inflight.values())` (or similar testing seam); assert one summary row exists in DB AND `is_pending == False`.
- [x] 3.3 [P] Write `packages/backend/tests/summarization/test_runtime.py::test_failed_run_skips_upsert_but_pops_registry` — RED test with mock summarizer raising `asyncio.TimeoutError`; spawn; await; assert ZERO summary rows for the meeting AND `is_pending == False` AND no exception escapes the task.
- [x] 3.4 [P] Write `packages/backend/tests/summarization/test_runtime.py::test_summary_format_error_treated_as_failure` — RED test with mock summarizer raising `SummaryFormatError`; assert same as 3.3 (no row, registry popped, no exception escapes).
- [x] 3.5 Add `packages/backend/meeting_playbook/summarization/runtime.py` exporting `_inflight: dict[str, asyncio.Task[None]]`, `_lock: asyncio.Lock`, `spawn_summary_task(meeting_id, *, summarizer=None, session_factory=None) -> bool` (testing seam — when summarizer/session_factory not passed, fetches from `get_meeting_summarizer_dependency()` and `get_session_factory_dependency()`), `is_pending(meeting_id) -> bool`. The internal `_run(meeting_id, summarizer, session_factory)` coroutine wraps the call in try/except: on success calls `SummaryRepository(advice_session).upsert(meeting_id, markdown)` inside a fresh `async with session_factory() as ...`; on `asyncio.TimeoutError`, `SummaryFormatError`, or generic `Exception` calls `logger.exception(...)` and skips upsert. The `finally` block pops `meeting_id` from `_inflight` and is reached regardless of outcome. Drive 3.1-3.4 GREEN.

## 4. POST + GET endpoints

- [x] 4.1 [P] Write `packages/backend/tests/summarization/test_router.py::test_post_summary_owner_idle_returns_202_pending` — RED test with a meeting belonging to `u_a` and no in-flight task; POST `/api/meetings/{id}/summary` with `X-User-Id: u_a`; assert HTTP 202 with body `{"status": "pending"}` AND `runtime.is_pending(meeting_id) == True` after the call. Use a mock summarizer (via dependency override) that sleeps long enough for the test to observe the pending state.
- [x] 4.2 [P] Write `packages/backend/tests/summarization/test_router.py::test_post_summary_busy_returns_409_summary_busy` — RED test pre-populating runtime._inflight with a sleeping task; POST returns 409 with body `{"error_code": "summary.busy", ...}` AND no second task is spawned (assert `len(runtime._inflight) == 1`).
- [x] 4.3 [P] Write `packages/backend/tests/summarization/test_router.py::test_post_summary_non_owner_returns_404_meeting_not_found` — RED test meeting belongs to `u_a`, request as `u_b`; assert HTTP 404 with body `{"error_code": "meeting.not_found", ...}`.
- [x] 4.4 [P] Write `packages/backend/tests/summarization/test_router.py::test_post_summary_missing_x_user_id_returns_401` — RED test no header; assert 401 with body `{"error_code": "auth.gateway_bypass", ...}`.
- [x] 4.5 [P] Write `packages/backend/tests/summarization/test_router.py::test_get_summary_returns_persisted_row_with_is_stale_false_for_fresh_summary` — RED test with summary row created at T_now and no newer transcript / playbook / chat; GET returns 200 with body keys `id`, `meeting_id`, `markdown`, `generated_at`, `is_stale` AND `is_stale == False`.
- [x] 4.6 [P] Write `packages/backend/tests/summarization/test_router.py::test_get_summary_returns_pending_when_in_flight_no_row` — RED test no summary row, but `runtime._inflight` has an unfinished task for the meeting; GET returns 200 with body `{"status": "pending", "generated_at": null}`.
- [x] 4.7 [P] Write `packages/backend/tests/summarization/test_router.py::test_get_summary_returns_404_summary_not_found_when_no_row_no_pending` — RED test no row + no in-flight; GET returns 404 with body `{"error_code": "summary.not_found", ...}`.
- [x] 4.8 [P] Write `packages/backend/tests/summarization/test_router.py::test_get_summary_returns_is_stale_true_when_transcript_chunk_newer` — RED test summary at T0, then transcript_chunk at T0+10s; GET returns 200 with `is_stale: true`.
- [x] 4.9 Add `packages/backend/meeting_playbook/summarization/router.py` exporting `router = APIRouter(tags=["summary"])`. Two routes: `POST /api/meetings/{meeting_id}/summary` (verify ownership via MeetingRepository, check `runtime.is_pending` first → 409 if busy; else `runtime.spawn_summary_task` → 202 + `{status: "pending"}`) and `GET /api/meetings/{meeting_id}/summary` (verify ownership; query `repo.get_with_stale_flag` → 200 with full Summary if present; else if `runtime.is_pending` → 200 + pending shape; else 404 `summary.not_found`). Drive 4.1-4.8 GREEN.
- [x] 4.10 Update `packages/backend/meeting_playbook/server.py::create_app` to include `app.include_router(summarization_router)` (mirroring slice-9 chat router mount). Re-run 4.5 to confirm endpoint reachable from `TestClient(app)`.
- [x] 4.11 Update `packages/backend/tests/conftest.py` truncate cascade to add `summary` table (insert before `meeting`'s truncate so child rows die first per slice-9 conftest pattern).

## 5. WS finalize fire-and-forget spawn

- [x] 5.1 [P] Write `packages/backend/tests/sessions/test_router_summary_spawn.py::test_end_meeting_spawns_summary_task` — RED integration test (mirror the pattern of `test_router_advice.py`). Override `get_meeting_summarizer_dependency` with a stub that records calls and sleeps 30s to keep the task pending. Drive a full session through `start_meeting` → `end_meeting`; after the WS closes, assert `runtime.is_pending(meeting_id) == True` AND the stub summarizer's `summarize` was invoked exactly once with the correct meeting_id.
- [x] 5.2 [P] Write `packages/backend/tests/sessions/test_router_summary_spawn.py::test_end_meeting_does_not_block_websocket_close_on_long_summary` — RED test using a stub summarizer that sleeps 60s; record the wall-clock duration from `end_meeting` send to `WebSocketDisconnect`; assert duration < 5 seconds (the spawn must NOT await the summary task).
- [x] 5.3 [P] Write `packages/backend/tests/sessions/test_router_summary_spawn.py::test_end_meeting_when_summary_already_pending_logs_and_does_not_double_spawn` — RED test pre-populating `runtime._inflight` with a sleeping task before connecting the WS; drive `end_meeting`; assert `len(runtime._inflight) == 1` AND only the original task exists.
- [x] 5.4 Update `packages/backend/meeting_playbook/sessions/router.py` — at the end of `meeting_session_endpoint`, after the existing finalize block (recording rows persisted, status → completed, `meeting_ended` sent, websocket closed in the existing `with contextlib.suppress(...)` block), add `with contextlib.suppress(Exception): summarization.runtime.spawn_summary_task(meeting_id)`. Wrap in suppress because spawn failure (e.g., dependency not initialized in pathological test setup) MUST NOT raise out of the WS handler. Log at info level when `spawn_summary_task` returns False (already in-flight from a prior session). Drive 5.1-5.3 GREEN.

## 6. Frontend Tabs UI + SummaryPane + export

- [x] 6.1 [P] Add `packages/web/src/components/ui/tabs.tsx` — shadcn Tabs primitive (Tabs / TabsList / TabsTrigger / TabsContent) using Radix UI's `@radix-ui/react-tabs` if not already present in the project; if present, skip creation.
- [x] 6.2 [P] Add `packages/web/src/lib/summary-api.ts` exporting TypeScript types (`Summary`, `SummaryPending`, `SummaryResponse = Summary | SummaryPending`), `summaryQueryOptions(meetingId, opts: { enabled, refetchInterval? })` (queryKey `["summary", meetingId]`, 404 → `null`, 200 with `{status: "pending"}` → typed pending), `regenerateSummary(meetingId)` (POST returning 202 / 409 / 404; throws `SummaryApiError` on 409 / 404), and `SummaryApiError` with `status` + `errorCode` + `message`.
- [x] 6.3 [P] Write `packages/web/src/lib/summary-api.test.ts::test_summaryQueryOptions_parses_summary_pending_and_404` — RED test mocking `globalThis.fetch` to return each of the three GET shapes (Summary 200, pending 200, 404 not_found); assert the queryFn returns the right typed shape (Summary | SummaryPending | null) for each.
- [x] 6.4 [P] Write `packages/web/src/lib/summary-api.test.ts::test_regenerateSummary_throws_SummaryApiError_on_409_busy` — RED test mocking POST to return 409 with body `{error_code: "summary.busy", message}`; assert `regenerateSummary("m_x")` throws `SummaryApiError` with `status === 409` and `errorCode === "summary.busy"`.
- [x] 6.5 [P] Add `packages/web/src/lib/markdown-export.ts` exporting `exportSummaryAsMarkdown(meeting: { title, created_at }, markdown: string)` per Decision 7 (File System Access API with AbortError respect; blob fallback). Filename = `{meeting.title}-{YYYY-MM-DD}.md` with non-filesystem-safe chars `[/\\:*?"<>|]` replaced by `_`.
- [x] 6.6 [P] Write `packages/web/src/lib/markdown-export.test.ts::test_filename_strips_non_filesystem_safe_chars` — RED unit test calling the filename builder helper with title `"Q3/Q4 review: 對方?"`; assert returned filename matches `/Q3_Q4 review_ 對方_-\d{4}-\d{2}-\d{2}\.md/`.
- [x] 6.7 [P] Write `packages/web/src/lib/markdown-export.test.ts::test_falls_back_to_blob_download_when_showSaveFilePicker_unavailable` — RED test with `globalThis.window.showSaveFilePicker` set to undefined; mock `URL.createObjectURL` + `URL.revokeObjectURL` + spy on `document.createElement("a").click`; call export; assert the blob path was taken.
- [x] 6.8 [P] Add `packages/web/src/hooks/use-detail-tab.ts` exporting `useDetailTab(meetingId, meeting)` returning `[tab, setTab]` with `tab` typed as `"workspace" | "summary"`. Persists to `localStorage` keyed `meeting-detail-tab:{meetingId}`. If the persisted value is `"summary"` but `meeting.status !== "completed"`, returns `"workspace"` (fallback per Decision 6).
- [x] 6.9 [P] Write `packages/web/src/hooks/use-detail-tab.test.tsx::test_default_is_workspace_when_no_persisted_value` — RED test with empty `localStorage`; assert the hook returns `"workspace"`.
- [x] 6.10 [P] Write `packages/web/src/hooks/use-detail-tab.test.tsx::test_persisted_summary_falls_back_to_workspace_when_meeting_not_completed` — RED test pre-setting `localStorage` to `summary` and meeting `status: "in_progress"`; assert the hook returns `"workspace"`.
- [x] 6.11 [P] Write `packages/web/src/hooks/use-detail-tab.test.tsx::test_setTab_summary_persists_to_localStorage_when_meeting_completed` — RED test with meeting `status: "completed"`; call `setTab("summary")`; assert `localStorage.getItem("meeting-detail-tab:m_x") === "summary"`.
- [x] 6.12 [P] Add `packages/web/src/components/summary-pane.tsx` — props `{ meetingId, meeting }`. Uses `useQuery(summaryQueryOptions(meetingId, { enabled: meeting.status === "completed", refetchInterval: data => (data && "status" in data && data.status === "pending") ? 1000 : false }))`. Renders 5 visual states per Decision 6 + spec scenario list: loading skeleton, pending hint, done with status row + body + export + regenerate, done + stale-alert variant, empty (404) with `生成摘要` button. Regenerate button calls `regenerateSummary` then `queryClient.invalidateQueries(["summary", meetingId])`. Export button calls `exportSummaryAsMarkdown(meeting, summary.markdown)`. data-testid attributes: `summary-loading`, `summary-pending`, `summary-stale-alert`, `summary-empty`, `summary-regenerate-button`, `summary-export-button`, `summary-generate-button`.
- [x] 6.13 [P] Write `packages/web/src/components/summary-pane.test.tsx::test_loading_state_renders_summary_loading_testid` — RED test with React Query in initial loading state (mock fetch to never resolve); assert `data-testid="summary-loading"` is present.
- [x] 6.14 [P] Write `packages/web/src/components/summary-pane.test.tsx::test_pending_state_renders_hint_and_polls_every_1s` — RED test with fetch returning `{status: "pending", generated_at: null}` first, then a Summary on second poll; assert pending UI renders, then after `await waitFor(...)` the done UI renders; assert fetch was called at least twice within 2 seconds.
- [x] 6.15 [P] Write `packages/web/src/components/summary-pane.test.tsx::test_done_state_renders_markdown_body_and_actions` — RED test with fetch returning a done Summary; assert MarkdownPreview renders the markdown content; assert `summary-regenerate-button` and `summary-export-button` are visible; assert `summary-stale-alert` is NOT in the DOM (is_stale=false).
- [x] 6.16 [P] Write `packages/web/src/components/summary-pane.test.tsx::test_stale_alert_renders_when_is_stale_true` — RED test with fetch returning Summary with `is_stale: true`; assert `summary-stale-alert` is visible above the markdown body.
- [x] 6.17 [P] Write `packages/web/src/components/summary-pane.test.tsx::test_empty_state_renders_generate_button_when_404` — RED test with fetch returning 404 `summary.not_found`; assert `summary-empty` is rendered with a `summary-generate-button`.
- [x] 6.18 [P] Write `packages/web/src/components/summary-pane.test.tsx::test_regenerate_click_calls_POST_and_invalidates_cache` — RED test with done state + spy on POST endpoint; click regenerate; assert POST was called once AND React Query cache for `["summary", meetingId]` was invalidated.
- [x] 6.19 [P] Write `packages/web/src/components/summary-pane.test.tsx::test_export_click_invokes_helper_with_meeting_and_markdown` — RED test mocking `exportSummaryAsMarkdown`; click export button; assert helper called once with the right meeting + markdown args.
- [x] 6.20 Update `packages/web/src/routes/meetings/detail.tsx` to wrap the existing 3-column workspace AND a new SummaryPane inside `<Tabs>`. Use `useDetailTab(meetingId, meeting)` for state. Two TabsTrigger: `workspace` (always enabled) and `summary` (`disabled={meeting.status !== "completed"}` with `title` attr containing the localised disabled hint). TabsContent for `workspace` wraps the existing layout switcher + 3-column grid. TabsContent for `summary` renders `<SummaryPane meetingId={meetingId} meeting={meeting} />` only when the active tab is `summary` AND `meeting.status === "completed"`.
- [x] 6.21 [P] Write `packages/web/src/routes/meetings/detail.test.tsx::test_workspace_tab_active_by_default_summary_disabled_when_not_completed` — RED test mounting detail page with meeting `status: "scheduled"`; assert workspace TabsContent is visible (3-column structure present); assert summary TabsTrigger has `disabled` attribute; assert SummaryPane is NOT in the DOM.
- [x] 6.22 [P] Write `packages/web/src/routes/meetings/detail.test.tsx::test_summary_tab_enabled_and_clickable_when_meeting_completed` — RED test with meeting `status: "completed"` (mock fetch for /summary too); click summary tab trigger; assert `summary-empty` (no row) or `summary-loading` (initial fetch) is rendered AND workspace 3-column structure is no longer the active panel.

## 7. i18n + agent doc + verification

- [x] 7.1 [P] Update `packages/web/src/locales/zh-TW.json` AND `packages/web/src/locales/en.json` (same commit, both files): add the following keys with the suggested values:
  - `meetings.detail.tabs.workspace` → `工作區` / `Workspace`
  - `meetings.detail.tabs.summary` → `摘要` / `Summary`
  - `meetings.summary.disabledHint` → `會議結束後可看摘要` / `Summary becomes available after the meeting ends`
  - `meetings.summary.heading` → `摘要` / `Summary`
  - `meetings.summary.generating` → `生成中…` / `Generating…`
  - `meetings.summary.generatingHint` → `（約 30-60 秒）` / `(about 30-60 seconds)`
  - `meetings.summary.generatedAt` → `生成於 {{at}}` / `Generated {{at}}`
  - `meetings.summary.regenerate` → `重新生成` / `Regenerate`
  - `meetings.summary.exportMd` → `匯出 .md` / `Export .md`
  - `meetings.summary.staleAlert` → `內容已更新，建議重新生成` / `Content has been updated; consider regenerating`
  - `meetings.summary.emptyState` → `尚無摘要` / `No summary yet`
  - `meetings.summary.generate` → `生成摘要` / `Generate summary`
  - `errors.summary.busy` → `上一個生成尚未完成` / `Previous generation still running`
  - `errors.summary.not_found` → `尚未生成摘要` / `Summary has not been generated`
  - `errors.summary.generation_failed` → `摘要生成失敗，請重試` / `Summary generation failed, please retry`
  - `errors.summary.generation_timeout` → `摘要生成逾時，請重試` / `Summary generation timed out, please retry`
- [x] 7.2 Run `cd packages/web && bun test src/locales/locales.test.ts`; assert deep-equal between `zh-TW.json` and `en.json` passes (the locale-drift guard).
- [x] 7.3 [P] Add `docs/agents/summarization.md` documenting the `MeetingSummarizer` Protocol + `VertexProSummarizer` impl, the 4-section prompt structure with required heading enumeration, the auto-trigger / fire-and-forget flow at WS finalize, the in-flight registry concurrency model with `_inflight` dict + `_lock`, the POST/GET endpoint contract (3 response shapes), the markdown export helper (File System Access + blob fallback), and the stale-judgement SQL (4-table COALESCE comparison). Cross-reference to `docs/agents/playbook-generation.md` for the Vertex 2.5 Pro pattern shared with slice 5.
- [x] 7.4 Run the full backend test suite: `cd packages/backend && uv run pytest` — all new summarization / migration / runtime / router-spawn tests pass alongside existing 258+.
- [x] 7.5 Run the full web test suite: `cd packages/web && bun test` — new summary-pane / summary-api / markdown-export / use-detail-tab / detail tab integration tests pass; pre-existing 8 baseline failures may remain (no NEW failures introduced).
- [x] 7.6 Run `cd packages/backend && uv run ruff check meeting_playbook tests` — no new lint violations introduced (pre-existing RUF unicode warnings tolerable).
- [x] 7.7 Manual smoke-test (Sean): start the dev stack; run `cd packages/backend && uv run alembic upgrade head` to apply 0006 migration; open a meeting → in_progress → speak / type a few exchanges + click Get Advice once → end the meeting; verify (a) within 30-60 seconds the Summary tab becomes clickable; (b) clicking Summary tab shows pending state then transitions to done with 4 markdown sections; (c) clicking 匯出 .md opens the native save dialog (Chrome) or downloads to default Downloads (other browsers) with filename pattern `{title}-{YYYY-MM-DD}.md`; (d) edit playbook → reload Summary tab → see stale alert above markdown; (e) click 重新生成 → enters pending → done with new content reflecting the updated playbook; (f) try POSTing manually while in-flight (via DevTools) → backend returns 409 summary.busy.
