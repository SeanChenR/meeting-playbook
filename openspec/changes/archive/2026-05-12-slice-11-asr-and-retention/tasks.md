## 1. MLX dep spike + Qwen3 model bring-up

- [x] 1.1 [Decision 8: MLX dep 選擇 — apply 階段 spike 確認] Spike Qwen3 ASR loader。**結果**：5 輪 MLX-based loaders（mlx-lm / mlx-whisper / mlx-vlm + custom helper）全部失敗於 `doggy8088/Qwen3-ASR-1.7B-MLX-8bit`；改用上游 `Qwen/Qwen3-ASR-1.7B` + 官方 `qwen-asr` PyPI package + `device_map="mps"` 一發成功。Trade-off: ~5 GB FP16 / instance（10 GB 兩 streams 仍在 18 GB 預算）。產出 `scripts/spike_qwen3_asr.py` 為 production loader 的可重跑對照範本。**驗證**：`uv run --project packages/backend python scripts/spike_qwen3_asr.py packages/backend/tests/asr/fixtures/short_speech_zh.wav` 印出非空中文 transcript（已驗證：`'你好，这是会议准备工具的测试录音。'`）；結論寫進 `qwen3_provider.py` module docstring。
- [x] 1.2 [Qwen3ASRProvider implements ASRProvider Protocol via MLX 8-bit model] 在 `packages/backend/meeting_playbook/asr/qwen3_provider.py` 新增 `Qwen3ASRProvider` 滿足 `asr/base.py::ASRProvider` Protocol（`transcribe_chunk(audio: np.ndarray, sample_rate: int) -> TranscriptChunk`），首次呼叫 lazy load model + warmup，後續呼叫重用同一個 instance。**驗證**：新增 `packages/backend/tests/asr/test_qwen3_provider.py` 至少四個案例：(a) Protocol 結構斷言（`isinstance(qwen3, ASRProvider)`）、(b) 第一次 `transcribe_chunk` 載 model 且 `warmup_loaded == True`、(c) 第二次呼叫不再 load、(d) 對 `tests/asr/fixtures/short_speech_zh.wav` 跑出非空 `text` 欄位。`pytest packages/backend/tests/asr/test_qwen3_provider.py -v` 全綠。

## 2. ASR factory + WS connect-time provider selection

- [x] 2.1 [P] [ASR factory selects provider per meeting at WebSocket connect time] 在 `packages/backend/meeting_playbook/asr/factory.py` 新增 `get_asr_providers_for_meeting(provider_name: str) -> tuple[ASRProvider, ASRProvider]`，內部用 `@lru_cache` per `(provider_name, stream)`；未知 provider name 走 warning log + Whisper fallback。**驗證**：新增 `tests/asr/test_factory.py` 涵蓋：(a) `"qwen3"` 回兩個 `Qwen3ASRProvider` 實例、(b) `"whisper"` 回兩個 `WhisperASRProvider` 實例、(c) 同一 `(name, stream)` 第二次呼叫回同一個 instance（lru_cache 行為）、(d) 未知 name 回 Whisper + 觸發 `caplog.records` 內 `unknown asr_provider` warning。
- [x] 2.2 [Decision 1: Provider 切換 timing — WS connect 時依 meeting.asr_provider 動態選] [WebSocket connect resolves ASR providers via factory using meeting.asr_provider] 在 `packages/backend/meeting_playbook/sessions/router.py::meeting_session_endpoint` 改用 `get_asr_providers_for_meeting(meeting.asr_provider)` 取代舊的 `Depends(get_whisper_singleton_*)`，刪除 `sessions/dependencies.py` 內的 `_whisper_singleton_me/counterparty` lru_cache。**驗證**：擴充 `tests/sessions/test_router.py` 加兩個 case：(a) meeting `asr_provider="qwen3"` connect 後 `SessionService` 拿到的 providers 都是 Qwen3 instance、(b) `asr_provider="whisper"` 拿到的是 Whisper instance。既有 WS shape 測試（`audio_chunk_*` → `transcript_chunk_*`）保持綠燈。
- [x] 2.3 [Switching meeting.asr_provider mid-session has no effect on the live WS] 確保 router handler 把 factory 結果存成 local var 後不再 re-read meeting row；新增 `tests/sessions/test_router.py::test_mid_session_provider_switch_ignored` 模擬：connect 時 meeting 是 whisper → 中途 PUT 改 qwen3 → 後續 audio frame 的 transcript 仍由 whisper 產出（透過 spy / monkeypatch provider.transcribe_chunk 驗證 call source）。

## 3. Meeting model + migration + recordings_available 派生欄位

- [x] 3.1 [Decision 2: `Meeting.asr_provider` default 改 qwen3，**不 back-fill 既有 row**] [New meetings default to qwen3 ASR provider; existing rows are not back-filled] 改 `packages/backend/meeting_playbook/meetings/models.py::Meeting.asr_provider` ORM default 從 `"whisper"` 變 `"qwen3"`；新增 Alembic migration `0008_meeting_asr_provider_default_qwen3` 只動 `ALTER COLUMN ... SET DEFAULT 'qwen3'`，**不**寫 UPDATE。**驗證**：(a) `pytest packages/backend/tests/meetings/test_create.py` 加一個 case：POST `/api/meetings` 不帶 `asr_provider` → response `asr_provider == "qwen3"`、(b) 新增 `tests/migrations/test_0008_default_only.py`：seed 一個 `asr_provider="whisper"` 的 row → 跑 migration → row 仍是 `whisper` + column default 是 `qwen3`（query `information_schema.columns`）。
- [x] 3.2 [P] [Meeting.asr_provider defaults to qwen3 for new meetings] 確認 spec scenario「Migration changes default but does not back-fill historical rows」由上一 task 的 migration test 涵蓋；同時 update `packages/backend/meeting_playbook/meetings/schemas.py` 讓 `MeetingCreateRequest.asr_provider` Field default 也改 `"qwen3"`（讓 OpenAPI 文件對齊）。**驗證**：`pytest tests/meetings/test_schemas.py` 全綠。
- [x] 3.3 [GET /api/meetings/{id} returns recordings_available and rerun_asr_pending derived fields] 在 `packages/backend/meeting_playbook/meetings/repository.py` 加 `get_meeting_with_runtime_flags(meeting_id) -> MeetingWithFlags`，回傳 dict 含 `recordings_available`（query：`SELECT EXISTS (SELECT 1 FROM recording WHERE meeting_id=? AND deleted_at IS NULL)`）+ `rerun_asr_pending`（call `rerun.runtime.is_pending(meeting_id)`）。GET `/api/meetings/{id}` response model 加這兩個 required boolean 欄位。**驗證**：`tests/meetings/test_get_runtime_flags.py` 涵蓋 spec 4 scenarios（completed+available true / all-deleted false / scheduled no-rows false / pending flips true→false）。

## 4. recording.deleted_at + retention job + lifespan

- [x] 4.1 [Decision 5: `recording.deleted_at` schema + cleanup 不刪 row] [recording.deleted_at column tracks soft-deletion timestamp] 新增 Alembic migration `0007_add_recording_deleted_at` 加 `deleted_at TIMESTAMPTZ NULL` 到 `recording` 表（無 index、無 backfill）；對應 update `packages/backend/meeting_playbook/recordings/models.py::Recording` ORM 加 `deleted_at: Mapped[datetime | None]`。**驗證**：`tests/migrations/test_0007_add_deleted_at.py` 兩個 case：(a) migration 後 `information_schema.columns` 顯示 `deleted_at` `is_nullable = YES` + type `timestamp with time zone`、(b) seed 5 個 pre-migration row → migration 後全部 `deleted_at IS NULL` + 全部仍存在。
- [x] 4.2 [RecordingRetentionJob.cleanup deletes WAV files older than threshold] 新增 `packages/backend/meeting_playbook/retention/job.py` export `cleanup(now, *, retention_days, recordings_dir) -> int`，行為：select where `created_at < now - retention_days AND deleted_at IS NULL` → 對每 row 嘗試 `Path(rec.file_path).unlink()`（FileNotFoundError 視為 self-heal 仍設 `deleted_at`）；OSError 跳過 + log warning + 不動 `deleted_at`；單 transaction commit；返回實際 unlink 或 self-heal 的 count。**驗證**：`tests/retention/test_job.py` 涵蓋 spec 三個 scenario：(a) 31-day file 刪 + 5-day file 留 + 60-day-already-missing self-heal、(b) 連跑兩次第二次 zero deletions（idempotent）、(c) cleanup 後同 meeting 的 `transcript_chunk` / `chat_message` row count 不變。
- [x] 4.3 [Decision 6: Cleanup job 用簡單 `asyncio.sleep(24h)` 迴圈 + FastAPI lifespan] [Retention job runs every 24 hours via FastAPI lifespan] 新增 `packages/backend/meeting_playbook/retention/runtime.py::run_forever(*, settings)`：infinite loop call `cleanup` → `await asyncio.sleep(24 * 3600)`；CancelledError re-raise，其他 Exception `logger.exception` + 繼續迴圈；改 `packages/backend/meeting_playbook/server.py` 加 `@asynccontextmanager async def lifespan(app)` spawn task on startup + cancel on shutdown（`contextlib.suppress(CancelledError)`）；FastAPI 改 `app = FastAPI(lifespan=lifespan, ...)`。**驗證**：`tests/retention/test_runtime.py` 涵蓋四個 scenario：(a) startup 1s 內 cleanup 被呼叫一次（用 monkeypatch + Event）、(b) sleep mock 讓兩 iteration 都跑、(c) 第一次 raise RuntimeError 第二次成功 → loop 仍活、(d) lifespan 結束時 cancel + await 不 hang。
- [x] 4.4 [P] [RECORDING_RETENTION_DAYS configuration with 30-day default] 在 `packages/backend/meeting_playbook/config.py::Settings` 加 `recording_retention_days: int = 30`（pydantic-settings env `RECORDING_RETENTION_DAYS`）；`.env.example` 加 commented-out 行 + 引用 ADR-0020。**驗證**：(a) `tests/test_config.py::test_recording_retention_days_default` 預設 30、(b) `monkeypatch.setenv("RECORDING_RETENTION_DAYS", "7")` 後 `Settings()` 拿 7、(c) `0` 合法且 cleanup 視為「now 之前的全砍」（搭配 4.2 場景三）。

## 5. Re-run runtime + endpoints

- [x] 5.1 [P] [Decision 3: Re-run 採 in-flight registry + 原子 DELETE+INSERT 替換] Re-run in-flight registry：新增 `packages/backend/meeting_playbook/rerun/runtime.py` 含 `_inflight: dict[str, asyncio.Task]` + `_progress: dict[str, ChunksProgress]` + `asyncio.Lock` + `spawn_rerun_task(meeting_id, *, providers, session_factory) -> bool` + `is_pending(meeting_id) -> bool` + `get_status(meeting_id) -> dict`。背景 task：開新 AsyncSession → load meeting + 兩個 recording → 每個 wav chunk 化 → 估算 total → 對每 chunk 用對應 stream provider 跑 → 蒐集到 list + progress.processed += 1 → 全部成功單一 transaction `DELETE WHERE meeting_id=?` + `INSERT RETURNING` + commit；exception 跳出不動既有 row；finally pop registry。**驗證**：`tests/rerun/test_runtime.py` 涵蓋：(a) concurrent spawn first True second False（同 slice-10 pattern）、(b) 成功路徑跑完 transcript_chunk row count == fake provider 產出 chunk 數 + registry 清空、(c) 任何 chunk raise 後既有 row 數量保持原狀 + 新 row 數 = 0、(d) `get_status` 回 `{status: "pending", processed, total}` 跟 `{status: "idle", 0, 0}`。
- [x] 5.2 [POST /api/meetings/{id}/rerun_asr triggers atomic transcript replacement] 在 `packages/backend/meeting_playbook/meetings/router.py` 加 POST `/api/meetings/{id}/rerun_asr`：驗 ownership（`X-User-Id`）→ 驗 status == completed（否則 422 `rerun.not_completed`）→ 驗 `recordings_available`（否則 410 `rerun.recording_expired`）→ call `spawn_rerun_task` → True → 202 `{status: "pending"}`；False → 409 `rerun.busy`。404 走既有 `meeting.not_found` 流程。**驗證**：`tests/rerun/test_endpoint_post.py` 五 case：202 / 409 / 410 / 422 / 404；envelope `{error_code, message}` 結構驗證。
- [x] 5.3 [GET /api/meetings/{id}/rerun_asr_status returns polling-friendly progress shape] GET `/api/meetings/{id}/rerun_asr_status` 直接呼叫 `runtime.get_status(meeting_id)` → 200 `{status, chunks_processed, chunks_total}`；非 owner / 不存在 404。**驗證**：`tests/rerun/test_endpoint_status.py` 三 case：(a) 未 spawn → idle 0/0、(b) spawn 中 → pending 帶 progress、(c) 完成後 → 回 idle。

## 6. Frontend：selector / badge / button + transcript overlay + i18n

- [x] 6.1 [Decision 7: UI 三件 + Recording badge 用色彩 dot 不用 emoji] [Detail page metadata card exposes ASR provider selector, recording badge, and re-run action] 在 `packages/web/src/routes/meetings/detail.tsx` 把 metadata 卡片內既有「ASR 引擎」row 換成新 `AsrProviderSelector` 元件（在 `packages/web/src/components/asr-provider-selector.tsx` 新增）— shadcn select dropdown、`onValueChange` PUT `/api/meetings/{id}` body `{asr_provider}`、底下顯示 `meetings.detail.asrProviderSwitchHint` localised 文字；新增 `RecordingBadge`（`recording-badge.tsx`，`data-testid="recording-badge"` + `data-state="available|expired"` + Tailwind 色 dot）；新增 `RerunButton`（`rerun-button.tsx`，gating: `status==='completed' && recordings_available && !rerun_asr_pending`，click 走 React Query mutation POST endpoint）。**驗證**：`packages/web/src/components/__tests__/asr-provider-selector.test.tsx` + `recording-badge.test.tsx` + `rerun-button.test.tsx` 共 7 case 涵蓋 spec scenarios（dropdown 反映現值 / 切換 PUT / badge available 綠 dot / badge expired 灰 dot / button hidden in_progress / button hidden expired / button click loading state）。
- [x] 6.2 [Decision 4: HTTP polling for re-run progress] [TranscriptPane shows progress overlay while re-run is pending] 改 `packages/web/src/components/transcript-pane.tsx` 在 `meeting.rerun_asr_pending === true || rerunStatus.status === "pending"` 時 render `data-testid="transcript-rerun-overlay"` 含 localised `meetings.transcript.rerunOverlay` 文字 + `({chunks_processed}/{chunks_total} chunks)` 計數器（total === 0 時顯示 `(...)`）；新增 `packages/web/src/lib/rerun-api.ts` 提供 `useRerunStatus(meetingId)` hook（React Query polling，`refetchInterval: (q) => q.state.data?.status === "pending" ? 1000 : false`）；status 從 pending 轉 idle 時 invalidate `["transcripts", meetingId]`。**驗證**：`packages/web/src/components/__tests__/transcript-pane-rerun.test.tsx` 兩 case：pending 時 overlay 可見 + 含「12/45 chunks」字串；transition 後 overlay 消失 + invalidate 被呼叫。
- [x] 6.3 [P] i18n parity：在 `packages/web/src/locales/zh-TW.json` 跟 `en.json` 同步加 `meetings.detail.asrProviderSwitchHint`（zh：「切換下一場會議生效」/ en：「Takes effect on the next meeting」）、`meetings.detail.recordingLabel`、`meetings.detail.recordingAvailable`、`meetings.detail.recordingExpired`、`meetings.detail.rerunButton`、`meetings.transcript.rerunOverlay`、`errors.rerun.busy`、`errors.rerun.recording_expired`、`errors.rerun.not_completed`。**驗證**：`packages/web/src/locales/locales.test.ts` deep-equal guard 過。

## 7. Acceptance verification

- [x] 7.1 後端 `pytest packages/backend` 全綠（含新 `tests/asr/test_qwen3_provider.py` / `tests/asr/test_factory.py` / `tests/sessions/test_router.py` / `tests/meetings/test_get_runtime_flags.py` / `tests/migrations/test_0007_*.py` / `test_0008_*.py` / `tests/retention/test_job.py` / `test_runtime.py` / `tests/rerun/test_runtime.py` / `test_endpoint_post.py` / `test_endpoint_status.py` / `tests/test_config.py`）；前端 `bun test packages/web` 無新失敗。**驗證**：兩個 test 指令 exit code 0（baseline 既有失敗保留）。
- [ ] 7.2 [Decision 9: Re-run 進行中時 summary trigger / regenerate 行為] [Implementation Contract: Behavior contract / Data shapes / Failure modes / Acceptance criteria / Scope boundaries (in / out)] Manual smoke-test (Sean) — 跑 design.md `### Acceptance criteria` 段內描述的完整劇本，過程中需驗證 behavior contract 行為符合 design + data shapes 回應欄位齊全 + failure modes 在錯誤情境不留 partial state + scope boundaries (in / out) 沒有越界做出 design 標記為 out 的事項（new meeting Qwen3 中文 → end → 切 selector → 下一場 Whisper → re-run 老 meeting → `RECORDING_RETENTION_DAYS=0` 重啟 → cleanup 立即 → badge 變 expired + button 消失）。**驗證**：Sean 確認劇本每一步通過後在 GitHub Issue #13 + Issue #14 把 acceptance criteria 打勾，並回報「OK」才算完成。
- [ ] 7.3 Verify all acceptance criteria from the agent brief are checked off in the GitHub Issue (#13 ASR provider selection + #14 recording retention)。
