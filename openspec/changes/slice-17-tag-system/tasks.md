## 1. Schema 與 palette 落地

- [x] 1.1 [P] 新增 Alembic migration `packages/backend/alembic/versions/XXXX_tag_system.py`：建立 `tag` 與 `meeting_tag` 表（columns 依設計決策「Schema：`tag` + `meeting_tag` 雙表，case-insensitive uniqueness 用 expression index」），含 `(user_id, lower(name))` expression unique index、FK CASCADE。**Behavior**：upgrade 後 `psql -c "\d tag"` 可看到 schema + index、`\d meeting_tag` 可看到複合主鍵；downgrade 後兩表消失。**Verification**：新測 `packages/backend/tests/test_alembic_tag_system.py` 跑 upgrade → downgrade → upgrade 三輪驗證可逆，落實 `tag table stores per-user tag definitions with case-insensitive uniqueness` 與 `meeting_tag junction table associates meetings with tags` 兩個 spec requirements。
- [x] 1.2 [P] 在 `packages/web/src/lib/tag-palette.ts` 與 `packages/backend/meeting_playbook/tags/colors.py` 同步維護一份 8 色 hex 白名單（依設計決策「Color palette：preset 8 色，存 hex string，前後端共用 enum」），並在前端 export `pickReadableTextColor(bg) -> 'light' | 'dark'`。**Behavior**：兩處白名單字串完全相同；前端 helper 對任意 palette 色都能回 light/dark 標籤、light 色 chip 渲染深字、深色 chip 渲染亮字。**Verification**：新測 `packages/web/src/lib/tag-palette.test.ts` 跑 WCAG AA contrast (≥ 4.5:1) 對每個 palette 色逐一斷言；新測 `packages/backend/tests/tags/test_palette_parity.py` 讀兩個檔案比對白名單長度與內容相等。

## 2. TagRepository 走 TDD

- [x] 2.1 在 `packages/backend/meeting_playbook/tags/models.py` 定義 SQLAlchemy `Tag` 與 `MeetingTag` model 對應 1.1 的 schema，並在既有 `Meeting` model 加 `tags: list[Tag]` relationship。**Behavior**：`Meeting.tags` 透過 `meeting_tag` junction 多對多取得 `Tag` 物件，且 relationship 預設不會自動觸發 lazy load。**Verification**：新測 `packages/backend/tests/tags/test_models.py` 兩條斷言：(a) `Tag(__tablename__) == "tag"` 且欄位齊全；(b) `Meeting.tags` 走 secondary `meeting_tag`。
- [x] 2.2 在 `packages/backend/meeting_playbook/tags/repository.py` 實作 `TagRepository`，並對應落實 `TagRepository enforces case-insensitive uniqueness, cascade-on-delete, and per-meeting limit` requirement、設計決策「Repository 的 cascade 行為走 DB FK，不在 application 層手動 delete」與「單一 meeting ≤ 10 tags：DB CHECK 還是 application enforce？」。**Behavior**：`create / update` trim name + 將重複 `(user_id, lower(name))` 轉成 `TagNameTaken`；`delete` 只發單一 `DELETE FROM tag ...` 並依靠 FK cascade；`attach` 在 transaction 內 `FOR UPDATE` lock + reject 第 11 個 tag 並 raise `TagLimitExceeded(current_count)`；duplicate (meeting, tag) 視為 idempotent。**Verification**：新測 `packages/backend/tests/tags/test_repository.py` 走 red-first，覆蓋 5 條 scenario — (a) case-insensitive duplicate create raises `TagNameTaken`、(b) different users 同名互不衝突、(c) delete 後 `meeting_tag` rows 為 0（cascade 生效）、(d) 第 11 個 attach raises `TagLimitExceeded(current_count=10)`、(e) duplicate attach idempotent 不增加 row 數；對應 `tag-system` spec 三個 requirement 的全部 scenarios。

## 3. Tag CRUD API

- [x] 3.1 在 `packages/backend/meeting_playbook/tags/router.py` 實作 `GET /api/tags`，支援 `with_meeting_count=true` query。**Behavior**：回 user-owned tags 陣列、`with_meeting_count=true` 時每筆加 `meeting_count`（單一 query JOIN + GROUP BY，不 N+1）；items 依 `created_at` 升序排序。**Verification**：新測 `packages/backend/tests/tags/test_router.py` 三條 — (a) user A 只看到自己的 tag、不看 user B 的、(b) `with_meeting_count=true` 回的 `meeting_count` 等於 `meeting_tag` 實際數、(c) 缺 query 則 response item 不含 `meeting_count` key；落實 `GET /api/tags lists per-user tags with optional meeting_count` requirement 全部 scenarios。
- [x] 3.2 在 `packages/backend/meeting_playbook/tags/router.py` 補 `POST / PATCH / DELETE /api/tags(/{tag_id})` 三個 endpoint。**Behavior**：成功時 201 / 200 / 204；name 重複回 422 `tag.name_taken`、color 不在 palette 回 422 `tag.invalid_color`、name 空白回 422 `tag.name_required`；操作別人 tag 一律 HTTP 404 不洩漏存在；DELETE 即使 tag 已掛在 meeting 上仍會 cascade 清 junction。**Verification**：擴 `test_router.py` 五條 — (a) POST 422 name_taken、(b) POST 422 invalid_color、(c) POST 422 name_required、(d) PATCH 別人的 tag 404、(e) DELETE 後 `meeting_tag` 中該 tag 的 rows 全部消失；落實 `POST / PATCH / DELETE /api/tags manage a single tag with strict ownership and validation` requirement 全部 scenarios。
- [x] 3.3 在 `packages/backend/meeting_playbook/tags/router.py` 加 `POST /api/meetings/{meeting_id}/tags` 與 `DELETE /api/meetings/{meeting_id}/tags/{tag_id}`，在 `packages/backend/meeting_playbook/server.py` 註冊 `tag_router`。**Behavior**：attach 第 11 個回 422 `tag.too_many_for_meeting` 且 message 含 `current_count=10`；attach 別人 meeting 或 tag 一律 404；DELETE 對不存在 pair 仍回 204 (idempotent)。**Verification**：擴 `test_router.py` 三條 — (a) 第 11 attach 回 422 + message 含 "10"、(b) detach 不存在 pair 回 204 且 row 數不變、(c) attach 跨使用者 tag 回 404；落實 `POST / DELETE /api/meetings/{id}/tags attach and detach tags with per-meeting limit` requirement 全部 scenarios。

## 4. 既有 meeting endpoints 整合 tags + ?tag_ids 過濾

- [x] 4.1 擴 `packages/backend/meeting_playbook/meetings/repository.py` 的 `list_for_user(user_id, tag_ids=None)` 與 `get_for_user`，依設計決策「既有 meeting payload 怎麼加 `tags` array：N+1 prevention」用 `selectinload(Meeting.tags)`；`?tag_ids=` 過濾用設計決策「`?tag_ids=` AND 過濾語意」的 `HAVING COUNT(DISTINCT tag_id) = :n` 子查詢。**Behavior**：list / detail payload 都含 `tags: [{id, name, color}]`；空 `tag_ids` no-op；含未授權 tag id 在 router 層 raise 422 `tag.unknown_id`；list N 筆 meeting 時對 `tag / meeting_tag` 的 SELECT 次數 ≤ 2。**Verification**：擴 `packages/backend/tests/test_meetings_router.py` 五條 — (a) list response item 含 `tags` array、(b) AND 過濾正確（三 meeting fixture 驗 m_1 only）、(c) tag_ids 含他人 tag 回 422 unknown_id、(d) 空 tag_ids 等同無 filter、(e) 50-meeting fixture 用 query log 斷言 SELECT 數 ≤ 2；落實 `Meeting list is sorted newest-first by creation time` MODIFIED requirement 五個新 scenarios。
- [x] 4.2 在 `packages/backend/meeting_playbook/meetings/router.py` 的 detail endpoint 確保 `tags` 走同一個 selectinload 路徑。**Behavior**：detail 回 `tags: [...]`，無 tag 時回 `tags: []`，順序依 `attached_at` 升序。**Verification**：擴 `test_meetings_router.py` 兩條 — (a) 有 2 個 tag 的 meeting detail 回完整 array 順序對；(b) 0 tag 的 meeting detail 回 `tags: []`；落實 `GET /api/meetings/{id} returns recordings_available and rerun_asr_pending derived fields` MODIFIED requirement 內新增的兩個 tags 相關 scenarios。

## 5. 前端三大元件

- [x] 5.1 在 `packages/web/src/lib/tags-api.ts` 實作 API client wrapper（`listTags / createTag / updateTag / deleteTag / attachTag / detachTag`），錯誤 throw `ApiError(error_code, message)`。**Behavior**：每個 helper 對應 backend endpoint，呼叫成功 resolve 對應 payload；422 / 404 都會 throw 含 `error_code` 的 `ApiError`，caller 可走 `localizedErrorMessage(error_code, t)` 渲染。**Verification**：新測 `packages/web/src/lib/tags-api.test.ts` 走 mocked fetch 三條 — (a) 成功路徑 resolve、(b) 422 throw `ApiError` 且 `.error_code === "tag.name_taken"`、(c) 404 throw `ApiError` 且 `.error_code === "tag.not_found"`。
- [x] 5.2 [P] 在 `packages/web/src/components/tags/tag-chip.tsx` 實作 `<TagChip>`（依設計決策「`<TagChip>` / `<TagPicker>` / `<TagFilter>` 三個元件分工」display-only 分工）。**Behavior**：props `{name, color, onRemove?, size?}` 渲染 chip；用 `pickReadableTextColor` 決定文字色；size sm/md 對應不同 padding；onRemove 存在時顯示 X 按鈕。**Verification**：新測 `packages/web/src/components/tags/tag-chip.test.tsx` 三條 — (a) light 色背景 → 深字 class、(b) 深色背景 → 亮字 class、(c) `onRemove` 點擊觸發 callback；落實 `TagChip, TagPicker, and TagFilter components render tags in all meeting views` requirement 的 `TagChip computes readable text color from background` scenario。
- [x] 5.3 [P] 在 `packages/web/src/components/tags/tag-picker.tsx` 實作 `<TagPicker>` 與 `packages/web/src/components/tags/tag-filter.tsx` 實作 `<TagFilter>`，並依設計決策「`<TagFilter>` URL state：用 search param 不用 path」把 filter state 落在 URL `?tag_ids=`。**Behavior**：TagPicker 開啟 popover 顯示 Command search + inline-create row（query 不在既有 tag 時才出現）；inline-create 串接 `createTag → attachTag`；已掛 tag 點擊 detach。TagFilter 多選 checkbox 寫回 URL search param，trigger label 顯示 "標籤 (N)" / "Tags (N)"；reload 後保留 selection。**Verification**：新測 `packages/web/src/components/tags/tag-picker.test.tsx` 與 `tag-filter.test.tsx` 共四條 — (a) picker inline-create 串接兩個 API；(b) picker click 已掛 tag 觸發 detach；(c) filter 多選後 URL param 更新；(d) filter trigger label 含 selected count；落實同 requirement 剩下 3 個 scenarios（inline-create chains, URL search param, selected count）。

## 6. 三視圖 + /settings/tags + i18n

- [x] 6.1 把 `<TagChip>` 整合進 `packages/web/src/routes/meetings/list.tsx`、`kanban.tsx`、`calendar.tsx`、`detail.tsx` 顯示 meeting tags；把 `<TagFilter>` 掛到 list / kanban / calendar 三視圖共用列；把 `<TagPicker>` 掛到 detail page header。**Behavior**：三視圖 row / card / pill 看得到 tag chips、頂端共用 filter 區可多選 + 重新整理保留；detail 進 picker 可 attach / detach 直接反映；切換 view tabs 時 URL 同時保留 `view` 與 `tag_ids`。**Verification**：擴 `packages/web/src/routes/meetings/list.test.tsx`、`kanban.test.tsx`、`calendar.test.tsx`、`detail.test.tsx` 各加一條 component test 渲染含 tags 的 meeting fixture 並斷言 chip 出現；新測 `packages/web/src/routes/meetings/tag-filter-roundtrip.test.tsx` 斷言 view tab 切換不清掉 `tag_ids`。
- [x] 6.2 在 `packages/web/src/routes/settings/tags.tsx` 實作管理頁，依設計決策「Hard delete 確認文案「將從 N 個會議移除」」用 `with_meeting_count=true` 拿 meeting_count；delete dialog 用 ICU plural（en）渲染動態文案；inline rename + recolor swatch（preset palette only）。在 `packages/web/src/route-tree.tsx` 註冊 `/settings/tags`。**Behavior**：列表渲染所有 tags + 各自 meeting_count；rename 422 `tag.name_taken` 顯示本地化錯誤且 input 保留 focus；recolor swatch 只列 palette；delete confirmation 描述含正確 count（en 走 ICU plural、zh-TW 走單一形式）。**Verification**：新測 `packages/web/src/routes/settings/tags.test.tsx` 四條 — (a) delete dialog 描述含 `meeting_count`；(b) rename 422 顯示 localized error 且 input 仍可編輯；(c) swatch 只顯示 palette 色；(d) 成功 delete 後該 row 從 list 消失；落實 `/settings/tags page provides CRUD with dynamic delete confirmation` requirement 全部 scenarios + example table。
- [x] 6.3 [P] 在 `packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json` 同步加入 tags + settings.tags + errors.tag 命名空間（依 design.md Implementation Contract 列出的所有 keys），en 的 `settings.tags.deleteConfirm.description` 用 ICU plural；更新 `CONTEXT.md` glossary 加「Tag (標籤)」條目。**Behavior**：兩 locale 檔 key set 完全相同；ICU plural 字串符合 i18next 格式。**Verification**：跑 `bun --filter @meeting-playbook/web test packages/web/src/locales/locales.test.ts` deep-equal 通過；`grep -F "Tag (標籤)" CONTEXT.md` 命中；新測 `packages/web/src/lib/i18n-plural.test.ts` 對 en `settings.tags.deleteConfirm.description` 餵 `{count: 1}` 與 `{count: 5}` 驗證輸出含 `"1 meeting"` 與 `"5 meetings"`。

## 7. 收尾

- [x] 7.1 跑覆蓋率 `cd packages/backend && uv run pytest --cov=meeting_playbook.tags --cov=meeting_playbook.meetings --cov-report=term-missing` 與 `bun --filter @meeting-playbook/web test --coverage`，並逐項勾掉 GitHub Issue #20 acceptance criteria。**Behavior**：`meeting_playbook/tags/*` 覆蓋率 ≥ 80%；既有 `meetings/router.py` 在本 slice 新加分支 ≥ 80%；前端 tags 元件 ≥ 80%。**Verification**：附上 coverage 報表截圖或 stdout 貼到 PR；GitHub Issue #20 內 8 個 acceptance criteria checkbox 全勾選；issue 連結：https://github.com/SeanChenR/meeting-playbook/issues/20。
