## 0. Coverage map (analyzer aid — links task groups to spec requirements + design decisions)

| Task group | tactical-advisor spec requirement(s)          | meeting-session spec requirement(s)        | meeting-detail-layout spec requirement(s) | design.md decision(s) |
| ---------- | --------------------------------------------- | ------------------------------------------ | ----------------------------------------- | --------------------- |
| 1          | chat_message table + ChatMessageRepository    | —                                          | —                                         | Decision 1 |
| 2          | GET /chat_messages                             | —                                          | —                                         | Decision 6 |
| 3          | chat_history in advise() + prompt section      | —                                          | —                                         | Decision 5 |
| 4          | advise stream success persists pair            | chat_message client frame; persists pair   | —                                         | Decision 2, 3, 4, 7 |
| 5          | AdvisorPane chat history rendering             | —                                          | hydration via React Query                 | Decision 6, 8 |
| 6          | (i18n + agent doc + verification)              | —                                          | —                                         | — |

## 1. chat_message table + ChatMessageRepository

- [x] 1.1 [P] Write `packages/backend/tests/test_alembic_chat_message.py::test_upgrade_creates_table_with_columns_and_index` — RED test that runs the new Alembic migration on the test DB and asserts the `chat_message` table exists with columns (`id`, `meeting_id`, `role`, `content`, `created_at`), the FK constraint to `meeting.id` with `ON DELETE CASCADE`, the CHECK constraint on `role`, and the composite index `chat_message_meeting_created_idx`. Use the same `_migrated_db_url` fixture and `alembic upgrade/downgrade` round-trip pattern as `test_alembic_meeting.py`.
- [x] 1.2 Add `packages/backend/alembic/versions/0005_create_chat_message.py` with `revision="0005_create_chat_message"` and `down_revision="0004_add_meeting_scheduled_times"`. The `upgrade()` function SHALL create the `chat_message` table per Decision 1's exact schema (5 columns, CHECK on role, FK CASCADE to meeting), then create the composite index `chat_message_meeting_created_idx` on `(meeting_id, created_at)`. The `downgrade()` function SHALL drop the index then the table. Drive 1.1 GREEN.
- [x] 1.3 [P] Add `packages/backend/meeting_playbook/chat/__init__.py` (empty package marker).
- [x] 1.4 [P] Add `packages/backend/meeting_playbook/chat/models.py` exporting `ChatMessage` SQLAlchemy ORM mapping the new table; columns mirror the migration. Use the same Mapped/mapped_column pattern as `sessions/models.py::TranscriptChunk`.
- [x] 1.5 [P] Write `packages/backend/tests/chat/__init__.py` (empty) AND `packages/backend/tests/chat/test_repository.py::test_insert_pair_after_advice_writes_two_rows_atomically` — RED test seeding a meeting then calling `repo.insert_pair_after_advice(meeting_id, user_content="Q", advisor_content="A")`; asserts two rows exist with correct roles + contents + same meeting_id; asserts user `created_at` <= advisor `created_at`.
- [x] 1.6 [P] Write `packages/backend/tests/chat/test_repository.py::test_list_for_meeting_returns_chronological_order` — RED test inserting 5 rows out of `created_at` order then asserting `repo.list_for_meeting(meeting_id)` returns them ascending.
- [x] 1.7 [P] Write `packages/backend/tests/chat/test_repository.py::test_cascade_delete_on_meeting_removal` — RED test inserting a meeting + 3 chat_message rows; deleting the meeting; asserting all 3 chat_message rows are gone.
- [x] 1.8 Add `packages/backend/meeting_playbook/chat/repository.py` exporting `ChatMessageRepository(session)` with two methods: `list_for_meeting(meeting_id) -> list[ChatMessage]` (SELECT ordered by `created_at` ASC) and `insert_pair_after_advice(meeting_id, user_content, advisor_content) -> tuple[ChatMessage, ChatMessage]` (creates two `ChatMessage` instances with `id=cm_<token_urlsafe(16)>`, `created_at=datetime.now(UTC)`; user_content row's `created_at` SHALL be set strictly earlier than advisor_content row's, then `session.add_all([user, advisor])` and `await session.commit()`). The repository SHALL NOT expose any update or delete method. Drive 1.5/1.6/1.7 GREEN.

## 2. GET /api/meetings/{id}/chat_messages

- [x] 2.1 [P] Write `packages/backend/tests/chat/test_router.py::test_get_chat_messages_owner_returns_chronological_history` — RED test seeding a meeting belonging to user `u_a` with three chat_message rows; calling GET `/api/meetings/{meeting_id}/chat_messages` with header `X-User-Id: u_a`; asserting HTTP 200 and JSON array of three objects ordered by `created_at` ASC, each with keys `id`, `meeting_id`, `role`, `content`, `created_at`.
- [x] 2.2 [P] Write `packages/backend/tests/chat/test_router.py::test_get_chat_messages_non_owner_returns_404` — RED test with meeting belonging to `u_a` but request as `u_b`; asserts HTTP 404 with body `{"detail": {"error_code": "meeting.not_found", ...}}`.
- [x] 2.3 [P] Write `packages/backend/tests/chat/test_router.py::test_get_chat_messages_empty_history_returns_empty_array` — RED test with a meeting that has zero chat_message rows; asserts HTTP 200 and body `[]`.
- [x] 2.4 Add `packages/backend/meeting_playbook/chat/router.py` exporting `router = APIRouter(tags=["chat"])` with the GET endpoint at `/api/meetings/{meeting_id}/chat_messages`. Inject `user_id = Depends(get_user_id_dependency)` and `session = Depends(get_session_dependency)`. Use `MeetingRepository(session).get_for_user(user_id, meeting_id)` to verify ownership; raise `HTTPException(404, {"error_code": "meeting.not_found", "message": "Meeting not found"})` on miss. On success, call `ChatMessageRepository(session).list_for_meeting(meeting_id)` and return a list of dicts with `id`, `meeting_id`, `role`, `content`, `created_at` (ISO-8601). Drive 2.1/2.2/2.3 GREEN.
- [x] 2.5 Update `packages/backend/meeting_playbook/server.py::create_app` — `app.include_router(chat.router.router)` so the new endpoint mounts under the same FastAPI app as `sessions.router`. Add an integration smoke check (re-run 2.1 after this) so the endpoint is reachable from `TestClient(app)`.

## 3. chat_history parameter on TacticalAdvisor + prompt section

- [x] 3.1 [P] Write `packages/backend/tests/advisor/test_prompts.py::test_chat_history_section_omitted_when_empty` — RED test calling `build_user_message(playbook=<non-empty>, recent_chunks=<non-empty>, ..., chat_history=[], locale="zh-TW")`; asserts the returned string does NOT contain the substring `## 對話紀錄` and does NOT contain `## Chat history`.
- [x] 3.2 [P] Write `packages/backend/tests/advisor/test_prompts.py::test_chat_history_section_renders_in_order_with_role_labels` — RED test feeding 4 ChatMessage rows in order (user "問題A", advisor "回答A", user "問題B", advisor "回答B") with `me_display_name="Sean"`; asserts the returned string contains a `## 對話紀錄` section with the four lines `Sean: 問題A`, `Advisor: 回答A`, `Sean: 問題B`, `Advisor: 回答B` in that order.
- [x] 3.3 [P] Write `packages/backend/tests/advisor/test_prompts.py::test_chat_history_section_sits_between_transcript_and_question` — RED test with non-empty playbook + non-empty chunks + non-empty chat_history + `user_question="新問題"`; asserts the returned string contains the four section headings/lines in the order: `## 會議 playbook`, `## 最近 60 秒對話`, `## 對話紀錄`, then a line containing `"新問題"`.
- [x] 3.4 [P] Write `packages/backend/tests/advisor/test_prompts.py::test_chat_history_uses_advisor_label_not_counterparty_name` — RED test with `me_display_name="Sean"` and `counterparty_display_name="林經理"`; asserts the chat_history advisor lines are prefixed with literal `Advisor:` (not `林經理:`).
- [x] 3.5 Update `packages/backend/meeting_playbook/advisor/prompts.py` — add `_format_chat_history(messages: Sequence[ChatMessage], me_display_name: str, locale: Locale) -> str` returning a string starting with `## 對話紀錄` (zh-TW) or `## Chat history` (en) followed by one line per message, formatted as `{me_display_name}: {content}` for `role='user'` and `Advisor: {content}` for `role='advisor'`, joined by `\n`. Returns empty string when `messages` is empty. Then update `build_user_message(...)` to accept a new `chat_history: Sequence[ChatMessage]` keyword argument; insert `_format_chat_history(...)` output between the dialogue section and the question line ONLY when the formatter returns a non-empty string. Drive 3.1-3.4 GREEN.
- [x] 3.6 [P] Write `packages/backend/tests/advisor/test_vertex_advisor.py::test_advise_passes_chat_history_to_prompt_builder` — RED test mocking the SDK and the prompt builder; asserts that calling `advisor.advise(..., chat_history=<2 rows>)` invokes `build_user_message` with `chat_history=<the same 2 rows>`.
- [x] 3.7 Update `packages/backend/meeting_playbook/advisor/base.py::TacticalAdvisor.advise` Protocol signature — add `chat_history: list[ChatMessage]` keyword parameter (positioned at the end, before `locale`). Update `vertex_advisor.py::VertexFlashAdvisor.advise` impl to accept it and pass through to `build_user_message`. Drive 3.6 GREEN.

## 4. Router orchestration: chat_message frame + INSERT pair on success + cancel-previous + button path

- [x] 4.1 [P] Write `packages/backend/tests/sessions/test_messages.py::test_chat_message_frame_validates` — RED test asserting `ChatMessageRequestMessage(request_id="r1", content="hi", locale="zh-TW")` round-trips through `parse_client_message`; asserts `parse_client_message` rejects `{"type": "chat_message", "request_id": "r1", "content": "", "locale": "zh-TW"}` with `ValidationError` (empty content not allowed).
- [x] 4.2 Update `packages/backend/meeting_playbook/sessions/messages.py` — add `ChatMessageRequestMessage` Pydantic class with `type: Literal["chat_message"]`, `request_id: str`, `content: str = Field(min_length=1)`, `locale: Literal["zh-TW", "en"]`, and `model_config = {"extra": "forbid"}`. Add it to the `ClientMessage` discriminated union. Drive 4.1 GREEN.
- [x] 4.3 [P] Write `packages/backend/tests/sessions/test_router_advice.py::test_chat_message_frame_streams_advice_and_inserts_pair_on_done` — RED integration test with mock advisor yielding 3 strings; client sends `chat_message {request_id, content="X", locale="zh-TW"}`; asserts WS receives 3 `advice_chunk` then `advice_done`; after `advice_done` the test queries the DB and asserts a user row with `content="X"` AND an advisor row with `content="<concat>"` exist for the meeting.
- [x] 4.4 [P] Write `packages/backend/tests/sessions/test_router_advice.py::test_request_advice_button_path_inserts_pair_with_default_user_content` — RED test with mock advisor yielding 1 string; client sends legacy `request_advice` (no user_question) with locale `zh-TW`; asserts `advice_done` arrives; asserts DB has a user row with `content="請給出戰術建議。"` and an advisor row with the streamed content.
- [x] 4.5 [P] Write `packages/backend/tests/sessions/test_router_advice.py::test_failed_advice_writes_no_chat_message_rows` — RED test with `_RaisingAdvisor(ResourceExhausted("quota"))`; client sends `chat_message`; asserts `advisor_failed` frame received; asserts DB has zero new chat_message rows for the meeting.
- [x] 4.6 [P] Write `packages/backend/tests/sessions/test_router_advice.py::test_chat_message_cancels_prior_in_flight_advice_no_pair_written_for_cancelled` — RED test with `_SlowAdvisor` that hangs; client sends `chat_message` with content="A"; immediately sends second `chat_message` with content="B" (cancels first); replace advisor mid-test with one that completes for B; asserts only ONE pair (user="B" + advisor=...) exists in DB; asserts NO row with content="A" exists.
- [x] 4.7 [P] Write `packages/backend/tests/sessions/test_router_advice.py::test_chat_message_passes_chat_history_from_db_to_advise` — RED test with mock advisor whose `advise` records the `chat_history` it received; pre-seed the DB with 2 chat_message rows; client sends `chat_message`; asserts the advisor's recorded `chat_history` argument equals the pre-seeded rows in `created_at` order.
- [x] 4.8 Update `packages/backend/meeting_playbook/sessions/router.py` — in `_client_listener`, add a branch for `ChatMessageRequestMessage`: cancel any in-flight `advice_task` (suppress `CancelledError`/`Exception`), then `advice_task = asyncio.create_task(_run_advice(msg.request_id, user_question=msg.content, locale=msg.locale), name=f"advice-{msg.request_id}")`. In `_run_advice`, change the signature to accept `user_question: str | None` (button path passes `None`, chatbox path passes the chatbox content). At the start of `_run_advice`, derive `user_content = user_question if user_question else _DEFAULT_PROMPT[locale]` where `_DEFAULT_PROMPT = {"zh-TW": "請給出戰術建議。", "en": "Please give tactical advice."}`. Inside the same `session_factory()` context, fetch `chat_history = await ChatMessageRepository(advice_session).list_for_meeting(meeting_id)`. Pass `chat_history` to `tactical_advisor.advise(...)`. Accumulate yielded tokens into `advisor_content`. After the `async for` completes (and BEFORE sending `advice_done`), call `await ChatMessageRepository(advice_session).insert_pair_after_advice(meeting_id, user_content=user_content, advisor_content=advisor_content)`. Wrap the INSERT in its own try/except with `logger.warning` on failure (do not propagate — `advice_done` SHALL still be sent). Drive 4.3-4.7 GREEN.
- [x] 4.9 [P] Write `packages/backend/tests/sessions/test_router_advice.py::test_advice_during_active_capture_with_chat_message_no_session_collision` — RED test (mirror of slice-8's session-isolation test) using `chat_message` frame instead of `request_advice`; runs concurrent capture + chat_message advice over a 5-second window; asserts no SQLAlchemy `InvalidRequestError` and both `transcript_chunk` AND `advice_chunk` flow.

## 5. Frontend: AdvisorPane chatbox + history hydration + WS types + reducer extension

- [x] 5.1 [P] Write `packages/web/src/lib/session-ws.test.ts::test_chat_message_client_frame_serializes` — RED test asserting that calling `sock.send({type: "chat_message", request_id: "r1", content: "X", locale: "zh-TW"})` results in the expected JSON payload on the underlying WebSocket.
- [x] 5.2 Update `packages/web/src/lib/session-ws.ts` — add a new TS type `ChatMessageRequestMessage = {type: "chat_message"; request_id: string; content: string; locale: "zh-TW" | "en"}` and add it to the `ClientMessage` discriminated union. Drive 5.1 GREEN.
- [x] 5.3 [P] Add `packages/web/src/lib/chat-api.ts` exporting `chatMessagesQueryOptions(meetingId): UseQueryOptions<ChatMessage[]>` (queryKey `["chat_messages", meetingId]`, queryFn fetches `/api/meetings/{id}/chat_messages`, throws `ChatApiError` on non-200). Export `ChatMessage` TS type matching the backend response shape (`{id, meeting_id, role: "user" | "advisor", content, created_at}`).
- [x] 5.4 [P] Write `packages/web/src/lib/chat-api.test.ts::test_chatMessagesQueryOptions_fetches_and_parses_array` — RED test mocking `fetch` to return a 3-element array; asserts the query resolves with the parsed array; asserts non-200 throws `ChatApiError`.
- [x] 5.5 [P] Add `packages/web/src/components/chat-message-list.tsx` — props `{messages: ChatMessage[]; meDisplayName: string}`. Renders one bubble per message: user-role messages right-aligned with `data-role="user"`, advisor-role messages left-aligned with `data-role="advisor"` and `<MarkdownPreview source={content} />` for body. Auto-scrolls to bottom on `messages.length` change. Empty `messages` renders nothing (parent decides empty-state).
- [x] 5.6 [P] Write `packages/web/src/components/chat-message-list.test.tsx::test_renders_one_bubble_per_message_with_role_data_attribute` — RED test with 4 mixed messages; asserts 4 bubbles render in source order; user bubbles have `data-role="user"`, advisor bubbles have `data-role="advisor"`.
- [x] 5.7 [P] Add `packages/web/src/components/chat-input.tsx` — props `{onSend: (content: string) => void; disabled: boolean}`. Renders a `<textarea>` + a `<Button>{t("meetings.advisor.send")}</Button>`. Send is disabled when textarea content trimmed is empty OR `disabled` prop is true. `Cmd+Enter` (macOS) / `Ctrl+Enter` triggers send (calls `onSend(content)` and clears the textarea). Below the textarea, render a small grey `t("meetings.advisor.cmdEnterHint")` line.
- [x] 5.8 [P] Write `packages/web/src/components/chat-input.test.tsx::test_send_button_disabled_when_empty_and_when_prop_disabled_true` — RED test asserting Send is disabled with empty textarea; typing makes it enabled; passing `disabled=true` re-disables it regardless of content.
- [x] 5.9 [P] Write `packages/web/src/components/chat-input.test.tsx::test_cmd_enter_calls_onSend_and_clears_textarea` — RED test typing "X" then firing `keydown` with `metaKey: true, key: "Enter"`; asserts `onSend("X")` called once; asserts textarea value cleared.
- [x] 5.10 Update `packages/web/src/hooks/use-meeting-session.ts` — replace the existing `advisor: { requests: AdviceRequest[] }` reducer slice with `advisor: { messages: ChatMessage[]; inFlight: { requestId: string; userContent: string; advisorTokens: string; status: "streaming" | "failed"; error?: { code: string; message: string } } | null }`. Add reducer cases: `HISTORY_LOADED` (replace `messages`), `USER_MESSAGE_SENT` (set `inFlight = {requestId, userContent, advisorTokens: "", status: "streaming"}`), `ADVICE_TOKEN` (append to `inFlight.advisorTokens` if `inFlight?.requestId === payload.requestId`), `ADVICE_DONE` (clear `inFlight`; rely on React Query refetch to insert into `messages`), `ADVICE_FAILED` (set `inFlight.status = "failed"` + error). Add new callbacks: `sendChatMessage(content: string): void` (generates uuid, dispatches USER_MESSAGE_SENT, sends WS frame `chat_message`); `requestAdvice(): void` (Slice 8 callback retained; dispatches USER_MESSAGE_SENT with `userContent = t("meetings.advisor.defaultPromptText")` then sends `request_advice` frame).
- [x] 5.11 [P] Write `packages/web/src/hooks/use-meeting-session.test.tsx::test_history_loaded_replaces_messages` — RED test dispatching `HISTORY_LOADED` with 3 ChatMessage rows; asserts `state.advisor.messages` length 3.
- [x] 5.12 [P] Write `packages/web/src/hooks/use-meeting-session.test.tsx::test_send_chat_message_sends_frame_with_uuid_request_id_and_locale` — RED test calling `result.current.sendChatMessage("X")` after meeting_started; asserts the WebSocket received a frame `{type: "chat_message", request_id: <non-empty>, content: "X", locale: "zh-TW"}`.
- [x] 5.13 [P] Write `packages/web/src/hooks/use-meeting-session.test.tsx::test_advice_chunk_appends_to_inflight_advisorTokens` — RED test driving `sendChatMessage("Q")` then simulating 3 `advice_chunk` for the same request_id; asserts `state.advisor.inFlight.advisorTokens === "abc"` (token concatenation).
- [x] 5.14 [P] Write `packages/web/src/hooks/use-meeting-session.test.tsx::test_advice_done_clears_inflight` — RED test driving the full lifecycle through `advice_done`; asserts `state.advisor.inFlight === null` after `advice_done`.
- [x] 5.15 Rewrite `packages/web/src/components/advisor-pane.tsx` — replace the slice-8 cards rendering with three vertical sections: (1) at top, render `<ChatMessageList messages={[...session.state.advisor.messages, ...inFlightAsMessage]} meDisplayName={meeting.me_display_name} />` where `inFlightAsMessage` is computed from `state.advisor.inFlight` (a virtual user+advisor pair appended to the persisted list while streaming/failed); (2) in the middle, render a `<Button data-testid="get-advice-button">{t("meetings.advisor.getAdviceButton")}</Button>` calling `session.requestAdvice()` ONLY when `phase === "in_progress"` (else not rendered); button disabled when `inFlight?.status === "streaming"`; (3) at the bottom, render `<ChatInput onSend={session.sendChatMessage} disabled={inFlight?.status === "streaming"} />` ONLY when `phase === "in_progress"` (else not rendered). Failed `inFlight` SHALL render with `data-status="failed"` and a Retry button calling `session.requestAdvice()` or `session.sendChatMessage(inFlight.userContent)` matching the originating path.
- [x] 5.16 [P] Write `packages/web/src/components/advisor-pane.test.tsx::test_renders_persisted_messages_from_state` — REWRITE existing test (Slice 8 cards-based assumption no longer holds). RED test stubbing session with 4 persisted messages; asserts 4 bubbles render in order with correct `data-role` attributes.
- [x] 5.17 [P] Write `packages/web/src/components/advisor-pane.test.tsx::test_chatbox_textarea_and_send_visible_only_in_progress` — RED test in `idle` phase asserts no textarea + no Send button + no Get Advice button BUT history bubbles still render if any; in `in_progress` phase asserts all three visible.
- [x] 5.18 [P] Write `packages/web/src/components/advisor-pane.test.tsx::test_send_disabled_during_inflight_streaming` — RED test stubbing `inFlight.status = "streaming"`; asserts both Get Advice and Send buttons have `disabled` attribute.
- [x] 5.19 [P] Write `packages/web/src/components/advisor-pane.test.tsx::test_failed_inflight_shows_retry_button_calling_correct_send_path` — RED test with `inFlight.status = "failed"` and a recorded `userContent = "X"`; asserts a retry button exists; clicking it calls `session.sendChatMessage("X")` once.
- [x] 5.20 Update `packages/web/src/routes/meetings/detail.tsx` — add `useQuery(chatMessagesQueryOptions(meetingId))` next to the existing transcript chunks query; pass the resolved `data` into the session hook (e.g., via a new `useEffect(() => session.loadHistory(data), [data])` that dispatches `HISTORY_LOADED`). On `advice_done` (signalled by inFlight transitioning to null), call `queryClient.invalidateQueries({queryKey: ["chat_messages", meetingId]})` to refetch.
- [x] 5.21 [P] Write `packages/web/src/routes/meetings/detail.test.tsx::test_advisor_pane_renders_persisted_chat_history_on_mount` — RED test mocking the GET `/chat_messages` endpoint to return 2 messages; asserts both bubbles render in the AdvisorPane after the page mounts (use `waitFor`).
- [x] 5.22 [P] Write `packages/web/src/routes/meetings/detail.test.tsx::test_advice_done_invalidates_chat_messages_cache` — RED test driving the WS through `advice_done`; asserts a follow-up GET `/chat_messages` request fires (via the mocked fetch counter).

## 6. i18n + agent doc + verification

- [x] 6.1 [P] Update `packages/web/src/locales/zh-TW.json` AND `packages/web/src/locales/en.json` (same commit, both files): add `meetings.advisor.send` (`傳送` / `Send`), `meetings.advisor.chatPlaceholder` (`輸入問題…` / `Type your question…`), `meetings.advisor.defaultPromptText` (`請給出戰術建議。` / `Please give tactical advice.`), `meetings.advisor.userLabel` (`我` / `Me`), `meetings.advisor.advisorLabel` (`Advisor` / `Advisor`), `meetings.advisor.cmdEnterHint` (`⌘+Enter 送出` / `⌘+Enter to send`).
- [x] 6.2 Run `cd packages/web && bun test src/locales/locales.test.ts`; assert deep-equal between `zh-TW.json` and `en.json` passes (the locale-drift guard).
- [x] 6.3 [P] Update `docs/agents/advisor.md` — add a new section "Chat history (Slice 9)" documenting the chat_message table schema, the chatbox WS frame, the chat_history prompt section, the INSERT-pair-on-success policy, and the React Query hydration pattern. Update the Slice 9 upgrade path section to reflect the implemented design (delete the "future work" framing; replace with "implemented in Slice 9 — see chat_message table + ChatMessageRepository").
- [x] 6.4 Run the full backend test suite: `cd packages/backend && uv run pytest` — all new chat repo / router / migration / advisor multi-turn / router-advice tests pass alongside existing 234+.
- [x] 6.5 Run the full web test suite: `cd packages/web && bun test` — new chat-input / chat-message-list / advisor-pane (rewritten) / use-meeting-session (rewritten) / session-ws / detail tests pass; pre-existing 8 baseline failures may remain (no NEW failures introduced).
- [x] 6.6 Run `cd packages/backend && uv run ruff check meeting_playbook tests` — no new lint violations introduced (pre-existing RUF unicode warnings tolerable).
- [x] 6.7 Manual smoke-test (Sean): start the dev stack, open a meeting, transition to `in_progress`. Verify (a) clicking `Get Advice` produces the same streaming behavior as Slice 8 PLUS persists a user+advisor pair to the chat_message history list above; (b) typing in the chatbox + Cmd+Enter sends the question, streams the response, and adds the pair to the list; (c) refreshing the browser preserves all persisted messages but loses any in-flight one; (d) sending a second chatbox message while the first is streaming cancels the first (no row written for the cancelled one) and starts the new stream; (e) ending the meeting hides the input controls but keeps history visible; (f) navigating away to the meetings list and back to the detail page still shows the same chat history.
