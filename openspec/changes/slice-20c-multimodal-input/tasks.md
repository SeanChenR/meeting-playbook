## 1. Schema + 依賴 + config 落地

- [x] 1.1 [P] 新增 Alembic migration `packages/backend/alembic/versions/0014_add_attachment_hash_snapshot.py`：對 `playbook` 與 `summary` 兩張 table 各 `ADD COLUMN attachment_hash_snapshot TEXT NULL`，down 是 `DROP COLUMN`。**Behavior**：upgrade 後 psql `\d playbook` 與 `\d summary` 看得到新欄位且既有 row 對應欄位為 NULL；downgrade 後欄位消失，落實 spec 中 `summary table persists per-meeting markdown summary` 對五欄位 schema 的描述。**Verify**：新測 `packages/backend/tests/test_alembic_attachment_hash_snapshot.py` 跑 upgrade → downgrade → upgrade 三輪、查 information_schema 確認欄位可逆。
- [x] 1.2 [P] 在 `packages/backend/pyproject.toml` 加 `pypdf>=4.0` 與 `python-docx>=1.0` dependency；同步 `uv.lock`（`uv sync` 後 commit lockfile）。**Behavior**：`uv run python -c "import pypdf; import docx; print('ok')"` 印出 `ok`，是 `AttachmentProcessor 採 type-dispatched single-method 設計` 落地的前置依賴。**Verify**：上述 import 指令 exit 0 且 lockfile diff 含兩個套件條目。
- [x] 1.3 [P] 在 `packages/backend/meeting_playbook/config.py` 加 `attachment_text_extraction_timeout_seconds: int = 15`，並在 `.env.example` 加對應 commented section。**Behavior**：`Settings()` instance 有此 attribute 預設 `15`，可透過 `ATTACHMENT_TEXT_EXTRACTION_TIMEOUT_SECONDS=30` env 覆寫；落實 design 章節 `MultimodalContextBuilder：固定順序、單筆 corrupt attachment 不擋整次生成` 中對 timeout 的設定。**Verify**：`grep ATTACHMENT_TEXT_EXTRACTION_TIMEOUT_SECONDS .env.example` 命中 + `uv run pytest packages/backend/tests/test_config.py -k timeout` 通過（新 unit test 驗 default 與 override）。

## 2. AttachmentProcessor 深模組（TDD red → green）

- [x] 2.1 [P] 在 `packages/backend/tests/attachments/test_processor.py` 先寫 4 條 red test：(a) `image/png` → 回 `ProcessedAttachment(kind="image", bytes is not None, extracted_text is None, sha256 != "")`；(b) `application/pdf` happy → 回 `kind="text"` 且 `extracted_text == "Q3 briefing"`（mock pypdf）；(c) corrupt PDF → raise `AttachmentProcessingError(error_code="attachment.extraction_failed", ...)`；(d) docx happy → 回 `kind="text"` 且 `extracted_text` 含段落內容。落實 spec 中 `PlaybookGenerator skips unprocessable attachments instead of failing the whole call` 與 design 章節 `AttachmentProcessor 採 type-dispatched single-method 設計`。**Verify**：`uv run pytest packages/backend/tests/attachments/test_processor.py` 在尚無實作時 4 條全紅。
- [x] 2.2 在 `packages/backend/meeting_playbook/attachments/processor.py` 與 `exceptions.py` 實作 `ProcessedAttachment` dataclass、`AttachmentProcessor.process(...)`、`AttachmentProcessingError`。**Behavior**：四個 kind 分支 dispatch 行為與 2.1 預期一致；PDF / docx 抽文字超過 `attachment_text_extraction_timeout_seconds` 一律 raise `AttachmentProcessingError("attachment.extraction_timeout", ...)`。**Verify**：2.1 的 4 條測試由紅轉綠 + 補一條 timeout test（mock 抽文字 sleep > timeout）通過。

## 3. MultimodalContextBuilder 深模組（TDD 4 條 table-driven）

- [x] 3.1 [P] 在 `packages/backend/tests/attachments/test_multimodal_context.py` 寫 4 條 parametrize red test 對應 design 章節 `TDD 四條主路徑 table-driven`：`no_attachments` / `single_image` / `single_pdf` / `mixed`，各驗 `parts` 結構（image part 在 text part 前、`Part.from_bytes` vs `Part.from_text` 類型）、`snapshot_hash` 對相同 input 為 deterministic、對 input 順序 invariant、對附件內容變動敏感、空集合對應 canonical empty-set hash `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`。**Verify**：尚無實作時 4 條全紅。
- [x] 3.2 在 `packages/backend/meeting_playbook/attachments/multimodal_context.py` 實作 `MultimodalContext` dataclass + `MultimodalContextBuilder.build(text_context, attachments)`，固定順序「images first → combined text → trailing system text_context」。**Behavior**：對每個 attachment 呼叫 `AttachmentProcessor.process`，若 raise `AttachmentProcessingError` 則 log structured warning 含 `attachment_id` + `error_code`，加進 `skipped_attachment_ids`，繼續其他附件；對應 spec `PlaybookGenerator skips unprocessable attachments instead of failing the whole call` 與 design 章節 `MultimodalContextBuilder：固定順序、單筆 corrupt attachment 不擋整次生成`。**Verify**：3.1 的 4 條測試由紅轉綠；補一條「mixed 中 corrupt PDF」test 驗收 `skipped_attachment_ids == [corrupt_pdf.id]` 且 `snapshot_hash` 只反映剩餘附件、structured log entry 含正確 fields。

## 4. PlaybookGenerator 多模態接入（playbook-generation）

- [x] 4.1 [P] 在 `packages/backend/tests/playbook_generation/test_generator_multimodal.py` 寫 5 條 red test 對應 design 章節 `Generator 邊界：PlaybookGenerator 顯式接 attachment_refs；Summarizer 自己查 repo` 與 `Gemini multimodal call：用 google-genai 既有 types.Part.from_bytes + types.Part.from_text`：(a) 空 `attachment_refs` → `contents` 為純字串、回傳 dict 的 `attachment_hash_snapshot` 等於 canonical empty-set hash；(b) 1 PNG → `contents` 為 list、首元素是 `Part.from_bytes(mime_type="image/png")`；(c) 1 PDF → `contents` 為 list、`Part.from_text` 內含 `[Attachment: foo.pdf]` 標題與抽出文字；(d) 順序 invariant：兩個排列同 set 產出相同 `attachment_hash_snapshot`；(e) 一筆 corrupt PDF 之外其他 ok → call 成功、`attachment_hash_snapshot` 只反映 ok 那些。落實 spec requirements `PlaybookGenerator accepts an optional sequence of meeting attachments`、`PlaybookGenerator returns an attachment_hash_snapshot alongside the seven-field draft`、`PlaybookGenerator skips unprocessable attachments instead of failing the whole call`、`Generator uses Vertex AI Gemini 2.5 Pro through the official SDK`。**Verify**：5 條紅。
- [x] 4.2 擴 `packages/backend/meeting_playbook/playbook_generation/generator.py` 與 `prompts.py`：`PlaybookGenerator.generate(event_record, attachment_refs: Sequence[AttachmentRef] = ())` 內部走 `MultimodalContextBuilder.build(...)`、把 `parts` 餵 `client.models.generate_content`、回 dict 多帶 `attachment_hash_snapshot` key（值是 builder 的 `snapshot_hash`）。**Behavior**：既有七欄位 structured-output schema 不動；空 `attachment_refs` 路徑與 pre-S20c 行為 byte-identical（同 prompt 字串、同 `contents` 為純 text）。**Verify**：4.1 五條 + 既有 `test_generator.py` 全部綠。
- [x] 4.3 擴 `packages/backend/meeting_playbook/playbook_management/repository.py` 與 `models.py`：`upsert_for_meeting(payload)` 認 optional `attachment_hash_snapshot` key、寫入新 column；新增 `get_with_stale_flag(meeting_id) -> PlaybookWithStale | None`，is_stale 計算把當前附件 hash 與 row.snapshot 比對（NULL row 視為 canonical empty-set hash）。落實 spec `Generator output is suitable for direct upsert through the existing playbook repository` 與 `Playbook is_stale flag reacts to attachment set changes`，以及 design 章節 `Stale-tracking：附件集合 hash snapshot 寫進 playbook / summary row`。**Verify**：在 `packages/backend/tests/playbook_management/test_repository.py` 加 5 條 test 覆蓋 spec 內 5 個 stale scenario（unchanged / add / delete / byte-identical replace / legacy NULL no atts / legacy NULL with atts）並全綠。

## 5. VertexProSummarizer 多模態接入（meeting-summary）

- [x] 5.1 [P] 在 `packages/backend/tests/summarization/test_summarizer_multimodal.py` 寫 5 條 red test：(a) 0 attachments → `contents` 為純字串、回傳 `SummaryGenerationResult(markdown, attachment_hash_snapshot == canonical empty-set hash)`；(b) 1 PDF → `contents` 為 list；(c) 1 PNG + 1 PDF → 首 Part 是 `Part.from_bytes`、後續 text part 含 PDF 抽出文字 + 既有四節 context；(d) 90 秒 timeout 在帶附件路徑仍生效（mocked Vertex sleep 100s）；(e) 1 corrupt PDF + 1 good PNG → call 成功且 `attachment_hash_snapshot` 只 hash PNG、warning log 命中。落實 spec `MeetingSummarizer module exposes a single coroutine summarize()`。**Verify**：5 條紅。
- [x] 5.2 擴 `packages/backend/meeting_playbook/summarization/vertex_summarizer.py` 與 `prompts.py`：summarize fetch 區塊加 `MeetingAttachmentRepository.list_for_meeting(meeting_id)`、傳給 `MultimodalContextBuilder.build(...)`、`client.models.generate_content(contents=parts_or_text, ...)`、回 `SummaryGenerationResult(markdown=..., attachment_hash_snapshot=builder.snapshot_hash)`；既有四節 heading 驗證不動。落實 design 章節 `Generator 邊界：PlaybookGenerator 顯式接 attachment_refs；Summarizer 自己查 repo` 與 design 章節 "Gemini multimodal call：用 google-genai 既有 `types.Part.from_bytes` + `types.Part.from_text`"（image part 走 `Part.from_bytes`、抽出文字走 `Part.from_text`，不採 Files API）。**Verify**：5.1 五條 + 既有 `test_vertex_summarizer.py` 全綠。
- [x] 5.3 擴 `packages/backend/meeting_playbook/summarization/repository.py` 與 `models.py`：`Summary` model 加 `attachment_hash_snapshot: str | None`；`upsert(meeting_id, markdown, attachment_hash_snapshot)` 寫新 column；`get_with_stale_flag` 加第 4 條 stale 條件（附件 hash 比對）。落實 spec `summary table persists per-meeting markdown summary` 與 `Summary is_stale flag reacts to attachment set changes`。**Verify**：在 `packages/backend/tests/summarization/test_repository.py` 加 5 條 test 覆蓋 spec 內 5 個 attachment-axis scenario 並全綠；既有 transcript / playbook / chat 三軸 staleness test 不破。
- [x] 5.4 擴 `packages/backend/meeting_playbook/summarization/runtime.py` 中 `_run_summary_task` 把 `summarizer.summarize(meeting_id)` 回的 `SummaryGenerationResult` 解構傳給 `SummaryRepository.upsert(meeting_id=..., markdown=result.markdown, attachment_hash_snapshot=result.attachment_hash_snapshot)`。落實 spec `Summary generation runtime serializes per-meeting work via in-process registry`。**Verify**：既有 `test_runtime.py` 既 3 條 scenario + 新增「成功時 row 內 attachment_hash_snapshot 等於 summarizer 回值」test 全綠；既有 9 條 router test 不破。

## 6. 前端 stale banner（summary + playbook pane）

- [x] 6.1 [P] 在 `packages/web/src/locales/zh-TW.json` 與 `packages/web/src/locales/en.json` 同步新增 keys `summary.stale.attachments_changed`、`summary.stale.regenerate_button`、`playbook.stale.attachments_changed`、`playbook.stale.regenerate_button`（zh-TW 範例：「附件已更新 — 重新生成摘要？」「重新生成」；en 範例："Attachments changed — regenerate summary?" / "Regenerate"）。落實 spec `Summary pane shows a localized stale banner when attachments change` 的 i18n 條件。**Verify**：`bun --filter @meeting-playbook/web test src/locales/locales.test.ts` deep-equal 通過。
- [x] 6.2 擴 `packages/web/src/components/summary-pane.tsx`：當 GET response 帶 `is_stale === true` 時於 markdown 之上 render banner（reuse `<Alert>` shadcn 元件）含 `t("summary.stale.attachments_changed")` 文字 + `t("summary.stale.regenerate_button")` 按鈕，按鈕 onClick 觸發既有 POST `/api/meetings/{id}/summary` mutation；pending shape 與 `is_stale === false` 不顯示。落實 spec `Summary pane shows a localized stale banner when attachments change` 的全部三個 scenario。**Verify**：擴 `packages/web/src/components/summary-pane.test.tsx` 加 3 條 component test 對應 spec 三 scenario 並全綠。
- [x] 6.3 對 `packages/web/src/components/playbook-pane.tsx` 做相同處理（is_stale → banner + regenerate 按鈕呼叫既有 playbook 重新生成 endpoint）。**Behavior**：與 6.2 對稱、文字鍵走 `playbook.stale.*`。**Verify**：擴 component test 驗 `is_stale: true` 顯示 banner、`is_stale: false` 不顯示、pending 不顯示，3 條全綠。

## 7. End-to-end + 覆蓋率

- [x] 7.1 新增 `packages/backend/tests/integration/test_playbook_with_attachments.py`：跑 PlaybookGenerator e2e（real PDF fixture + real PNG fixture）、mock Vertex client、驗 Vertex 收到 `contents` 為 `list[Part]` 且 PNG bytes 與 PDF 抽出文字皆在內、router 端 upsert 後 `playbook.attachment_hash_snapshot` 為 builder 回的 hash、`get_with_stale_flag` 第一次 false，新增第 2 個附件後 true。落實 spec `Playbook is_stale flag reacts to attachment set changes`。**Verify**：`uv run pytest packages/backend/tests/integration/test_playbook_with_attachments.py` 全綠。
- [x] 7.2 新增 `packages/backend/tests/integration/test_summary_with_attachments.py`：跑 summarizer e2e（fixture meeting with transcript + playbook + chat + 1 PDF + 1 PNG）、mock Vertex、驗 `summary.attachment_hash_snapshot` 寫入正確、`get_with_stale_flag` 在「新增第 3 個附件」時翻 true，「byte-identical replace」時保持 false。落實 spec `Summary is_stale flag reacts to attachment set changes` 與 `MeetingSummarizer module exposes a single coroutine summarize()`。**Verify**：`uv run pytest packages/backend/tests/integration/test_summary_with_attachments.py` 全綠。
- [x] 7.3 跑覆蓋率 `uv run pytest --cov=meeting_playbook.attachments --cov=meeting_playbook.playbook_generation --cov=meeting_playbook.summarization --cov-report=term-missing` 並確認 `meeting_playbook.attachments` 80%+、本 slice 改動到的 generator / summarizer / repository 範圍 80%+。**Verify**：覆蓋率報告數字達標；若未達標，補單元測試直到達標再 commit。
