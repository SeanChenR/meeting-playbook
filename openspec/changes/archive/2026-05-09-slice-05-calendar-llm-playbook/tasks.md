## 1. Spike — Better Auth scope linking

- [x] 1.1 Action for "Calendar OAuth scope grant flow": spike whether Better Auth's `linkSocial({ provider: "google", scopes: [...] })` can add `https://www.googleapis.com/auth/calendar.events.readonly` to an already-linked Google account WITHOUT forcing the user to log out. Run a 30-minute experiment in `packages/auth/src/scratch/scope-link-spike.ts`; on result, write findings into `docs/adr/0027-calendar-scope-link.md` (status: Accepted) deciding whether to use `linkSocial` OR fall back to a hand-rolled OAuth callback. Delete the scratch file after the ADR is committed.

## 2. Backend — Internal token-store endpoints in the auth gateway

- [x] 2.1 [AC: Requirement "Calendar OAuth scope is granted via a separate connect flow"] Action for "TokenStore — Python ↔ TS gateway internal endpoint" (RED): add `packages/auth/src/__tests__/calendar-token-internal.test.ts` covering `GET /__internal__/users/:id/calendar-token` and `POST /__internal__/users/:id/refresh-calendar-token` — both endpoints reject without `X-Internal-Auth: <secret>` header (401), accept with valid header, return token from Better Auth account row, and the refresh endpoint exchanges the stored refresh_token via the Google token endpoint and updates the row.
- [x] 2.2 Implement the two internal endpoints in `packages/auth/src/server.ts`; bind only on `127.0.0.1`; secret read from `BACKEND_INTERNAL_AUTH_SECRET` env var; for the refresh endpoint use Google's `https://oauth2.googleapis.com/token` exchange (httpx-style fetch) and update `accessToken` + `accessTokenExpiresAt` in the Better Auth `account` table via `pg.Pool`. 2.1 turns green.
- [x] 2.3 [AC: Requirement "Calendar OAuth scope is granted via a separate connect flow"] Action for "Calendar OAuth scope grant flow" (RED): add `packages/auth/src/__tests__/calendar-link.test.ts` covering `POST /api/auth/calendar/link` (initiates OAuth with calendar.events.readonly scope; redirect target inspectable in test) and `GET /api/auth/calendar/status` (`{connected: true}` when account row has calendar scope; `{connected: false}` otherwise).
- [x] 2.4 Implement `POST /api/auth/calendar/link` and `GET /api/auth/calendar/status` in `packages/auth/src/server.ts` per the ADR-0027 decision (linkSocial path OR hand-rolled callback). 2.3 turns green.

## 3. Backend — CalendarClient (covers "CalendarClient (Python) wraps Google Calendar API")

- [x] 3.1 [AC: Requirement "GET upcoming events returns the user's events ordered by start time"] Action for "CalendarClient (Python) wraps Google Calendar API" (RED): add `packages/backend/tests/calendar/test_client.py` mocking `googleapiclient.discovery.build` and asserting `get_upcoming_events(user_id, hours=24)` (1) returns events ordered by start ascending, (2) handles a two-page response by following `pageToken` until both pages are consumed, (3) returns an empty list when Google returns no items, (4) on first 401 calls TokenStore.refresh and retries, (5) on second 401 raises `CalendarTokenExpired`.
- [x] 3.2 Add `packages/backend/meeting_playbook/calendar/{__init__,client.py,token_store.py}.py`; implement `CalendarClient`, `TokenStore` (httpx client hitting the gateway internal endpoints with `BACKEND_INTERNAL_AUTH_SECRET`), and the `CalendarEvent` dataclass. 3.1 turns green.
- [x] 3.3 [AC: Requirement "Calendar failure modes surface localizable error codes"] Extend `tests/calendar/test_client.py` covering the network-error path: persistent HTTP 503 from Google causes `CalendarNetworkError`; never-connected user causes `CalendarNotConnected`. Map both into the matching exception types in `client.py`.

## 4. Backend — PlaybookGenerator (covers "PlaybookGenerator (deep module) calls Gemini 2.5 Pro on Vertex AI" and "Prompt engineering for sparse events")

- [x] 4.1 [AC: Requirement "Generator produces all seven playbook fields with non-empty content for any Calendar event input"] Action for "PlaybookGenerator (deep module) calls Gemini 2.5 Pro on Vertex AI" (RED, fixtures): create six event JSON fixtures under `packages/backend/tests/playbook_generation/fixtures/`: `rich.json`, `sparse.json`, `long.json`, `zh.json`, `en.json`, `mixed.json`. Then add `tests/playbook_generation/test_generator.py` with one parametrized test per fixture that mocks the `google-genai` client to return a deterministic 7-field JSON response and asserts `generate(event)` returns a draft with all seven fields non-empty (per the spec's "minimum non-empty contract per field" example table) and `free_form_markdown` ≥ 3 lines.
- [x] 4.2 [AC: Requirements "Generator uses Vertex AI Gemini 2.5 Pro through the official SDK" and "Generator output is suitable for direct upsert through the existing playbook repository"] Add `packages/backend/meeting_playbook/playbook_generation/{__init__,generator.py,prompts.py}.py`. Implement `BUILD_PRIMARY_PROMPT(event) -> str`, structured-output mode (`response_schema` with seven string keys via `google-genai` SDK against Vertex AI; the test in `test_generator.py` MUST grep the source to confirm no non-Vertex provider import exists). The `generate(event) -> PlaybookDraft` return shape MUST match the `PlaybookUpsertPayload` keys exactly so callers can pass it straight to `PlaybookRepository.upsert_for_meeting` without remapping; add an explicit test that round-trips a generator output through the repository upsert. 4.1 turns green.
- [x] 4.3 [AC: Requirement "Generator produces all seven playbook fields with non-empty content for any Calendar event input"] Action for "Prompt engineering for sparse events" (RED): add a test in `test_generator.py` that mocks the client to return an empty `objective` on the first call and a non-empty value on the second, and asserts `generate(event)` triggered the fallback prompt path (second call) and returned a non-empty `objective`. Implement the fallback path in `generator.py`. Add a third sub-test where both calls return empty `red_lines` and assert the final draft fills in a localized "(請自行填寫)" sentinel string per the design's third-fallback rule.
- [x] 4.4 [AC: Requirement "Generator surfaces localizable failure codes for upstream and parsing errors"] Add tests in `test_generator.py` for the failure paths: simulating a 60-second hang raises `PlaybookGenerationTimeout`; a malformed-JSON response raises `PlaybookGenerationFailed`. Implement the timeout via `asyncio.wait_for(..., timeout=60)` and the schema validation via Pydantic in `generator.py`.

## 5. Backend — REST router (covers "REST endpoints")

- [x] 5.1 [AC: Requirements "GET upcoming events returns the user's events ordered by start time" and "Calendar failure modes surface localizable error codes"] Action for "REST endpoints" (RED, GET upcoming): add `packages/backend/tests/calendar/test_endpoints.py` covering `GET /api/calendar/upcoming?hours=24` for an authenticated connected user (HTTP 200 with mocked CalendarClient response), an unauthenticated request (HTTP 401 + `auth.gateway_bypass`), a not-connected user (HTTP 401 + `calendar.not_connected`), token-expired (HTTP 401 + `calendar.token_expired`), and persistent network error (HTTP 502 + `calendar.network_error`).
- [x] 5.2 Add `packages/backend/meeting_playbook/calendar/{router,schemas}.py`; mount in `packages/backend/meeting_playbook/server.py`; the router catches the `CalendarClient` exception types declared in 3.x and maps them to HTTPException(detail={"error_code", "message"}). 5.1 turns green.
- [x] 5.3 [AC: Requirement "POST from-calendar creates a meeting and persists a generated playbook draft"] Action for "REST endpoints" (RED, POST from-calendar): extend `tests/calendar/test_endpoints.py` covering `POST /api/meetings/from-calendar`: success path (creates meeting with `calendar_event_id` populated, persists generated playbook, returns `{meeting_id}`); generator timeout path (meeting persists, playbook untouched, response is HTTP 504 + `playbook.generation_timeout`); cross-user event id path (response surfaces a Calendar-domain error with no leak that the event exists elsewhere).
- [x] 5.4 Implement the `POST /api/meetings/from-calendar` handler in `calendar/router.py`: events.get → MeetingRepository.create → PlaybookGenerator.generate → PlaybookRepository.upsert_for_meeting → return `{meeting_id}`; wrap step 3-4 in try/except so generator failure does NOT roll back step 2. 5.3 turns green.

## 6. Frontend — calendar-api lib

- [x] 6.1 [AC: Requirements "GET upcoming events..." and "POST from-calendar..."] Action for "`lib/calendar-api.ts` query options + mutation hook" (queries half, RED): add `packages/web/src/lib/calendar-api.queries.test.ts` covering `upcomingEventsQueryOptions(hours)` (queryKey `["calendar", "upcoming", hours]`, queryFn calls `GET /api/calendar/upcoming?hours=` with the right value); plus error-mapping tests for `calendar.not_connected`, `calendar.token_expired`, `calendar.network_error` raising a `CalendarApiError` with `errorCode`.
- [x] 6.2 Add `packages/web/src/lib/calendar-api.ts` (fetch wrappers + `CalendarApiError` + query options). 6.1 turns green.
- [x] 6.3 [AC: Requirement "POST from-calendar creates a meeting and persists a generated playbook draft"] Action for "`lib/calendar-api.ts` query options + mutation hook" (mutation half, RED): add `packages/web/src/lib/calendar-api.mutations.test.tsx` using `renderHook` + `QueryClientProvider` to verify `useImportFromCalendarMutation()` invalidates `["meetings"]` on success.
- [x] 6.4 Implement `useImportFromCalendarMutation` in `calendar-api.ts`. 6.3 turns green.

## 7. Frontend — `/calendar` upcoming events page (covers "React `/calendar` upcoming events page")

- [x] 7.1 [AC: Requirement "GET upcoming events returns the user's events ordered by start time" and "Calendar failure modes surface localizable error codes"] Action for "React `/calendar` upcoming events page" (RED): add `packages/web/src/routes/calendar/upcoming.test.tsx` covering "renders rows for fetched events with title and attendee count", "renders Connect Calendar CTA when query errors with `calendar.not_connected`", "clicking import-and-generate on a row dispatches the mutation and on success navigates to `/meetings/$id`", "clicking import on a row that produces `playbook.generation_timeout` shows the localized error and does NOT navigate".
- [x] 7.2 Implement `packages/web/src/routes/calendar/upcoming.tsx` wrapped in `<ProtectedShell>`. Add the route to `packages/web/src/route-tree.tsx` at path `/calendar`. Wire into `packages/web/src/routes/meetings/list.tsx` as a "從 Calendar 匯入" link next to "+ 新會議". 7.1 turns green.

## 8. i18n + docs + scope guard

- [x] 8.1 [P] Add the `calendar.*` namespace to BOTH `packages/web/src/locales/zh-TW.json` and `packages/web/src/locales/en.json` covering: page heading (`calendar.heading`), Connect-Calendar CTA (`calendar.connect.cta`, `calendar.connect.description`), upcoming-events row labels (`calendar.row.attendees`, `calendar.row.import`), import-pending state (`calendar.row.importing`), `errors.calendar.not_connected`, `errors.calendar.token_expired`, `errors.calendar.network_error`, `errors.playbook.generation_timeout`, `errors.playbook.generation_failed`. Update `packages/web/src/locales/locales.test.ts` to assert every required key in both files.
- [x] 8.2 [P] Add `docs/agents/calendar.md` describing the connect-Calendar flow, the internal token-store endpoint contract (Python ↔ gateway), and the `CalendarClient` invariant ("any Python access to Calendar tokens MUST go through TokenStore, which MUST go through the gateway internal endpoint — never read the Better Auth account table directly from Python").
- [x] 8.3 [P] Add `docs/agents/playbook-generation.md` describing the `PlaybookGenerator` invariant ("non-Vertex-AI providers are forbidden in production"), the prompt strategy (primary → fallback → sentinel), the seven-field structured-output contract, and the failure-code mapping.
- [x] 8.4 Update `.env.example` adding `BACKEND_INTERNAL_AUTH_SECRET`, `BACKEND_INTERNAL_AUTH_URL` (default `http://localhost:3001`), confirm `VERTEX_AI_PROJECT` / `VERTEX_AI_LOCATION` exist, and (if Slice 1 has not already) add `GOOGLE_APPLICATION_CREDENTIALS` (path to service account JSON).
- [x] 8.5 Verify all acceptance criteria from the agent brief are checked off in GitHub Issue #7.

## 9. Test strategy roll-up (covers "Test strategy (TDD vertical slice)")

- [x] 9.1 Action for "Test strategy (TDD vertical slice)": run the full test sweep proving the slice satisfies the design's TDD discipline — `cd packages/backend && uv run pytest` (existing 79 + new calendar / playbook_generation tests all green), `bun --filter @meeting-playbook/web test` (existing 114 + new calendar route + calendar-api tests all green), `bun --filter @meeting-playbook/auth test` (existing 24 + new internal-token + calendar-link tests all green). On each suite run, capture the number of new tests added by this slice in the commit message.

## 10. Smoke-feedback follow-ups (ingested 2026-05-09)

- [x] 10.1 [AC: Requirement "POST from-calendar derives display names from session identity and attendee filtering"] Action for "Gateway propagates X-User-Name + X-User-Email alongside X-User-Id" (RED): extend `packages/auth/src/__tests__/gateway.test.ts` (or add `packages/auth/src/__tests__/gateway-user-headers.test.ts`) with three cases — (a) authenticated `/api/meetings` request: forwarded request carries `X-User-Name` and `X-User-Email` matching the session, (b) client-supplied `X-User-Name` / `X-User-Email` headers are discarded and overwritten by session values, (c) public `/api/health` request does NOT receive any of the three identity headers.
- [x] 10.2 Implement the gateway change in `packages/auth/src/server.ts`: in the same block that sets `X-User-Id`, also set `X-User-Name = session.user.name` and `X-User-Email = session.user.email`. 10.1 turns green.
- [x] 10.3 [AC: Requirement "POST from-calendar derives display names from session identity and attendee filtering"] Action for "POST /api/meetings/from-calendar uses session identity for both display names" (RED): extend `packages/backend/tests/calendar/test_endpoints.py` with the spec scenarios as four explicit cases — (a) multi-attendee event picks the OTHER attendee as `counterparty_display_name` (case-insensitive email match), (b) self-organized event with no other attendee falls back to organizer, (c) self-organized event with no organizer falls back to title, (d) `me_display_name` is taken from `X-User-Name` and is NOT the literal `"Me"`.
- [x] 10.4 Implement the changes in `packages/backend/meeting_playbook/calendar/router.py`: replace `me_display = "Me"` and `counterparty = event.organizer or "Calendar event"` with the algorithm described in the design. Read `X-User-Name` and `X-User-Email` via two new dependencies in `meeting_playbook/meetings/dependencies.py` (mirror `get_user_id_dependency`). Add a private helper `_pick_counterparty(event, user_email)` that handles the attendee → organizer → title fallback chain and is independently unit-testable. 10.3 turns green.
- [x] 10.5 [AC: Requirement "POST from-calendar derives display names from session identity and attendee filtering"] Add `tests/calendar/test_pick_counterparty.py` with parametrized cases covering: empty attendees, only-self attendee, multi-attendee with self first, multi-attendee with email casing differences, attendee with displayName vs email-only, organizer-only fallback, title-only fallback.
- [x] 10.6 Update `docs/agents/calendar.md` with two new subsections — (a) "Display-name derivation" describing the X-User-Name / X-User-Email gateway invariant and the attendee → organizer → title fallback chain, (b) "Limitation: Google identity required" stating that the connect flow uses Better Auth `linkSocialAccount` against the google provider, so any user who connects Calendar (including those who originally signed in with email + password) ends up with a Google identity linked on their account, and users with no Google account cannot use Calendar in this slice.
- [x] 10.7 [no-op] Confirm out-of-scope boundaries from the ingest design — "Out of scope: Markdown rendering in the playbook editor" and "Out of scope: non-Google identity providers for Calendar" — are both reflected in `docs/agents/calendar.md` (limitation note) and the project memory (markdown rendering deferred to a separate change). No code changes; mark done after verifying the doc note from 10.6 lands and a memory entry exists.
- [x] 10.8 Re-run the full test sweep proving the ingest changes hold — `cd packages/backend && uv run pytest`, `bun --filter @meeting-playbook/web test`, `bun --filter @meeting-playbook/auth test` — and confirm test counts grew by the expected number of new cases (3 gateway + 4 endpoint scenarios + parametrized `_pick_counterparty` rows).

## 11. Smoke-feedback follow-ups (ingested 2026-05-09 round 2: viewer perspective)

- [x] 11.1 [AC: Requirement "Generator output is written from the signed-in viewer's point of view"] Action for "CalendarEvent gains an organizer_email field" (RED): extend `packages/backend/tests/calendar/test_client.py` with one case asserting that `_event_from_resource` populates `event.organizer_email` from the Google response's `organizer.email` (case: `{"organizer": {"email": "host@x.com", "displayName": "Host"}}` → `event.organizer_email == "host@x.com"`), and one case asserting `organizer_email` defaults to `""` when the Google response omits the email field (subscribed-calendar events).
- [x] 11.2 Implement `organizer_email: str` on the `CalendarEvent` dataclass in `packages/backend/meeting_playbook/calendar/client.py`; update `_event_from_resource` to populate it. 11.1 turns green.
- [x] 11.3 [AC: Requirement "Generator output is written from the signed-in viewer's point of view"] Action for "Role classification" (RED): add `packages/backend/tests/calendar/test_classify_viewer_role.py` parametrized across the three branches and edge cases — (a) viewer matches organizer_email exactly → `organizer`, (b) viewer matches organizer_email with case + whitespace differences → `organizer`, (c) viewer in attendees but not organizer → `attendee`, (d) viewer matches both organizer + attendee (organizer self-invited) → `organizer` wins, (e) viewer in neither → `external`, (f) empty viewer_email → `external`, (g) empty organizer_email + viewer matches an attendee → `attendee`, (h) empty organizer_email + viewer matches no one → `external`.
- [x] 11.4 Implement `classify_viewer_role(event, viewer_email) -> Literal["organizer", "attendee", "external"]` in `packages/backend/meeting_playbook/calendar/identity.py`. 11.3 turns green.
- [x] 11.5 [AC: Requirement "Generator output is written from the signed-in viewer's point of view"] Action for "Prompt prelude" + "PlaybookGenerator.generate signature: (event, viewer_email, viewer_name)" (RED): in `packages/backend/tests/playbook_generation/test_generator.py` (a) update the existing parametrized fixture test to call `generate(event, viewer_email="me@x.com", viewer_name="Me")` (keyword args), (b) add three role-specific cases that capture the prompt string sent to the injected `call_model` callback and assert it contains the role label (`organizer` / `attendee` / `external`) AND the role-specific guidance sentence AND the viewer's name, (c) add an "empty viewer_email" case asserting role classification degrades to `external` and the call still succeeds.
- [x] 11.6 Update `packages/backend/meeting_playbook/playbook_generation/{generator.py,prompts.py}` to: (i) change `generate` signature to `generate(self, event, *, viewer_email, viewer_name)`, (ii) add a private `_render_viewer_block(event, viewer_email, viewer_name)` helper that emits the leading paragraph including viewer name + email + role label + role-specific guidance + others list, (iii) prepend that block to both `build_primary_prompt` and `build_fallback_prompt`. 11.5 turns green. Update the round-1 generator tests that call `generate(event)` positionally — every call site passes viewer_email + viewer_name keyword args.
- [x] 11.7 [AC: Requirement "Generator output is written from the signed-in viewer's point of view"] Action for "Router: pass session identity into the generator" (RED): in `packages/backend/tests/calendar/test_endpoints.py` update the existing four ingest-round-1 scenarios (multi-attendee picks counterparty; falls back to organizer; falls back to title; me_display_name from X-User-Name) to assert the stub `_StubPlaybookGenerator.generate` was called with `viewer_email` matching the X-User-Email header AND `viewer_name` matching the X-User-Name header. Add one new scenario: an event where the viewer is `external` (organizer_email differs, viewer_email NOT in attendees) imports successfully and the generator was invoked with the correct viewer args.
- [x] 11.8 Update `packages/backend/meeting_playbook/calendar/router.py` `import_from_calendar` to pass `viewer_email=user_email` and `viewer_name=user_name` keyword args into `generator.generate(event, ...)`. Update `_StubPlaybookGenerator` in `tests/calendar/test_endpoints.py` to accept and record those args. 11.7 turns green.
- [x] 11.9 Update `docs/agents/playbook-generation.md` with a "Viewer perspective" subsection: name the three roles, the classification rule, the prompt prelude shape, and a note that empty viewer_email degrades to `external` rather than failing.
- [x] 11.10 [no-op] Confirm "Out of scope (still)" boundary from round-2 design — markdown rendering in the playbook editor remains deferred and round-2 changes MUST NOT touch the playbook editor surface (`packages/web/src/components/playbook-pane.tsx`, `packages/web/src/routes/meetings/detail.tsx`). Verify the diff for round-2 contains zero edits to either file before marking done.
- [x] 11.11 Action for "Test impact" — re-run the full test sweep proving the round-2 changes hold — `cd packages/backend && uv run pytest`, `bun --filter @meeting-playbook/web test`, `bun --filter @meeting-playbook/auth test` — and confirm the new test counts (8 classify_viewer_role rows + 3 role-specific generator cases + 1 external endpoint case + organizer_email assertions in test_client) all pass.
